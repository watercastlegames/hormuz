# 진행 기록

## 2026-07-27 — STEP 0 사전 이미지 시안 v1

- 한 일: `HANDOVER.md`와 정본 문서 3종(기획서 v0.5, 화면 구성서 v1.0, 코딩 설계서 v1.0)을 정독하고, 본편 S3의 최고 퀄리티 목표를 보여주는 시네마틱 전체화면 UI 시안 이미지를 생성했다.
- 반영 내용: 상황실 홀로그램 해도, 호르무즈 실지형 근사 구도, 황혼 라이팅, 청록 미군·호박 이란·백색 민간·적색 위험 색상 규칙, 항모전단·고속정·유조선·기뢰밭·미사일 요격 연출, 좌상 날짜/국면·우상 계기판 6개·중하 결정 패널·최하단 속보 티커.
- 결과: `output/step0/hormuz-ui-cinematic-concept-v1.png` (1672×941).
- 남은 일: 사장님 톤·구도 컨펌 후 `mockup.html`을 `?q=basic|rich|cinematic` 3단계로 구현하고 PC 1600×900·모바일 430×932 스크린샷을 생성한다.
- 사장님 확인 필요: 시안의 전체 톤, 3D 전장 밀도, HUD 크기와 배치, 시네마틱 단계의 발광 강도.

## 2026-07-27 — STEP 0 실제 3D 시안 및 생산 인벤토리

- 한 일: 승인된 이미지 시안을 기준으로 전체 게임의 환경 10종·고유 3D 유닛 16종·공용 이펙트 10종·HUD 아이콘 20종·버튼 컴포넌트 15종을 목록화하고, `mockup.html`에 실제 Three.js 3D 시안을 구현했다.
- 구현 내용: 절차형 호르무즈 근사 지형, 단일 패스 물 셰이더, TSS 레인, 도시 불빛, 항모전단·구축함·유조선·IRGC 고속정·기뢰·TEL, 미사일 궤적·요격 섬광·파티클, 자동/자유 카메라, 선택 버튼과 속보 반영, PC/모바일 반응형 HUD.
- 품질 사다리: `?q=basic|rich|cinematic`에 따라 DPR·도시 불빛·유닛·파티클·지형 라인 밀도를 차등 적용했다.
- 검증: 로컬 서버에서 PC 1600×900·모바일 430×932 각 3단계 총 6장 캡처. 브라우저 콘솔 오류 0. `격침 허가` 선택 시 티커 갱신, `AUTO CAM` 토글 동작 확인.
- three.js 참고: r185의 `three.module.js`가 `three.core.js`를 import하므로 두 파일을 함께 동봉했다. CDN·package.json은 사용하지 않았다.
- 남은 일: STEP 0 실제 렌더 톤 컨펌 후 M1 룰 코어. M2에서 Natural Earth 실해안선과 고유 유닛 16종 조형으로 교체해 기준 이미지와의 세부 일치도를 높인다.
- 사장님 확인 필요: 실제 렌더의 카메라 높이, 전장 밝기, HUD 크기, 기본 품질과 시네마틱 품질의 밀도 차이.

## 2026-07-27 — Meshy 고품질 모델 파이프라인 전환

- 한 일: 실제 3D 렌더가 승인 이미지보다 디테일이 낮다는 피드백을 반영해 절차형 유닛을 최종 모델로 사용하지 않기로 했다. `../catAgentGame/`의 Meshy 6 작업 기록·GLB 최적화 스크립트·GLTFLoader 적용 코드를 읽고 동일한 생산 파이프라인을 확정했다.
- 참고 결과: `catAgentGame`은 Meshy 6 image-to-3D, GLB, 4K 텍스처, 40,000 폴리, 조명 제거 설정에서 객체당 30크레딧을 사용했다. 원본 약 13MB를 웹용 약 0.7~2.6MB로 최적화했다.
- 준비한 입력: 니미츠급 항모와 알레이버크급 구축함의 단일 객체 기준 이미지를 생성해 `assets/meshy-references/ships-v1/`에 저장했다.
- 1차 품질 게이트: 위 2종을 Meshy 6 GLB로 생성·웹 최적화·장면 교체해 테스트한다. 예상 비용은 60크레딧이다.
- 남은 일: 사장님의 60크레딧 사용 확인 → Meshy 6 생성 → 다운로드·최적화 → `mockup.html` 교체 → PC/모바일 테스트 캡처.
- 사장님 확인 필요: Meshy 6 image-to-3D 2종, 총 60크레딧 사용 승인.

## 2026-07-27 — Meshy 6 함선 2종 생성·웹 통합 완료

- 사장님이 승인한 60크레딧 범위에서 Meshy 6 image-to-3D를 실행했다. 니미츠급 항공모함 작업 `019fa2c9-b689-7d8c-b3db-2f802de7b000`, 알레이버크급 구축함 작업 `019fa2c9-ddc7-7d95-8aa4-0478159f20bf`가 모두 성공했으며 각 30크레딧, 합계 60크레딧을 사용했다.
- 생성 설정은 GLB, 4K base color, triangle, 40,000 poly 목표, remesh, remove_lighting, PBR 비활성으로 고정했다. 원본 GLB와 별도 4K base-color PNG를 `assets/meshy-source/ships-v1/`에 보존했다.
- `catAgentGame`의 검증된 최적화 방식을 적용해 항공모함 15,326,048B를 2,761,768B로, 구축함 15,423,880B를 2,798,764B로 줄였다. 웹 GLB는 `assets/models/ships-v1/`에 저장했다.
- `mockup.html`에 GLTFLoader·MeshoptDecoder를 연결하고 기존 절차형 함대를 로딩 실패 시 폴백으로 남겼다. 실제 Meshy 함선 로딩 성공 시에만 절차형 함대를 숨긴다.
- 품질 확인용 `output/step0/meshy-ship-quality-gate.html`을 추가했다. 두 모델을 큰 축척으로 회전·확대해서 볼 수 있으며 PC 1600×900, 모바일 430×932에서 GLB 로딩 성공과 콘솔 오류 0건을 확인했다.
- 현 단계 판정: 함선 단일 객체의 형상·텍스처는 품질 게이트에서 확인 가능하다. 전체 장면이 콘셉트 이미지 수준에 도달하려면 남은 고유 3D 자산과 지형·항만·전투 효과를 같은 파이프라인으로 교체해야 한다. 추가 유료 생성은 다음 묶음의 정확한 비용 승인 전까지 진행하지 않는다.

## 2026-07-27 — CINEMATIC v4 전략 줌 품질 상향

- 기준 콘셉트의 산악·해안·도시 불빛을 유지하면서 HUD·함선·효과가 없는 고해상도 환경 클린 플레이트를 제작해 `assets/environment/cinematic-v1/`에 연결했다.
- 원거리에서 작은 3D 모델의 실루엣이 무너지는 문제를 해결하기 위해 항모전단·유조선 호송대·IRGC 고속정 스웜·항만 크레인 4종의 시네마틱 이미지 LOD를 제작했다. 크로마키 원본과 투명 PNG를 `assets/impostors/cinematic-v1/`에 함께 보존했다.
- `mockup.html`의 CINEMATIC 품질에 투명 LOD, 청록 점선 항로, 항모 방어권, 적색 기뢰밭, 미사일 궤적·타격 섬광을 합성했다. BASIC·RICH와 근접 품질 게이트의 실제 Three.js/Meshy GLB 경로는 그대로 유지했다.
- PC 1600×900과 모바일 430×932 v4 캡처를 생성했다. 기준 콘셉트와 동일 크기의 정적 좌우 비교 PNG 및 드래그 비교 HTML을 추가했다.
- 판정: 전략 화면의 환경 디테일·함종 식별·색상 규율·HUD 가독성은 STEP 0 기준 범위에 도달했다. 실제 고유 Meshy 모델은 16종 중 2종이므로 전체 3D 자산 완성도는 12.5%이며, mockup은 완성 게임이 아니다.
- 남은 일: 사장님 STEP 0 v4 컨펌 후 M1 룰 코어. 다음 Meshy 후보는 유조선·고속정·항만 크레인·TEL 4종이며 최고 설정 기준 120크레딧이라 별도 승인 전 실행하지 않는다.
- 사장님 확인 필요: `output/step0/quality-comparison-concept-vs-v4.html`에서 구도·객체 밀도·HUD 가독성을 확인하고 STEP 0 통과 여부 결정.

## 2026-07-27 — Meshy 6 전략 객체 2차 묶음·CINEMATIC v5

- 사장님 승인 범위 120크레딧으로 Meshy 6 image-to-3D 4건을 실행했다. 유조선·항만 갠트리 크레인·연안 방어 TEL 3건은 성공했고, IRGC 고속정은 30크레딧 보고 후 99%에서 Meshy 서버 timeout으로 실패했다. 승인액을 넘기지 않기 위해 재생성하지 않았다.
- 성공한 원본 GLB와 4K 베이스컬러를 `assets/meshy-source/strategic-v1/`에 보존하고, `catAgentGame`에서 검증된 최적화 파이프라인으로 2K 웹 GLB 3종을 `assets/models/strategic-v1/`에 만들었다.
- `mockup.html`을 부분 성공 로더로 변경했다. 기존 항모·구축함과 신규 유조선·크레인·TEL은 실제 GLB로 표시하며, 고속정만 절차형 스웜 폴백을 유지한다. 한 모델 실패가 전체 묶음을 되돌리지 않는다.
- 실제 자산 확대 품질 게이트를 5 GLB + 1 폴백 구성으로 갱신했다. PC 1600×900 및 모바일 430×932 전용 배치를 각각 검증했다.
- CINEMATIC v5 PC·모바일 캡처와 목표 콘셉트 좌우 비교 PNG·드래그 HTML을 만들었다. 시각 일치 조건 8/8, HTTP 200, WebGL 활성, 콘솔 error 0건을 확인했다.
- 현재 실제 Meshy GLB는 총 5종이다. 16종 유닛 목록 기준 완료는 항모·구축함·유조선·TEL 4종(25%)이며 크레인은 환경 객체로 별도다.
- 다음 할 일: STEP 0 화면 컨펌 후 M1 룰 코어. 고속정 Meshy 재시도는 별도 30크레딧 승인 시에만 실행.

## 2026-07-27 — M1+M2 플레이 가능 세로 슬라이스·실증 지도 v1

- 한 일: 정적 STEP 0 시안과 별도로 실제 게임 진입점 `index.html`을 만들고, 18일 자동 진행·3개 방침 선택·긴급 대응·협상·점수·10종 엔딩·로컬 저장/복구를 하나의 플레이 루프로 연결했다.
- 실제 지도: Natural Earth 1:10m land/minor-islands 원본에서 동경 54.9~57.0도·북위 25.0~27.3도를 잘라 355개 해안선 폴리곤을 생성했다. 반다르아바스·케심·호르무즈·라라크·헹감·무산담·카사브·푸자이라 등 표식은 실제 경위도에 배치했다.
- 항로 고증: IMO Resolution A.161 (ES.IV)의 통항분리대 중심 좌표 4점을 공식 구간으로 사용했다. 문서에 없는 서쪽 연장선은 `게임용 접근 항로`로 시각적으로 구분하고 범례에 출처를 표시했다.
- 3D 전장: 실제 좌표 투영 지형·입체 해안선·해상 그리드·TSS 레인·움직이는 호송대를 구현했다. 기존 Meshy GLB 5종(항모·구축함·유조선·크레인·TEL)을 로드하고 고속정만 절차형 폴백을 유지했다. 정적 콘셉트 배경 이미지는 플레이 화면에 사용하지 않는다.
- 게임 시스템: 방침 카드 34개(MIL 12, DIP 9, ECO 7, INFO 6), 이란 이벤트 10개, 엔딩 10개, 역사 리플레이 5개, 협상력/요구 강도, 최고점 로컬 갱신, 미니게임 3종(요격·소해·정밀 폭격)을 구현했다. 선택과 미니게임은 제한시간 만료 시 자동 폴백된다.
- 검증: 정본 esbuild 명령으로 `assets/js/game.bundle.js` 생성 성공. JSON 8종 파싱 성공. 자동 브라우저 완주에서 엔딩 도달·치명 오류 없음·콘솔 error 0건을 확인했다. 1600×900과 430×932 모두 문서 스크롤 넘침 0, 모바일 엔딩 패널 400×434가 화면 안에 표시됐다. 페이지 자산 검사에서 5개 Meshy GLB 요청을 모두 확인했다.
- 규칙 시뮬레이션: 고정 시드 100회에서 100/100 종료, 점수 0~3500 범위, 종료일 DAY 1~18 범위를 모두 통과했다. 상세 결과는 `output/validation/simulation-100-runs.json`.
- 실행: `python -m http.server 8080 --bind 127.0.0.1` 후 `http://127.0.0.1:8080/index.html`. 자동 완주 회귀 검사는 `http://127.0.0.1:8080/index.html?debug=fast&autostart=1`.
- 비용: 이번 단계에서는 Meshy API를 새로 호출하지 않았고 추가 크레딧 사용은 0이다.
- 남은 일: Firebase 신규 프로젝트 생성·config 발급 전까지 온라인 최고점 랭킹은 로컬 최고점으로 동작한다. M3 실랭킹·공유 카드와 M4 공개 문서/배포는 사장님 Firebase·도메인 승인 후 진행한다.

## 2026-07-27 — 플레이 화면 전체 가림 회귀 수정

- 증상: `http://127.0.0.1:8080/index.html` 접속 시 짙은 빈 화면만 보이고 타이틀·HUD가 표시되지 않았다.
- 원인: `#fatal-error`에 HTML `hidden` 속성이 있었지만 작성자 CSS의 `.fatal-error { display:grid }`가 브라우저 기본 `[hidden] { display:none }`보다 우선해 빈 오류 레이어가 전체 화면을 가렸다. 게임·3D 전장·데이터 로딩은 뒤에서 정상 실행 중이었다.
- 수정: 전역 `[hidden] { display:none !important }` 안전 규칙을 추가하고 CSS 캐시 버전을 `v=3`으로 갱신했다.
- 검증: 일반 접속에서 최상단 요소가 `title-screen`으로 바뀌고 HORMUZ 타이틀·새 작전 시작 버튼·실제 좌표 전장이 표시되는지 다시 확인했다.

## 2026-07-27 — 실지형 폴리곤 정합 수정 v2

- 사용자 캡처에서 이란 본토와 아라비아반도 사이를 가로지르는 거대 대각선 육지 면을 재현했다.
- 원인은 전 지구적으로 연결된 Natural Earth 본토 외곽선을 자체 단일 링 클리퍼가 bbox 내부의 하나의 면으로 강제 연결한 것이었다. 0번 본토 폴리곤에서 자기교차 4건을 확인했다.
- Mapshaper 토폴로지 클리핑으로 이란 본토와 아라비아반도를 분리된 MultiPolygon으로 보존했다. 런타임 해안선은 355개 중복·파편 폴리곤에서 18개 유효 폴리곤으로 정리됐고 자기교차는 0건이다.
- 전장 동서/남북 실제 비율 0.820에 맞춰 Three.js 월드를 112×136.6으로 수정했다. 이전 112×122 설정의 가로 약 12% 과장을 제거했다.
- 기본 및 연출 카메라의 큰 좌우 회전을 제한하고 고도를 높여 북쪽이 화면 위에 유지되도록 했다. 육지·해안선 대비도 상향했다.
- 독립 검수용 `?mapcheck=1&autostart=1` 북쪽 정사영 모드를 추가했다.
- PC 1600×900 정사영·게임 화면과 모바일 430×932 게임 화면을 촬영했다. 콘솔 error 0건, 모바일 문서 넘침 0, 빈 오류 레이어 재발 없음.

## 2026-07-27 — 해안선·3D 육지 배경 면 정합 수정 v3

- 사용자 캡처를 다시 대조한 결과, 해안선 데이터 자체가 아니라 육지 `ExtrudeGeometry`를 수평으로 눕히는 X축 회전 부호가 원인이었다. 해안선은 투영 결과의 `z=-latitude`를 그대로 사용했지만 육지 면은 `+90°` 회전되어 `z=+latitude`가 되어, 뒤쪽 육지 면만 남북으로 반전됐다.
- 육지 면의 회전을 `-90°`로 고쳐 면의 XZ 바닥 좌표와 해안선 XZ 좌표가 동일해지도록 했다. 계산상 기본 외곽선 최대 좌표 차이는 0 world unit이며, 상단의 0.14 world unit bevel만 의도적으로 유지했다.
- 정합 검사용 `mapcheck` 모드는 원거리 카메라 안개를 끄고 노출을 올려 면과 선을 눈으로 명확히 비교할 수 있게 했다. 일반 플레이의 안개 연출은 변경하지 않았다.
- 캐시 버전을 `v=8`로 갱신하고 정본 esbuild 명령으로 번들을 다시 생성했다.
- 브라우저 검증: 북쪽 정사영 1600×900, PC 1600×900, 모바일 430×932를 최종 캡처했다. PC·모바일 모두 문서 넘침 0, 치명 오류 레이어 숨김, 로딩 레이어 숨김, 결정 버튼 3개 표시를 확인했다.

## 2026-07-27 — 3D 직접 전투 임무 구조로 발주 변경

- 사장님의 새 지시에 따라 기존 `자동 진행 + 15/10초 선택 + 2D 미니게임 3종` 결정을 폐기한다.
- 새 정본은 `장시간 선택(방침 180초·긴급 120초) + 임무 브리핑 + 전력 선택 + 3D 직접 조종·사격 + 목표 판정`이다.
- 최초 확정 임무는 `IRGC 고속정 6척 격침`이며, F/A-18E·알레이버크급 구축함·MH-60R 중 전력을 선택한다.
- 방공·소해·전략 타격도 별도 미니게임이 아니라 동일한 3D 직접 조종 시스템의 임무 프리셋으로 제작한다.
- 이 변경은 사장님의 명시적 발주 변경이므로 `HANDOVER.md` 확정 결정 1·3 및 함정 5를 함께 갱신했다.

## 2026-07-27 — 3D 직접 전투 플레이 가능 세로 슬라이스 v14

- `IRGC 고속정 6척 격침` 임무를 실제 3D 전장 안에 연결했다. 브리핑에서 F/A-18E·알레이버크급 구축함·MH-60R을 선택한 뒤, WASD/방향키와 모바일 방향 버튼으로 이동하고 SPACE·마우스·화면 사격 버튼으로 교전한다.
- 동일 시스템의 임무 데이터로 무인기 8기 요격, 기뢰 7발 무력화, 연안 TEL 4기 파괴를 추가했다. 전력 데이터는 전투기·구축함·헬기·USV·B-2의 속도·회전·사거리·연사·탄약·방어력으로 분리했다.
- 방침/전력 선택은 180초, 긴급 대응은 120초로 늘렸다. DAY 1에는 고속정 격침 임무가 보장되며, 임무 성공·실패가 국가 지표와 뉴스에 즉시 반영된다.
- 기존 2D 미니게임 UI·실행 코드를 삭제하고 `cards.json`·`events.json`의 트리거도 `mission`으로 교체했다. 소스와 데이터에서 `minigame`·`미니게임` 잔존 참조 0건을 확인했다.
- 3D 품질은 그라데이션 하늘·단일 패스 애니메이션 해수·청록/호박 조명·해안 윤곽·저폴리 산악 릴리프·도시 불빛·미사일·폭발·표적 락·추적 카메라를 추가했다. 함선·유조선·TEL은 기존 Meshy GLB를 사용하며 전투기·헬기·고속정은 아직 절차형 자산이다.
- 플레이 검증: F/A-18E로 6개 표적을 교전해 `6/6 격침 완료` 뉴스와 성공 지표 반영을 확인했다. 모바일 430×932에서 가로 넘침 0, 전투 HUD·이동·사격 버튼 표시, 콘솔 error 0건을 확인했다.
- 표적을 지나친 뒤 잠금이 풀리는 문제를 수정했다. 기본 자동 순항을 12%로 낮추고 전후방의 가장 가까운 생존 표적을 재포착하도록 변경했다. 직접 임무 중에는 기존 전략 화면의 고속정·기뢰·위험구역 장식을 숨겨 표적 중복과 시각 혼잡을 제거한다.
- 최종 빌드는 캐시 버전 `v14`이며 정본 esbuild 명령 성공, JSON 9종 파싱 성공, `git diff --check` 통과다. 이번 단계의 Meshy 추가 호출과 추가 크레딧 사용은 0이다.
- 재현 가능한 검수 수치는 `output/validation/hormuz-3d-direct-combat-validation-v1.json`에 기록했다.
- 품질 판정: 조작 가능한 첫 전투 세로 슬라이스는 완성했지만, STEP 0 시네마틱 정지 원화와 100% 동일한 사실적 자산 수준은 아니다. 절차형 전투기·헬기·고속정을 Meshy/고유 GLB로 교체하고 지형 텍스처·항만·전투 VFX를 추가하는 후속 자산 패스가 필요하다.

## 2026-07-27 — Meshy 6 직접 전투 자산 4종·런타임 v16

- 사장님이 승인한 최대 120크레딧으로 F/A-18E·MH-60R·MCM USV·IRGC 고속정 4종을 Meshy 6 image-to-3D 최고 설정으로 생성했다. 네 작업 모두 성공했고 각 30크레딧, 총 120크레딧을 사용했다.
- 내장 ImageGen으로 F/A-18E·MH-60R·USV의 단일 객체 기준 이미지를 만들었다. 프롬프트는 공통으로 `Meshy image-to-3D용 고품질 PBR 제품 렌더, 중립 연회색 배경, 전신 3/4 시점, 단일 연결 객체, 텍스트·워터마크·바닥 그림자 없음`을 사용하고 각 기체·선박의 실루엣과 장비 조건을 추가했다. 고속정은 기존 기준 이미지를 재사용했다.
- 원본 4K GLB 약 58MB를 보존하고 기존 `catAgentGame` 최적화 스크립트로 2K JPEG 내장 웹 GLB 약 11MB를 만들었다.
- 4종 확대 품질 게이트에서 GLB 4/4 로딩·콘솔 error 0을 확인했다. 본게임에서 기수/선수 로컬 X축을 조종계 전진 Z축으로 맞추는 `+90°` 보정을 적용했다.
- F/A-18E·MH-60R·USV의 직접 조종 진입을 확인했고, Meshy IRGC 고속정을 상대로 사격 후 `1/6` 격침 판정이 진행되는 것을 확인했다.
- 모바일 430×932에서 문서 가로 넘침 0, 전투 HUD·방향·사격 버튼 표시, 콘솔 error 0을 확인했다.
- 최종 캐시 버전은 `v16`이다. 실제 Meshy 런타임 GLB는 총 9종이며, 유닛 생산률은 16종 중 8종 50%다. 크레인은 환경 객체로 별도다.
- 남은 시각 품질 차이는 절차형 B-2와 지형 표면·산악·도시/항만 밀도·전투 VFX다. 이번 승인 한도를 초과하는 추가 유료 생성은 실행하지 않았다.

## 2026-07-27 — RTS 전구 전투 기획서 v1.0 작성

- 사장님 요청에 따라 직접 운전·조준형 전투 대신 스타크래프트식 선택·다중 선택·우클릭 이동/공격/호위 중심의 소규모 RTS 전투 구조를 기획했다.
- `docs/hormuz-rts-theater-combat-design-v1.0.md`에 정확한 PC·모바일 조작, 전술 일시정지, 공격 이동·위치 사수·철수·지휘그룹, 전력 편성 수량, 120 BP 작전예산, 탄약·손실·국가 지표 정산을 정의했다.
- 공격 지역은 호르무즈 해협, 이란 남부 해안·도서, 이란 내륙 군사 전구, 걸프만·오만만, 카타르·바레인·쿠웨이트, 이라크·요르단 방어 전구로 구성했다.
- 2026-07-27 기준 CENTCOM·미 국방부·미 육군·Reuters 공개 자료를 확인해 공격-반격 방향의 공개 근거를 문서에 기록했다. 실제 좌표·취약점·공격 절차는 제외하고 국가·전구·군사 기능 유형 단위로 추상화했다.
- 해협 고속정 차단, 유조선 호송, 기뢰 항로 개척, 지역 미군기지 방공, 남부 해안 억제, 내륙 정밀타격, 복수 전구 반격의 7개 전투 유형을 정의했다.
- 이 문서는 사장님 검토용 기획 초안이며 아직 코드와 `HANDOVER.md`의 확정 결정에는 반영하지 않았다. 승인 후 P0 해협 고속정 차단전 수직 슬라이스부터 구현한다.

## 2026-07-27 — RTS 전구 전투 기획서 HTML 검토본

- `docs/hormuz-rts-theater-combat-design-v1.0.md` 전체 내용을 독립 실행형 HTML 문서 `docs/hormuz-rts-theater-combat-design-v1.0.html`로 변환했다.
- HORMUZ 상황실 색상 체계, 문서 헤더, 17개 큰 섹션 목차, 활성 목차 표시, 반응형 표, 코드 블록, 맨 위 이동, 인쇄·PDF 스타일을 적용했다. 외부 CSS·JavaScript·폰트 의존성은 없다.
- 로컬 서버 URL에서 데스크톱과 모바일 430×932를 브라우저 검수했다. 표 14개, 목차 링크 60개, 큰 섹션 17개, 문서 가로 넘침 0, 콘솔 오류 0건을 확인했다.
- 모바일 목차는 260px 내부 스크롤로 제한해 긴 목차 뒤에 본문이 묻히지 않도록 조정했다.
- 이 HTML은 기획 검토본이며 게임 코드와 `HANDOVER.md` 확정 결정에는 아직 적용하지 않았다.

## 2026-07-28 — 해협 RTS 전투 플레이 가능 P0 버티컬 슬라이스

- 사장님의 승인된 RTS 지시를 `HANDOVER.md` 확정 결정에 반영했다. 직접 운전·직접 사격은 폐기하고 좌클릭 단일 선택, 드래그 다중 선택, 우클릭 이동·집중 공격, 공격 이동, 위치 사수, 명령 중지, 전구 이탈, Ctrl+1~5 지휘그룹, 전술 일시정지를 정본 조작으로 변경했다.
- 독립 실행 진입점 `rts-combat.html`과 전용 CSS·JSON·Three.js 소스·번들을 만들었다. 실제 좌표 해안선·TSS·호송 항로 위에서 DDG-81, VFA-27, HSM-77과 IRGC 고속정 8척, 민간 유조선 3척이 동시에 움직이고 실탄 발사·피격·폭발·HP·승패 판정을 수행한다.
- 대표 아군 구축함·F/A-18E·IRGC 고속정에 기존 Meshy GLB를 사용하고, 나머지는 성능 상한을 위한 절차형 편대 모델로 구성했다. 이번 단계의 신규 Meshy API 호출과 크레딧 사용은 0이다.
- 전투 시작 전 45초 전술 배치 시간을 추가했다. 적이 먼저 피격되면 즉시 반격하며, 아무 명령도 내리지 않으면 배치 시간 종료 후 호송대를 공격한다. 공격 이동을 선제 실행한 검증에서는 IRGC 8/8 무력화·호송대 3/3·아군 3/3 생존으로 성공했고, 위치 사수만 한 검증에서는 호송대 손실로 실패해 명령에 따른 양방향 결과가 확인됐다.
- 검증 중 `result-layer[hidden]`이 투명 전체화면 레이어로 시작 버튼을 가리던 회귀를 찾아 전역 `[hidden]` 규칙으로 수정했다. 최신 CSS·번들은 캐시 버전 `v=5`로 고정했다.
- 브라우저 검증: `http://127.0.0.1:8080/rts-combat.html?v=5`에서 초기 자산 14개 유닛 표시, 우클릭 이동과 우클릭 집중 공격, 공격 이동, 일시정지 중 시간 고정, 적의 유조선 반격, 성공·실패 결과를 확인했다. 데스크톱 1600×900과 모바일 430×932 모두 가로 넘침 0, 치명 오류 레이어 숨김, 콘솔 error/warn 0건이다.
- 검증 캡처: `output/validation/hormuz-rts-combat-briefing-desktop-1600x900.png`, `output/validation/hormuz-rts-combat-desktop-1600x900.png`, `output/validation/hormuz-rts-combat-mobile-430x242.png`, `output/validation/hormuz-rts-combat-victory-desktop-1600x900.png`. 모바일 DOM 뷰포트는 430×932로 검증했으며 Chrome 검수 캡처 파일은 현재 보이는 창 영역 430×242이다.
- 남은 일: 이 P0 전투를 본편 `index.html`의 작전예산·사전 전력 수량 선택·지지도·국제여론·확전도 정산에 연결하고, 지형 표면·도시/항만·전투 VFX를 STEP 0 시네마틱 기준으로 한 단계 더 고도화한다.

## 2026-07-28 — RTS 전투 리얼리즘·성능 패스 v18

- 함선과 항공기의 모델 축을 이동 루트와 분리해 `visualRoot`에서 보정하고, 바다 유닛은 가속·감속·회전 반경·선수 방향 전진만 사용하도록 변경했다. 호송선도 항로 접선과 선수를 즉시 일치시켜 옆으로 미끄러져 이동하는 표현을 제거했다.
- DDG·F/A-18E·IRGC 고속정의 기존 Meshy GLB를 약 65% LOD로 추가 최적화했다. 근접 실루엣과 2K 텍스처는 유지하면서 전체 전투 장면을 성능 헌법 안으로 낮췄다. 신규 Meshy API 호출과 추가 크레딧 사용은 0이다.
- 바다 유닛별 월드 공간 이중 리본 항적, 함포·추적탄·로켓·미사일 무기 유형, 미사일 곡선 비행·연기, 포구 섬광, 불꽃·연기·수면 기둥 피격 효과를 추가했다. 섬광·연기·파편은 공유 파티클 풀로 묶어 교전 중 draw call이 증가하지 않게 했다.
- 유조선 절차형 모델의 원형 탱크 덩어리를 실제 선박 실루엣에 가까운 갑판·종방향 배관으로 교체하고, 분리된 10개 메시를 재질별 2개 메시로 병합했다. 육지 폴리곤과 해안선도 북·남 권역별로 병합해 실제 해안 정합은 보존하면서 렌더 호출을 줄였다.
- F 키는 첫 선택 전력을 근접 추적하는 전술 카메라로, Home 키는 전구 전체 보기로 동작한다. 선택 링은 크기·두께·광도를 낮춰 근접 모델을 가리지 않도록 수정했다.
- 브라우저 실전 검증에서 선수-진행 방향 내적 `1.000`, `66 draw calls`, `106,862 triangles`, 항적 샘플 56개, 활성 파티클 74개를 기록했다. 정본 상한 90 draw calls·120,000 triangles를 모두 통과했고 v18 콘솔 error/warn은 0건이다.
- 모바일 430×932 검증은 `48 draw calls`, `53,378 triangles`, 콘솔 error/warn 0건이다. 상세 수치와 품질 판정은 `output/validation/hormuz-rts-combat-v18-validation.json`에 기록했다.
- 품질 판정: 함선 방향·선회·항적·발사/피격·근접 Meshy 자산을 실제 플레이 가능한 수준으로 고도화했다. 다만 STEP 0 시네마틱 합성 정지 원화와 픽셀 단위 100% 동일하다고 주장하지 않으며, 다음 시각 패스는 실제 지형 기반 연속 표면 재질·항만/도시 밀도·원거리 대기 원근이다.

## 2026-07-28 — 전 유닛 Meshy 품질·지형 텍스처·함대 편성 v24

- 잠수함처럼 보이던 절차형 대체물은 실제 잠수함이 아니라 MH-60R 헬기 폴백이었다. 현 전장에 등장하는 구축함·전투기·헬기·IRGC 고속정·VLCC 유조선을 모두 원본 UV와 2K 텍스처를 보존한 Meshy GLB로 교체했다. MH-60R에는 교차 회전 로터 애니메이션도 적용했다.
- 바다는 호르무즈 블루아워 수면 텍스처를 셰이더에 합성하고, 북쪽 이란 육지는 산악 암반, 남쪽 오만 육지는 석회암 사막 타일 텍스처를 적용했다. 실제 해안선 폴리곤 정합은 유지하고 지형을 바위 덩어리처럼 보이게 하던 과장 입체 릴리프는 제거했다.
- 성능 헌법을 지키면서 다수 전력을 고품질로 표시하기 위해 전술 그룹 표현을 도입했다. 화면에는 IRGC 고속정 3개 그룹이 각각 3·3·2척, 유조선 2개 그룹이 2·1척을 대표하며 HUD·피해·격침·승패 판정은 실제 논리 수량 8척·3척으로 계산한다.
- 전투 시작 전 60 BP 안에서 알레이버크급 구축함 1~3척, F/A-18E 편대 1~4개, MH-60R 편대 0~3개를 증감하는 함대 편성 UI를 추가했다. 선택 수량은 아군 명칭·HP·공격력·HUD 집계에 직접 연결되며 예산을 넘기는 증가는 자동 차단된다.
- 브라우저에서 기본 편성 DDG x1·VFA x2·HSM x1, 총 41/60 BP를 확인했다. DDG를 x2로 늘린 57/60 BP 상태에서 추가 증가 버튼이 비활성화되는 것을 확인한 뒤 기본값으로 복원해 전투를 시작했다.
- 공격 이동으로 IRGC x3 전술 그룹을 격파했을 때 논리 적 수치가 8에서 5로 감소했다. 데스크톱은 진행방향 내적 1.000, 40 draw calls, 96,907 triangles이며 모바일은 1.000, 34 draw calls, 51,384 triangles로 90 draw calls·120,000 triangles 상한을 모두 통과했다.
- 1600×900과 430×932 편성·전투 화면을 각각 캡처했고 콘솔 error/warn 0건을 확인했다. 상세 검증 수치는 `output/validation/hormuz-rts-fleet-texture-v24-validation.json`에 기록했다.
- 이번 단계의 Meshy 신규 API 호출과 추가 크레딧 사용은 0이다. 기존 고품질 Meshy 원본에서 웹용 LOD만 로컬 생성했다.

## 2026-07-28 — 전투기 실제 편대·헬기 로터·읽기 쉬운 한국어 UI v25

- 전투기 편대 수량이 공격력과 HUD에만 반영되고 실제 3D 기체는 1대만 생성되던 문제를 수정했다. 선두기는 기존 고품질 Meshy 모델을 유지하고, 추가 기체는 2K 텍스처를 보존한 윙맨 LOD를 사용해 선택 수량 1~4대가 실제 V자 편대로 모두 표시된다.
- 기본 선택 2대와 최대 선택 4대를 각각 실제 전장에 생성해 확인했다. 4대 편성은 57/60점이며 52 draw calls·114,868 triangles로 성능 헌법 상한을 통과했다.
- MH-60R에 빨간 팁 1개와 흰 팁 3개가 있는 4엽 메인 로터를 추가했다. 로터는 초당 10.8라디안으로 계속 회전하며 브라우저 검증에서 데스크톱 각도 2.865→3.420, 모바일 4.507→0.924로 변하는 것을 확인했다.
- 전체 UI에서 12px 미만으로 내려가던 글자를 모두 제거했다. 편성·HUD·레이더·명령 버튼·유닛 라벨·결과 화면의 표시 글자 최소값을 PC와 모바일 모두 12px로 확인했고, 모바일 가로 넘침은 0px다.
- 한국어 화면의 MISSION·FRIENDLY FORCE·THREAT·PC COMMAND·DISABLED·영문 고정 부대명을 쉬운 한국어로 교체했다. 목표·조작 설명도 짧은 문장으로 단순화하고 BP 표기를 `점`으로 바꿨다. 영어 버전 문자열은 별도로 유지한다.
- 모바일에서 커진 목표 문구와 좌우 패널이 겹치지 않도록 전력·레이더 패널 시작 위치를 아래로 조정했다.
- 최종 v25 콘솔 error/warn은 0건이다. 상세 검증은 `output/validation/hormuz-rts-v25-formation-rotor-font-validation.json`에 기록했다.

## 2026-07-28 — 전투기 독립 제어·현실적 미사일 발사 v28

- 한 편대 안에 여러 모델만 붙어 있던 전투기를 실제 독립 RTS 유닛으로 분리했다. 선택 수량 1~4대가 각각 고유 ID·호출명·체력·명령·발사 시각·피격 판정을 가지며, 왼쪽 아군 카드에서 한 대씩 선택하거나 드래그·전체 선택으로 묶을 수 있다.
- 전투기 4대를 생성한 뒤 `전투기 1=이동`, `전투기 2=명령 중지`, `전투기 3·4=대기` 상태가 동시에 유지되는 것을 확인했다. 개별 선택 수는 1대이며 다른 전투기의 명령은 덮어쓰지 않는다.
- 전투기별 최초 사격을 0.18초 간격으로 분산하고 각 기체의 좌·우 파일런 위치에서 번갈아 발사하도록 했다. 4대 집중 교전에서 고유 발사 기체 4/4를 확인했다.
- 기존 Catmull-Rom 고각 포물선 비행을 제거했다. 미사일은 기수 방향으로 0.14초 직진·가속한 다음 선회율 제한을 적용해 이동 표적을 추적하며, 로켓도 같은 2단계 구조에서 더 낮은 선회율을 사용한다. 검증값은 최초 방향 내적 1.000, 곡선형 미사일 0, 캡처 시 유도 보정 123회다.
- 아군 목록을 0.1초마다 통째로 교체하던 렌더링을 제거하고 카드 DOM을 유지한 채 선택·손실·HP만 갱신하도록 바꿨다. 개별 클릭 누락과 캡처 중 카드가 잘리는 현상을 함께 막았다.
- 브라우저 1920×897 검증에서 전투기 4유닛·4모델, 개별 선택 1대, 고유 발사 기체 4대를 확인했다. 정상 화면 24회 표본은 44 draw calls·114,036 triangles, 미사일 교전 캡처는 51 draw calls·102,281 triangles이며 전체 관측 최고치가 90·120,000 성능 상한을 통과했다. 치명 오류 레이어도 숨김 상태다.
- 모바일 430×932에서도 전투기 4유닛·고유 ID, 가로 넘침 0px, 47 draw calls·32,680 triangles, 치명 오류 레이어 숨김을 확인한 뒤 브라우저 화면 크기를 기본값으로 복원했다.
- 상세 검증은 `output/validation/hormuz-rts-v28-independent-fighter-missile-validation.json`, 화면은 `output/validation/hormuz-rts-v28-four-independent-fighters-desktop-1920x897.png`와 `output/validation/hormuz-rts-v28-independent-missile-launch-desktop-1920x897.png`에 기록했다.
- 이번 단계의 Meshy 신규 API 호출과 추가 크레딧 사용은 0이다.

## 2026-07-28 — 미사일 탄체 축·선택 표시 보정 v32

- 미사일 위치는 기수 방향으로 이동하지만 원통 메시의 긴 로컬 Y축이 `lookAt()` 회전으로 덮여 탄체가 진행 방향과 90도 어긋나 보이던 원인을 수정했다. 원통 형상을 생성 단계에서 로컬 +Z축으로 굽고, 매 프레임 탄체 +Z축을 실제 속도 벡터에 직접 맞춘다.
- 전투기 4대 사격 검증에서 기수-발사 방향 내적 1.000, 탄체 축-이동 방향 내적 1.000, 초기 직진 구간 내적 1.000, 곡선형 미사일 0개를 확인했다.
- 항공기 선택 원을 바다 표면이 아니라 기체 바로 아래에 배치하고 선 두께를 키웠다. 최초 강화값 0.76은 사장님 피드백에 따라 약 1/3인 0.26으로 낮췄으며, 선택된 이름표도 청록색 테두리로 함께 구분된다.
- 전투기 한 대 선택 시 선택 카드·기체 원·요약이 함께 표시되고, 빈 전장 왼쪽 클릭 후 선택 전투기 수 0과 `선택한 아군이 없습니다.` 문구로 해제되는 것을 확인했다.
- 브라우저 검증 최고 관측값은 42 draw calls·106,050 triangles로 성능 헌법을 통과했고 치명 오류 레이어는 숨김 상태다. 상세 결과는 `output/validation/hormuz-rts-v32-missile-axis-selection-validation.json`에 기록했다.
- 최종 화면은 `output/validation/hormuz-rts-v32-missile-axis-selection-desktop-1920x897.png`, `output/validation/hormuz-rts-v32-single-fighter-selection-desktop-1920x897.png`에 저장했다. 이번 단계의 Meshy 호출과 추가 크레딧 사용은 0이다.

## 2026-07-28 — 전투기 표적 유도·공격력 근거화 v34

- 미사일 탄체 축은 정상이어도 전투기가 표적 상공을 통과한 직후 기수 반대편으로 발사할 수 있던 원인을 수정했다. 공중 유도무기는 기체 중심이 아니라 실제 발사 소켓에서 표적까지의 전방 내적을 다시 검사하며, F/A-18E는 최소 0.76의 전방 정렬과 2.975 월드 단위의 최소 교전 거리를 만족해야 발사한다.
- 공중 유닛의 원형 공전 사격을 공격 통과 방식으로 바꿨다. 표적이 발사 원뿔 밖이면 먼저 기수를 돌리고, 정렬되면 발사 후 표적 옆을 통과한 다음 재진입한다.
- 움직이는 표적의 현재 속도를 반영한 선행 유도점과 프레임 사이 이동 구간을 검사하는 근접신관 판정을 추가했다. 첫 완전 일제사격에서 전투기 4대의 고유 발사 4발·명중 4발·빗나감 0발, 표적 전방 정렬 최저 0.988, 탄체 축·초기 직진 내적 1.000을 확인했다.
- 기존 `구축함 28·전투기 22·헬기 17`은 실측값이 아니라 임시 게임 튜닝값이었음을 바로잡았다. 미 해군·NAVAIR 공개자료에 따라 F/A-18E AGM-84 Harpoon 100, Mk 45 Mod 4 함포 45, MH-60R Hellfire/APKWS II 35의 0~100 상대 게임 지수로 재정의했다. 화면에도 실제 살상력 실측값이 아니라는 고지를 추가했다.
- 효과 활성 장면의 최고 관측값은 48 draw calls·102,097 triangles로 90·120,000 성능 상한을 통과했다. 상세 결과는 `output/validation/hormuz-rts-v34-homing-power-validation.json`, 전장 캡처는 `output/validation/hormuz-rts-v34-fighter-homing-overview-desktop-1920x897.jpg`에 기록했다. 이번 단계의 Meshy 호출과 추가 크레딧 사용은 0이다.

## 2026-07-28 — 병종별 0대 편성 허용 v35

- 구축함과 전투기의 최소 수량이 1로 고정돼 0대로 내릴 수 없던 설정을 수정했다. 구축함·전투기·헬기 모두 각각 0대까지 선택할 수 있다.
- 아군 전체가 0대일 때는 전투 시작 버튼만 비활성화하고 `아군을 한 대 이상 선택해야 전투를 시작할 수 있습니다.` 안내를 표시한다. 어느 병종이든 1대 이상 다시 선택하면 즉시 전투 시작이 가능하다.

## 2026-07-28 — 헬기·구축함 실제 수량 분리와 MH-60R 복합 무장 v39

- 헬기와 구축함 수량을 `x2`, `x3` 텍스트로만 합산하던 전술 그룹 방식을 제거했다. 헬기 3대는 `hsm-77-1~3`, 구축함 3척은 `ddg-81-1~3`의 독립 3D 유닛으로 생성되며 각자 체력·선택·명령·사격 판정을 가진다.
- 헬기 3대 모두 별도 Meshy 모델과 회전 메인 로터를 가진다. 구축함도 3척 모두 화면에 표시되고, 브라우저에서 `구축함 2` 한 척만 선택했을 때 다른 두 척은 선택되지 않는 것을 확인했다.
- 미 해군 공식 자료에 맞춰 MH-60R 무장을 거리별로 분리했다. 근거리 고속정에는 GAU-21 12.7mm(.50구경) 기관총을 짧은 점사 단위로 발사하고, 중거리에서는 APKWS II·AGM-114 Hellfire를 사용한다. M240D 7.62mm 승무원 운용화기도 실제 탑재 무장 근거에 기록했다.
- 무장 종류는 공식 자료로 확인했지만 피해량은 실제 살상력 실측값이 아니다. GAU-21 점사 3.6·공격 간격 0.22초, 유도무장 35는 플레이 균형용 정규화 수치임을 데이터에 명시했다.
- 근접 기관총 캡처에서 헬기 3대·로터 3개, 기관총 발사 6회·캡처 시 명중 3회·비행 중 예광탄 3발을 확인했다. 지속 사격 검증은 26발·26명중이다.
- 최대 허용 편성 전투기 4대+헬기 3대가 처음에는 134,773 triangles로 상한을 넘었다. 선두 헬기는 13,323 triangles 고품질 모델을 유지하고 2·3번은 텍스처를 보존한 4,885 triangles LOD를 사용하며, 로터도 재질별 병합해 최종 71 draw calls·117,897 triangles로 성능 헌법을 통과했다.
- 검증 기록은 `output/validation/hormuz-rts-v39-independent-forces-gun-validation.json`, 화면은 `output/validation/hormuz-rts-v39-three-helicopters-gun-desktop-1920x897.jpg`와 `output/validation/hormuz-rts-v39-three-destroyers-desktop-1920x897.jpg`에 저장했다. 신규 Meshy API 호출과 크레딧 사용은 0이다.

## 2026-07-28 — 오디오·음성·이미지 제작 계획 v1.0

- ElevenLabs 효과음, Suno BGM, Supertone 한·영 무전 음성, 이미지 생성의 역할과 실행 순서를 `docs/hormuz-audio-voice-image-production-plan-v1.0.html`에 정리했다.
- “전체 상황 10개”는 재생 이벤트 10종으로, “음성 인물 5명×4개”는 언어별 20개 실제 대사로 분리했다. 한국어·영어 병렬 제작 시 총 40개 음성 파일이 된다.
- 효과음은 환경·전투기·구축함·헬기·피격·UI의 6개 그룹, 최종 77개를 목표로 한다. BGM은 브리핑·배치·저강도·고강도 루프 4개와 승리·실패 스팅어 2개로 설계했다.
- 이미지 생성은 오디오 전 스타일 비교 시안 3장, 음성 선정 후 가상 인물 초상 5장, SFX 1차 승인 후 상황 이미지 10장의 순서로 배치했다. 버튼·아이콘·문구는 이미지 생성 대상에서 제외하고 CSS/SVG/HTML로 유지한다.
- API 키는 클라이언트와 저장소에 넣지 않고 로컬 생성기 또는 서버 작업기에서만 사용한다. 실제 API 호출·크레딧 사용은 아직 0이며, 스타일·음색·핵심 효과음·통합 플레이의 4회 승인 뒤 단계적으로 실행한다.

## 2026-07-28 — 참고 프로젝트 실증 방식·3D·이미지 단계 보강

- `catAgentGame`, `crimeGame`과 해당 인수인계가 가리키는 SonFootballerTycoon의 실제 ElevenLabs·Supertone·Suno 후처리·Meshy 스크립트를 읽기 전용으로 확인해 계획서에 반영했다. 참고 프로젝트 파일은 수정하지 않았다.
- ElevenLabs는 환경변수 키, 별도 스테이징, `eleven_text_to_sound_v2`, 길이·루프·프롬프트 영향값, JSON 매니페스트를 사용한다. Supertone은 음색 선택 JSON을 먼저 확정하고 지원 모델·감정 스타일을 검사한 뒤 스테이징 생성·재시도·크레딧 기록·승인 후 본편 복사를 수행한다.
- 기존 Suno 작업은 로컬 공식 API 호출이 아니라 공식 서비스에서 받은 원본을 백업하고 무음 구간·길이·페이드를 후처리한 방식임을 명시했다. 호르무즈도 공식 API 접근권이 확인될 때만 REST API를 사용하고 비공식 중계 API는 사용하지 않는다.
- 이미지 생성 단계를 스타일 비교 3장 → 무전 인물 5장 → 신규 3D 누락 유닛 기준 이미지 → 작전 상황 10장 → 파생·최적화로 세분화했다. 신규 3D 기준 이미지는 기존 고유 모델 9종에 없는 객체만 종류당 1장 생성하며 현재 추가 생성 수는 0장이다.
- 3D는 단일 객체 기준 이미지 → Meshy 6 image-to-3D → 원본 GLB·작업 JSON 보존 → 4K에서 웹용 2K·LOD 최적화 → Three.js 축·스케일·회전부품·발사 소켓 검수 순서로 정리했다. 이번 문서 보강 중 신규 오디오·이미지·Meshy API 호출과 크레딧 사용은 0이다.

## 2026-07-28 — ElevenLabs 핵심 전투 효과음 A/B 검수 v1

- 계획서의 첫 승인 게이트에 따라 Mk 45 함포, F/A-18E 대함 미사일, MH-60R GAU-21, MH-60R M240D, MH-60R 유도무기, MH-60R 로터 루프, 대형 선체 폭발, 대형 수면 착탄의 핵심 8종을 A/B 2안씩 생성했다.
- ElevenLabs `eleven_text_to_sound_v2`로 총 16개를 생성했으며 실패 0건, 실제 사용량 166크레딧, 생성 후 잔여량 5,929크레딧을 `assets/audio/staging/elevenlabs/core-v1/elevenlabs-core-sfx-manifest-v1.json`에 기록했다.
- 후보는 승인 전용 `assets/audio/staging/elevenlabs/core-v1/`에만 두었고 본편에는 아직 연결하지 않았다. 무료 티어에서 만든 후보이므로 최종 상업 이용 전에 현재 라이선스와 출처 표기 조건을 다시 확인한다.
- `output/audio/elevenlabs-core-sfx-audition-v1.html`에서 8종 A/B 청음, 항목별 A·B·둘 다 재작업 선택, 브라우저 자동 저장, 선택 JSON 복사·다운로드가 가능하다.
- 데스크톱 1600×900과 모바일 430×932에서 검수했다. 가로 넘침 0, 브라우저 오류 0, MP3 16개 로딩 성공, 선택 저장·복원·초기화 동작을 확인했다.
- 재생성 도구는 `tools/generate-elevenlabs-core-sfx.mjs`와 `tools/run-elevenlabs-core-sfx.ps1`이며, API 키는 파일에 저장하지 않고 사용자 환경 변수에서만 읽는다.

## 2026-07-28 — 효과음 청음 페이지 로컬 파일 재생 수정

- `output/audio/elevenlabs-core-sfx-audition-v1.html`의 음원 주소가 `/assets/...` 절대 URL로 되어 있어 HTML을 D: 드라이브에서 직접 열면 `D:\assets\...`를 찾던 문제를 수정했다.
- 모든 오디오 주소를 페이지 위치 기준 `../../assets/...`로 자동 변환하여 `file:///D:/...` 직접 열기와 `http://127.0.0.1:8080/...` 서버 접속에서 모두 같은 음원을 찾도록 했다.
- Chromium에서 로컬 파일을 직접 열어 MP3 16개 로딩 성공, 오류 0건, 첫 효과음 실제 재생 성공과 재생 시간 증가를 확인했다.

## 2026-07-28 — 효과음 A/B `둘 다 선택` 승인 방식 추가

- 효과음 8종 각각에 `둘 다 선택` 버튼을 추가하고 선택 결과 JSON에는 `BOTH`로 저장되도록 했다.
- `둘 다 선택`은 A·B를 동시에 겹쳐 재생한다는 의미가 아니라 두 후보를 모두 승인한다는 의미이며, 본편에서는 상황·거리별 교대 또는 무작위 순환을 기본 사용 방식으로 안내했다.
- 로컬 파일 직접 열기 기준으로 카드 8개, 선택 버튼 32개, `둘 다 선택` 8개, `BOTH` 저장·선택 표시, MP3 16개 로딩, 모바일 가로 넘침 0, 버튼 최소 높이 44px, 브라우저 오류 0을 확인했다.

## 2026-07-28 — 승인 효과음 본편 3D 전투 연결 v40

- 사용자가 전달한 8/8 선택 결과를 `assets/audio/approvals/elevenlabs-core-sfx-selection-v1.json`으로 보존하고, 선택된 11개 MP3만 `assets/audio/sfx/combat/core-v1/` 본편 자산 폴더로 승격했다. 원본 스테이징 파일은 유지했다.
- `assets/data/rts-audio.json`과 `assets/js/rts-audio.js`를 추가해 Web Audio 기반 3D 위치음향, 거리 감쇠, 이벤트별 음량, 재생 간격 제한, 동시재생 상한, 헬기 로터 위치 루프를 구현했다.
- 구축함 Mk 45와 F/A-18E Harpoon은 A/B를 순서대로 교대한다. 헬기 기관총은 승인된 GAU-21 B와 M240D A/B를 순환하고, 유도무기는 A, 로터·선체 폭발은 B, 수면 착탄은 A를 사용한다.
- `rts-combat.js`의 함포·전투기 미사일·헬기 기관총·헬기 유도무기 발사, 유도무기 수면 추락, 함선 격파, 헬기 생성·격파·전투 종료 이벤트에 승인 효과음을 연결했다. 효과음이 준비된 이벤트에서는 기존 전자음이 겹치지 않도록 했다.
- `rts-combat.bundle.js`를 재번들하고 `rts-combat.html` 캐시 버전을 v40으로 올렸다. 데스크톱과 모바일 430×932 브라우저에서 승인 버퍼 11개 로딩, 실패 0, 수동 이벤트 6종 재생 6/6, 로터 위치 루프 1개, 실제 전투기 미사일 3발·Harpoon 효과음 3회, A/B 순환, 모바일 가로 넘침 0, 콘솔 오류 0을 확인했다.
- 검증 결과는 `output/validation/hormuz-rts-v40-audio-validation.json`에 기록했다. 무료 티어 생성물의 최종 상업 이용·출처 표기 조건은 공개 배포 전에 다시 확인해야 한다.

## 2026-07-28 — 기동·환경음과 작전 상황 이미지 본편 연결 v41

- ElevenLabs `eleven_text_to_sound_v2`로 F/A-18E 순항 엔진 루프·고속 통과음, 구축함 항해 엔진, 이란 고속정 엔진, 유조선 디젤 엔진, 해상 환경 루프의 6종을 생성했다. 실패 0건, 실제 사용량 440크레딧, 생성 후 잔여량 5,295크레딧이며 원본·프롬프트·해시를 `assets/audio/staging/elevenlabs/mobility-v1/elevenlabs-mobility-sfx-manifest-v1.json`에 보존했다.
- 승인된 6개 MP3를 `assets/audio/sfx/ambient/mobility-v1/`에 승격하고 기존 핵심 전투음 11개와 함께 본편 17개 오디오로 연결했다. 바다 환경음은 전역 루프, 헬기 로터·전투기·구축함·고속정·유조선 기동음은 유닛별 위치 기반 루프이며 전투기는 속도·카메라 거리가 맞을 때 별도 통과음을 재생한다.
- 잘 들리지 않던 MH-60R 로터는 음량 0.28, 기준 거리 22, 최대 거리 155, 감쇠 0.86으로 조정했다. 격파·항로 이탈·전투 종료 때 유닛별 루프와 전역 루프를 정리해 잔류 재생을 막았다.
- 승인 시네마틱 콘셉트를 스타일 기준으로 브리핑·적 접촉·표적 고정·무기 발사·적 명중·아군 피격·호송대 위험·재공격·성공·실패의 텍스트 없는 16:9 작전 이미지 10종을 생성했다. 원본 PNG 23,513,998바이트를 보존하고 WebP 2,017,688바이트로 최적화해 실행 용량을 91.4% 줄였다.
- 브리핑 이미지는 전투 전 편성 화면에, 8개 전투 상황 이미지는 해당 사건의 첫 발생 시 6.5~7.2초 카드로, 성공·실패 이미지는 결과 화면에 연결했다. 한국어 작전 UI의 작은 안내 글자는 최소 15px로 올리고, 데스크톱 브리핑을 2열로 정리해 시작 버튼이 첫 화면에 보이도록 했다.
- `rts-combat.bundle.js`를 재번들하고 HTML 캐시 버전을 v41로 올렸다. 데스크톱 1600×900과 모바일 430×932에서 오디오 17/17·이미지 10/10 HTTP 200, 브리핑·표적 고정 카드 로드, 모바일 가로 넘침 0, 최소 글자 15px, 콘솔 오류 0을 확인했다. 검증 결과는 `output/validation/hormuz-rts-v41-media-validation.json`에 기록했다.
- 공식 Suno BGM은 `SUNO_API_KEY`, Supertone 한·영 무전 음성은 `SUPERTONE_API_KEY`가 현재 환경에 없어 생성·연결하지 않았다. 비공식 중계 API나 대체 음성으로 우회하지 않았으며 키가 제공되면 기존 계획서의 승인·스테이징 절차로 이어간다.
- 이번 단계에서 Meshy 추가 호출과 3D 크레딧 사용은 0이다. ElevenLabs 무료 티어 생성물은 공개 배포 전에 현재 상업 이용·출처 표기 조건을 다시 확인해야 한다.

## 2026-07-28 — 본편 RTS 게이트·예산·의회 정치 연동 v1

- 새 게임과 역사 선택 시작은 `해협 호송대 방어` RTS에서 먼저 승리해야 본편 상황실로 진입하도록 바꿨다. 저장 작전 계속은 기존 본편 진행을 복구하므로 필수 전투를 다시 강제하지 않는다.
- RTS 성공 결과를 `localStorage`로 한 번만 인계하며, 임무 ID·성공 여부·작전비·투입 전력·격파 수·생존 아군·생존 유조선·의회/여당 변화·예산 강행 여부를 본편 상태에 반영한다. 브라우저 검증에서 초기 $1,200M 중 $465M이 집행되고 사용 가능 예산 $735M, 통항 38%, 국민 지지 49%로 상황실이 열린 것을 확인했다.
- 전력 사용점수를 폐기하고 구축함 $240M, 전투기 $85M, 헬기 $55M의 게임용 작전비를 적용했다. 세 병종은 각각 0대부터 선택할 수 있고 전체 0대면 전투 시작이 비활성화된다. 기본 편성은 $465M/$480M이며 초과 전력의 증가 버튼은 비활성화된다. 가장 저렴한 전력도 살 수 없는 경우에는 `승인 예산이 없어 투입 불가`와 의회 추가예산 요청 안내를 표시한다.
- 전투 편성 화면에 긴급 의회 추가예산 절차를 추가했다. 기밀 위협 브리핑·지역 조선소 일자리·동맹 비용 분담을 조합해 의회 찬성 51% 이상을 만들면 전투 한도가 $240M 늘어난다. 브라우저에서 39%→54%, 여당 58%→64%, 작전 한도 $480M→$720M 통과를 확인했다.
- 본편 `작전·예산` 화면을 추가해 승인 총액·누적 집행·사용 가능 예산·의회·여당·국민 지지·강행 처리 횟수와 작전 8종 로드맵을 한 번에 확인할 수 있게 했다. 본편 추가예산안은 정상 통과 시 $600M을 확보하며, 대통령 강행 시 의회 -12·여당 -10·국민 -7을 적용한다. 브라우저에서 강행 후 사용 가능 예산 $1,335M, 의회 27%, 여당 48%, 국민 42%, 강행 1회를 확인했다.
- 본편에서 발생하는 `surface_swarm` 임무도 전체 화면 iframe RTS로 열고 `postMessage`로 결과를 되돌려 받도록 연결했다. 성공 복귀 후 전투 화면이 닫히고 작전비 $465M, 통항·지지·탄약·국제 변화가 본편 HUD에 반영되는 것을 확인했다.
- `assets/data/campaign.json`에 호송대 방어·기뢰 회랑·미사일 방어·유조선 구조·연안 발사대 제압·미군 기지 반격 방어·항모 전단 방어·휴전 회랑 엄호의 8개 연속 작전과 예산·정치 쟁점을 데이터화했다. 현재 1번은 플레이 가능, 2~3번은 다음 개발, 4~8번은 기획 확정 상태다.
- 개선 기획·수치·정치 루프·확장 순서·완료 기준은 `docs/hormuz-campaign-budget-congress-design-v1.0.html`에 정리했다. 작전비와 전투력은 실제 조달가·살상력 실측이 아니라 게임 밸런스용 추정치임을 전투 화면과 문서에 명시했다.
- `game.bundle.js`와 `rts-combat.bundle.js`를 재번들하고 캐시 버전을 각각 v19·v46으로 갱신했다. 로컬 서버에서 첫 전투 게이트, 0대 편성, 예산 0 투입 차단, 의회 정상 통과, 본편 강행 처리, 독립 RTS 성공 반환, 본편 내장 RTS 성공 반환, 기획 HTML 로드를 검증했다.

## 2026-07-28 — 본편 상황실 시네마틱 지형·가독성·선택 변화 UI v20

- Google Maps JavaScript API와 Photorealistic 3D Tiles 연동 가능성은 공식 문서 기준으로 검토했다. API 키·결제 계정·Google 로고 및 데이터 출처 표시·타일 캐시 제한이 현재의 외부 런타임 요청 0·무키 정적 배포 원칙과 충돌하므로 이번 빌드에는 넣지 않고, 기존 Natural Earth 1:10m 해안선 좌표를 정본으로 유지했다.
- 전투 화면에서 검증한 로컬 바다·이란 암반·오만 석회질 텍스처를 본편에 이식했다. 단색 평판과 분리된 원뿔 봉우리를 제거하고 실제 해안선 내부에서만 생성되는 연속 삼각 고도 지형, 이란 산악대·케심섬·무산담 능선, 저녁 하늘·구름층·태양광 띠·수면 프레넬·파도·반사광을 추가했다.
- 유조선 경로는 IMO TSS 접근 시각화 좌표의 접선으로 선수 방향을 계산한다. VLCC 모델의 선수 축이 로컬 -X임을 기준으로 최초 배치부터 진행 방향과 일치시키고, 경로 말단의 앞점 래핑으로 선수가 순간 반전하던 조건도 제거했다.
- 우측 8개 국가 지표를 단일 표 모양에서 각각의 유리 패널 카드로 분리하고, `hud-metric-icons-v1.svg`의 통항·유가·지지·국제·탄약·확전·예산·여당 전용 이미지 아이콘을 연결했다. Pretendard·SUIT·Segoe UI Variable·Noto Sans KR 순의 현대적 로컬 서체 스택과 핵심 한국어 15px 최소 글자 규칙을 적용했다.
- 선택 결과는 상단 지표의 작은 증감 표시와 별도 결과 패널에 함께 표시한다. `이전값 → 현재값`, 정확한 증감 수치, `올랐다/내렸다`, 방향 화살표를 노출하고 플레이어에게 유리한 변화는 녹색, 불리한 변화는 적색으로 구분한다. 브라우저에서 국민 지지 +11, 국제 지지 +4, 탄약 -4, 통항 +3의 동시 표시를 확인했다.
- 시네마틱 스타일 기준으로 텍스트 없는 심야 긴급 의회 추가예산 심의 이미지를 생성해 PNG 원본과 WebP 실행본으로 보존했다. 본편 작전·예산실과 RTS 전력 편성의 의회 표결 화면에 연결하고, 의회·여당·국민·국제 변화의 누적값과 각 설득 카드의 +/− 효과를 색상 칩으로 표시했다.
- 전략 화면의 원거리 전력은 고품질 LOD 모델과 저폴리 전략 마커를 혼합하고, 항로 표식·도시 불빛·항만/발사대 마커를 병합·인스턴싱했다. 실제 브라우저 렌더 측정은 114,998 삼각형·73 드로콜로 성능 헌법의 120,000 삼각형·90 드로콜 상한을 통과했다.
- `game.bundle.js`와 `rts-combat.bundle.js`를 재번들하고 캐시 버전을 각각 v20·v47로 갱신했다. 본편 지형·하늘·HUD 아이콘·선택 변화 패널, 의회 이미지·설득 증감, 모델·텍스처 HTTP 로드, 콘솔 오류 0을 로컬 서버에서 검증하고 결과를 `output/validation/hormuz-main-v20-situation-room-validation.json`에 기록했다.

## 2026-07-28 — 전체 걸프 작전권·RTS 미션 통합·역사 분기 v26/v48

- 최신 요청을 17개 항목으로 고정해 `output/validation/hormuz-v26-requirements-validation.json`에 완료 상태와 외부 입력 대기 상태를 기록했다. Google 지도는 기술적으로 가능하지만 결제 프로젝트·제한된 API 키·승인 도메인이 필요하고, Meshy 신규 자폭 드론·해상 기뢰는 Meshy 6 이미지-투-3D 20크레딧씩 총 40크레딧의 명시적 승인이 필요하므로 두 항목을 다른 구현과 분리했다.
- 본편 지도 범위를 경도 48~63도, 위도 22~31.7도로 넓혀 쿠웨이트·바레인·도하·아부다비·두바이·무스카트·부셰르·차바하르까지 포함한 걸프 전체 작전권으로 확장했다. Natural Earth 1:10m 해안선을 다시 잘라 29개 육지 다각형으로 만들고, 실측 좌표 지명 17개를 브라우저에서 확인했다.
- 항모·VLCC 유조선·IRGC 고속정·항만 크레인·연안 TEL의 기존 Meshy 6 원본을 전략 화면용 GLB로 경량화했다. 임시 도형 대신 실제 모델을 연결하고, 모델별 수면 기준 높이와 위쪽 방향의 미세 부유만 사용해 함정이 물 아래로 내려가지 않게 했다. 유조선은 크기를 줄이고 3척의 진행률을 0.06·0.39·0.72로 벌린 뒤 같은 속도로 움직이게 해 겹침과 선수 방향 반전을 방지했다.
- 본편의 공중 방어·기뢰 제거·연안 타격 미션을 모두 별도 1인칭 요격 화면에서 3인칭 상공 RTS로 전환했다. 호송대 방어, 공중 위협 8기, 기뢰 7발, 연안 TEL 4기 시나리오가 동일한 개별 선택·드래그 선택·오른쪽 클릭 이동·오른쪽 클릭 공격을 사용한다. 브라우저에서 TEL 4기와 구축함 1척·전투기 2대·헬기 1대가 각각 독립 객체로 출격하는 것을 확인했다.
- 국가안보회의·이란 반응·오만 중재용 시네마틱 브리핑 이미지 3장을 생성하고, 일일 브리핑과 이란 반응을 확인 버튼 전까지 유지되는 큰 화면으로 바꿨다. 속보는 17px 이상 현대적 한글 글꼴과 클릭·키보드 접근성을 적용하고 전체 기록 팝업을 연결했다.
- 협상은 네 가지 의제를 한 단계씩 선택하고 양보 폭·합의 가능성을 실시간으로 확인한 뒤 최종 검토하거나 이전 단계로 돌아가는 구조로 바꿨다. 과거 진행은 2026년 2월 28일부터 7월 27일까지 여섯 개 주요 사건의 수동 선택으로 구성하고, 현재 시점 이후에는 기존 18일 일일 캠페인으로 이어지게 했다.
- 역사 선택의 변화값은 통항·유가·국민 지지·국제 지지·탄약·확전뿐 아니라 예산·여당 지지·의회 지지에도 반영한다. 선택 설명과 예상 변화가 보이고, 다음 주요 사건 버튼을 누르기 전에는 자동으로 넘어가지 않는다.
- 전반적인 톤매핑 노출·환경광·바다·육지·안개를 주간 작전 기준으로 밝히고, 메인 카메라 거리를 기본 124에서 최소 92·최대 158로 제한해 과도한 확대·축소를 막았다.
- 메인 전략 화면은 동시 표시 개수를 대표 고품질 Meshy 모델 중심으로 재편하고 나머지 적 접촉은 전술 표식으로 표현했다. 바다 메시 세분화를 44×44로 조정해 고정 성능 헌법을 다시 통과시켰다.
- `game.bundle.js`와 `rts-combat.bundle.js`를 각각 v26·v48로 재번들했다. 전체 지도는 12,498 삼각형·17 드로콜, 메인 장면은 118,843 삼각형·29 드로콜로 측정돼 120,000 삼각형·90 드로콜 상한을 통과했고, 지도·TEL·공중 위협·기뢰 시나리오 모두 브라우저 콘솔 오류 0을 확인했다. 전체 지도와 메인 상황실·TEL 브리핑·실행 화면을 `output/validation/`에 1600×900 PNG로 보존했다.

## 2026-07-28 — 주요 사건 선택 결과 이미지·3D 전투·본편 수치 연동 v27

- 과거 주요 사건 6개, 선택지 18개 전부에 실행 결과 제목·설명·결과 이미지를 연결했다. 선택 뒤 다음 사건으로 자동 진행하지 않고, 결과 화면에서 플레이어가 변화값을 확인한 뒤 직접 넘어가게 했다.
- 군사 행동이 포함된 선택지 7개는 `3D 전투 발생` 표식을 노출하고 호송대 방어·기뢰 제거·연안 TEL 제압 시나리오 중 해당 임무를 전체 화면 3인칭 RTS로 실행한다. 전력 편성·예산·의회 추가예산·개별/다중 선택·오른쪽 클릭 이동/공격은 기존 고품질 RTS 체계를 그대로 사용한다.
- 전투에서 반환된 성공/실패, 목표 제압 수, 생존 아군, 실제 작전비, 추가예산과 의회·여당 변화를 과거 선택 상태와 본편 시작 상태에 저장한다. 선택지에 적힌 고정 예산·탄약 변화는 군사 분기에서 중복 차감하지 않고 실제 전투 결과만 반영한다.
- 결과 화면에 16:9 시네마틱 이미지, 전투 결과 4개 카드, `이전값 → 현재값`, 정확한 증감치, 유리/불리 색상과 위·아래 화살표를 추가했다. 통항·유가·국민 지지·국제 지지·탄약·확전·가용 예산·여당 지지·의회 찬성을 모두 비교한다.
- 호송 작전 재개, 연안 타격, 기뢰 회랑 확보, 해협 재개방, 경제 압박의 텍스트 없는 16:9 결과 이미지 5장을 생성해 선택별로 재사용 가능한 결과 자산으로 연결했다.
- 데스크톱 1600×900 브라우저에서 `지휘부·군사시설 동시 타격 → 연안 TEL 3D 전투 → 성공 결과 복귀`를 실행했다. 목표 4/4, 작전비 $465M, 생존 아군 4, 가용 예산 $1,200M→$735M, 국제 55→42, 국민 지지 44→53, 확전 3→5, 여당 58→60이 본편에 반영됐고 콘솔 오류는 0이었다.
- 비군사 분기 `제재·금융 압박 우선`도 전투 없이 경제 압박 결과 이미지와 수치 변화 카드 6개를 표시했다. 전체 데이터 검사는 18/18 결과, 7/7 전투 연결, 누락 0건을 통과했다.
- `game.bundle.js`와 `rts-combat.bundle.js`를 재번들하고 본편 캐시 버전을 v27로 갱신했다. 검증 기록은 `output/validation/hormuz-history-choice-battle-v27-validation.json`에 보존했다.

## 2026-07-28 — TEL 육상 배치 수정·임무별 4개 지역 전장 v28/v49

- TEL은 전투기가 아니라 `Transporter Erector Launcher`, 즉 미사일을 운반·기립·발사하는 육상 이동 차량임을 UI에서 `이동식 미사일 발사 차량`으로 풀어 썼다. 기존 연안 타격 좌표 4개는 Natural Earth 해안선 판정에서 모두 바다였음을 확인했다.
- 연안 타격 전장을 호르무즈 해협에서 `이란 남부 · 부셰르 연안 가상 작전구역`으로 옮겼다. 새 발사 차량 좌표 4개는 모두 육지 판정을 통과하며, 런타임에서도 육상 유닛 좌표가 바다이면 가장 가까운 육지로 자동 보정하고 육지를 찾지 못하면 전투 시작을 중단하도록 방어 로직을 추가했다.
- 고정돼 있던 RTS 지도 경계를 임무별 프로필로 분리했다. 해협 호송대 방어는 호르무즈 해협, 공중 방어는 페르시아만 서부 바레인-카타르 해역, 기뢰 제거는 푸자이라 동쪽 오만만, 육상 발사 차량 타격은 부셰르 연안에서 열린다.
- 네 전장은 각각 별도의 위·경도 경계, 로컬 지도 투영 중심, 월드 크기, 카메라 중심·높이, 아군/적 배치, 호송 경로를 사용한다. 브리핑과 전투 HUD에도 현재 작전지역을 한국어/영어로 표시한다.
- 전체 걸프 해안선 다각형을 지역 지도에 그대로 투영해 지형이 별 모양으로 찌그러지던 조건을 발견해, Sutherland-Hodgman 방식으로 각 전투 경계에서 해안선 외곽·구멍을 잘라 렌더링하도록 수정했다. 지형 돌출은 전투 구역 안에서만 생성하고 크기·개수를 줄였다.
- 미 해군 제5함대의 공개 작전구역이 페르시아만뿐 아니라 오만만·아라비아해·홍해까지 포함한다는 공식 자료를 근거로, 게임 전장이 호르무즈 한 곳에 고정되지 않도록 확장 방향을 잡았다. 각 교전 위치와 전투 상황은 실제 군사 표적 정보가 아닌 지리 기반 가상 시나리오다.
- 데스크톱 1600×900에서 네 전장을 각각 실제로 시작했다. 지도 ID·경계가 모두 달랐고, 부셰르 전장의 이동식 발사 차량 4기는 전부 육지 판정 `true`, 네 전장 콘솔 오류는 0건이었다.
- `game.bundle.js`는 v28, `rts-combat.bundle.js`는 v49로 재번들했다. 네 전장 스크린샷과 검증 기록은 `output/validation/hormuz-rts-v49-regional-maps-validation.json`에 보존했다.

## 2026-07-28 — 해상 유닛 육지 관통 차단·구글 3D 지도 연동안 v52

- 구축함을 포함한 해상 유닛이 목표점까지 직선 이동하며 육지를 통과하던 원인을 수정했다. 위·경도 역투영, 해상 유닛별 선체 여유폭, 이동 선분의 해안 충돌 검사, 해안 접촉 시 좌·우 우회 조향, 육지 클릭 목표의 가장 가까운 안전 수면 보정을 추가했다.
- 선체 중심만 막는 방식으로는 선수·선미가 육지에 걸릴 수 있어, 구축함 길이와 충돌 반경을 기준으로 해안 여유폭을 계산한다. 이동 중 안전 수면 위치를 보존하고 남은 관성으로 육지에 진입하는 경우에도 최종 이동을 차단한다.
- 유조선 자동 항로에도 같은 해안 선분 검사를 적용하고, 바다 위 생성 좌표가 육지이거나 선체가 해안에 걸리면 가장 가까운 안전 수면으로 보정한다. 항공 유닛은 육지 위를 정상 통과하며 육상 TEL은 기존 육지 고정 규칙을 유지한다.
- 해안 판정을 매 프레임 사용해도 비용이 급증하지 않도록 현재 작전구역과 교차하는 해안 다각형만 보존하고, 각 다각형의 경계 상자로 먼저 거르는 공간 필터를 추가했다.
- 부셰르 연안 검증 모드에서 구축함 3척을 육상 TEL 좌표로 강제 이동시켰다. 해안 접촉 3회, 우회 조향 114회 동안 육지 위 해상 유닛은 0척을 유지해 `shoreQa=passed`를 확인했다. 브라우저 렌더는 33드로콜·74,265삼각형으로 고정 상한 이내였다.
- Google Maps Platform 공식 문서를 기준으로 현재 Three.js 전투를 유지할 수 있는 `Maps JavaScript API + WebGL Overlay View` 방식을 본 전투 권장안으로 정리했다. API 키·Map ID 발급, HTTP 리퍼러 제한, 로컬 폴백, 위·경도 좌표 이관, 4개 전장 범위 검증, 저작권 표시 요구사항은 `docs/google-3d-terrain-integration.html`에 기록하고 빈 설정 예시는 `net/google-maps-config.example.js`에 추가했다. 실제 Google 지도는 키와 Map ID를 받기 전에는 활성화하지 않았다.
- `rts-combat.bundle.js`와 `game.bundle.js`를 재번들하고 RTS 캐시 버전을 v52로 올렸다. 화면 검증은 `output/validation/hormuz-rts-v52-destroyer-coast-avoidance-2560x1313.png`, 수치 검증은 `output/validation/hormuz-rts-v52-destroyer-coast-avoidance-validation.json`에 보존했다.

## 2026-07-28 — Google Cloud 설정용 웹 에이전트 프롬프트

- Google Cloud에 로그인된 웹 에이전트가 HORMUZ용 Maps JavaScript API, JavaScript Vector Map ID, HTTP 리퍼러·API 제한이 적용된 브라우저 키를 안전하게 준비하도록 `docs/google-3d-map-web-agent-prompt.md`를 작성했다.
- 중복 프로젝트·Map ID·키 생성을 피하고, 결제 계정 선택이나 유료 업그레이드가 필요하면 임의 진행하지 않고 사용자에게 요청하도록 했다.
- Places·Routes·Street View·Map Tiles·Photorealistic 3D Tiles 등 불필요한 API 활성화, OAuth·서비스 계정 생성, 제한 없는 키 방치를 금지했다.
- localhost 2종과 sidak.kr HORMUZ 경로만 허용하고 Maps JavaScript API 하나로 제한하며, 가능한 경우 일일 250~300회 한도 또는 월 5달러 예산 알림을 설정하도록 했다.
- 파일 시스템 접근이 가능한 웹 에이전트는 제한된 키와 Map ID를 `net/google-maps-config.js`에 기록하되, 실제 코드 연결 전까지 `enabled: false`를 유지하고 보고서에는 키 마지막 6자리만 표시하도록 했다.

## 2026-07-28 — Google 지도 키 발급 프롬프트 브라우저 전용 개정

- 웹 에이전트는 로컬 폴더·파일·터미널에 접근할 수 없다는 사용자 지적을 반영해 `docs/google-3d-map-web-agent-prompt.md`를 브라우저 전용 실행 명령으로 개정했다.
- 설명만 작성하지 않고 Google Cloud Console을 직접 조작하도록 명시하고, 로그인·프로젝트 선택·결제 확인·Maps JavaScript API 활성화·JavaScript Vector Map ID 생성·제한된 API 키 생성 과정을 클릭 단위로 구체화했다.
- 로컬 설정 파일 생성 단계는 완전히 제거했다. 대신 HTTP 리퍼러 3개와 Maps JavaScript API 단일 제한을 저장·재확인한 뒤, API 키 전체 값과 Map ID 전체 값을 최종 `Codex 전달값` 블록으로 사용자에게 반환하도록 변경했다.
- 제한 저장에 실패하거나 HORMUZ 전용 프로젝트·JavaScript Vector Map ID 조건이 맞지 않으면 전체 키를 출력하지 않고 실패 원인만 보고하도록 했다.

## 2026-07-28 — Google Maps 3D 실제 지형·Meshy 모델 독립 연동 v1

- 웹 에이전트가 발급한 제한형 브라우저 API 키와 JavaScript Vector Map ID를 `net/google-maps-config.js`에 반영하고 Google 3D 지도 독립 검증 화면 `google-map-test.html`을 추가했다. 키 원문은 진행 기록과 검증 JSON에 재기록하지 않았다.
- Maps JavaScript API의 `maps3d` 라이브러리와 `Map3DElement`의 `HYBRID` 모드로 호르무즈 해협 실제 해안·산악 지형을 표시했다. 호르무즈 해협, 바레인·카타르 해역, 오만만, 부셰르 연안의 네 실제 좌표 시점을 버튼으로 전환한다.
- Google 3D 모델 API의 `Model3DInteractiveElement`로 기존 Meshy 6 알레이버크급 구축함 웹 GLB 2,798,764바이트를 지형 위에 직접 배치했다. 구축함 선택과 지역별 재배치, 실제 크기 기준 축척, 근접 카메라와 지형 회전 카메라를 추가했다.
- 2560×1313 실제 브라우저에서 API 연결, Map ID 적용, 호르무즈 지형, 부셰르 지역 전환, Meshy GLB 다운로드·렌더, 구축함 근접 시점을 확인했다. 치명 오류 화면은 표시되지 않았고 검증 결과는 `output/validation/hormuz-google-3d-map-v1-validation.json`에 보존했다.
- Google 3D 타일은 시점 이동 뒤 고해상도 타일이 단계적으로 들어오므로 즉시 캡처보다 수 초 뒤 해안선과 지명이 선명해진다. 이번 단계는 본편 교체 전 품질·비용·성능을 확인하는 독립 시험이며 본편 상황실에는 아직 적용하지 않았다.

## 2026-07-28 — Google 3D 구축함 방향·수면 높이 보정 v2

- Google 3D 근접 보기에서 구축함의 함교와 레이더 마스트가 아래로 향하던 축 변환 오류를 수정했다. Google Maps 3D 공식 모델 예제의 좌표계 보정값인 `tilt 270°`, `roll 90°`를 공통 상수로 적용하고 지역별 선수 방향은 기존 heading으로 유지했다.
- 모델 원점이 선체 중앙에 있어 상대 고도 2미터에서는 선체 절반이 수면 아래로 들어가던 조건도 함께 수정했다. 웹 GLB의 세로 경계와 90배 축척을 기준으로 상대 고도를 18미터로 올려 실제 흘수에 가까운 위치에 놓았다.
- 2560×1313 브라우저 근접 화면에서 갑판·함교·마스트가 위를 향하고 선수와 선미가 수면에 평행한 정상 자세를 확인했다. 기존 잘못된 근접 검증 이미지는 정상 화면으로 교체하고 별도의 v2 검증 이미지도 보존했다.

## 2026-07-28 — Google 3D 구축함 게임용 10배 축척 v3

- 실제 비율보다 게임 화면에서의 시인성을 우선한다는 요청에 따라 구축함 기본 축척을 90에서 900으로 정확히 10배 확대했다. 선택 시 축척은 960으로 설정해 선택 반응도 유지했다.
- 모델 축척이 10배 커진 만큼 수면 상대 고도도 18미터에서 180미터로 비례 조정해 기존 수면 자세를 유지했다.
- 기존 1,250미터 근접 카메라에서는 선수와 선체가 화면 밖으로 잘려, 모델 축척은 그대로 유지하고 카메라 범위만 2,300미터로 조정했다.
- 2560×1257 실제 브라우저에서 선수·선미·함교·레이더 마스트·헬기 갑판이 모두 화면 안에 크게 표시되는 것을 확인했다. 최종 화면은 `output/validation/hormuz-google-3d-map-v3-ship-scale-10x.png`에 보존하고 기존 근접 확인 이미지도 같은 화면으로 갱신했다.

## 2026-07-28 — Google 3D 지도 범위 1/3·구축함 추가 5배 확대 v4

- 현재 화면을 기준으로 호르무즈·바레인/카타르·오만만·부셰르 네 지역의 초기 카메라 range를 정확히 1/3로 줄였다. 각 지역은 기존보다 좁은 실제 지형 구역을 더 가까이 보여준다.
- 구축함 기본 축척을 직전 900에서 4500으로 5배 확대하고 선택 축척을 4800으로 조정했다. 최초 축척 90과 비교하면 총 50배의 게임용 표시 크기다.
- 축척 증가에 맞춰 수면 상대 고도도 180미터에서 900미터로 5배 보정했다. 함교와 마스트 방향, 선체의 수면 평행 자세는 그대로 유지된다.
- 근접 카메라는 10,500미터로 맞춰 확대된 선수·선미가 잘리지 않도록 했다. 2560×1257 브라우저에서 지도 1/3 초기 화면과 전체 구축함 근접 화면을 각각 검증해 v4 PNG 2장으로 보존했다.

## 2026-07-28 — 승인 크기 Google 3D 본편 적용 v29

- 독립 테스트 화면에서 승인된 Google 3D 호르무즈 실제 지형을 본편 `index.html` 상황실 배경에 기본 적용했다. 카메라 범위는 10,166.67m(직전 기준 1/3), Meshy 구축함은 기본 4,500·선택 4,800·상대 고도 900m로 고정했다.
- 본편 우측에 `전술 3D 보기 / 실제 지도 보기` 전환 버튼을 추가했다. Google 지도를 보는 동안 로컬 Three.js 렌더 루프를 정지하고, 로컬 전술 지도로 돌아오면 즉시 재개해 이중 GPU 렌더링을 방지했다.
- Google Maps API·Map ID·모델 로딩 중에는 기존 로컬 전술 지도가 계속 보이며, 연결 또는 렌더 오류가 나면 로컬 지도로 자동 복귀하도록 폴백을 적용했다.
- 2560×1313 실제 브라우저에서 Google 지도 `ready`, Map ID `applied`, 구축함 `mounted`, 치명적 오류 화면 없음, 전환 전후 렌더러 활성 상태 정상임을 확인했다. 로컬 Three.js는 118,843 triangles / 29 draw calls로 성능 상한을 통과했다.
- `game.bundle.js`를 다시 번들하고 본편 캐시 버전을 v29, CSS 캐시 버전을 v28로 갱신했다. 통합·로컬 전환·실제 자동 시작 검증 화면과 검증 JSON은 `output/validation/`에 보관했다.

## 2026-07-28 — Google 3D 실제 지형 RTS 전투씬 통합 v53

- 이전 v29 작업이 상황실 `index.html` 배경에만 Google 지도를 적용하고 실제 전투 렌더러인 `rts-combat.html`을 건드리지 않았던 범위 오류를 확인했다. 전투씬용 `RtsGoogleBattleMap`을 별도로 추가해 RTS 기본 지형 공급자를 Google 3D HYBRID로 변경했다.
- Google 3D 실제 지형을 아래 레이어에 렌더링하고, 기존 Three.js 부대·선택 원·이동 경로·미사일·폭발 효과를 투명 WebGL 캔버스로 그 위에 합성했다. 기존 마우스 선택, 드래그 다중 선택, 우클릭 이동·공격, 키보드 카메라, 휠 확대/축소는 그대로 유지된다.
- RTS의 지역별 equirectangular 좌표를 실제 경도·위도로 되돌리고, 카메라 높이·거리를 미터 단위 Google `range`와 `tilt`로 환산해 매 프레임 변화량을 제한하며 동기화했다. 호르무즈·바레인/카타르·오만만·부셰르 4개 전구가 같은 변환기를 사용한다.
- Google API 또는 Map ID 오류 시 로컬 Three.js 바다·육지·하늘을 즉시 복구하는 폴백을 유지했다. `google=0` 검증에서 로컬 전술 지형, 로딩 종료, 치명적 오류 없음이 확인됐다.
- 2560×1313 브라우저에서 호르무즈 전경과 구축함 근접 화면, 부셰르 연안 전경과 TEL 4기 육상 배치, 전투기 4대 독립 공격을 검증했다. 부셰르에서 `landPlacementValid=true`, `seaUnitsOnLand=0`, 전투기 유도무기 4발 발사·4발 명중, 투사체 축 정합 1.000을 확인했다.
- `rts-combat.bundle.js`를 다시 번들하고 캐시 버전을 v53, CSS를 v46으로 갱신했다. 검증 이미지 5장과 `hormuz-rts-v53-google-3d-battle-integration-validation.json`을 `output/validation/`에 보관했다.
## 2026-07-28 — Google 지형 흔들림 동기화·결과 팝업 3초 지연 v57

- 전투 폭발/피격 카메라 흔들림 값을 Google 3D 지도 호스트에도 같은 프레임으로 전달해 지형과 Three.js 함정·항공기·효과가 함께 움직이도록 수정했다.
- 지도 이동량을 화면 픽셀로 환산하고 최대 X 42px/Y 30px로 제한했으며, 흔들리는 동안 가장자리 노출 방지를 위해 1.025~1.06배 보정 확대를 적용했다.
- 흔들림이 끝나면 지도 `transform`을 제거해 정확한 원위치(`0,0`)로 복귀한다.
- 전투 종료 즉시 시뮬레이션과 반복 음향은 정지하지만, 마지막 폭발 효과와 카메라 흔들림은 정리되도록 유지한다.
- 결과 팝업은 전투 종료 후 정확히 3,000ms 동안 강제로 숨기고 전장을 보여준 뒤 표시한다. 브라우저 실측 전환 시간은 3,014ms였다.
- 브라우저 검증: Google 지도 `ready`, 지형 흔들림 19샘플/최대 13.26px, 종료 후 원위치 복귀, 콘솔 오류 0건.
- 검증 화면과 결과 JSON은 `output/validation/hormuz-rts-v57-*`에 저장했다.
## 2026-07-28 — 지상병력·TEL 발사차량 접지 그림자 v61

- 구글 3D 지도는 Three.js 지형과 깊이 버퍼를 공유하지 않으므로 일반 실시간 조명 그림자만으로는 지도 표면에 그림자가 투영되지 않는 문제를 확인했다.
- 모든 `domain: land` 지상병력에 부드러운 WebGL 접지 그림자를 추가해 로컬 지형에서도 바닥 접촉감이 보이도록 했다.
- Google 3D 모드에서는 지도와 WebGL 캔버스 사이에 화면 공간 타원 그림자를 추가했다. 카메라 이동·확대·회전에 맞춰 위치·크기·각도를 매 프레임 갱신한다.
- Google 3D 모드의 지상병력 배치 높이를 1.05에서 0.08로 분리 보정해 발사차량 바퀴와 그림자 사이의 떠 보이는 간격을 줄였다.
- 부셰르 연안 미사일 발사차량 4대 모두 WebGL 그림자 4개·화면 공간 그림자 4개가 활성화됐고, 지면 높이 0.055·콘솔 오류 0건을 브라우저에서 확인했다.
- 검증 화면과 JSON은 `output/validation/hormuz-rts-v61-ground-unit-shadow-*`에 저장했다.

## 2026-07-28 — 적군 관찰 선택·동일 객체 더블클릭 선택 v66

- 적군을 마우스 왼쪽 버튼으로 클릭하면 공격 명령 대신 관찰 대상으로 선택되도록 변경했다. 적군 관찰 중에는 이동·공격·방어·중지 명령을 내릴 수 없고, 붉은 선택 표식과 이름표·HP·사거리·레이더 강조로 위치를 확인할 수 있다.
- 객체를 더블클릭하면 스타크래프트처럼 현재 화면 안에 보이는 같은 진영·같은 종류의 객체만 묶어서 선택한다. 아군은 묶음 조종이 가능하고 적군은 묶음 관찰만 가능하다.
- 작은 3D 객체나 복잡한 모델도 클릭하기 쉽도록 광선 충돌 판정 뒤 화면 좌표 기반 보조 선택 판정을 추가했다. 빈 곳을 왼쪽 클릭하면 선택이 해제되고, F키는 선택한 아군 또는 관찰 중인 적군 쪽으로 카메라를 이동한다.
- Google 3D 부셰르 연안 전장에서 TEL 1기를 한 번 클릭했을 때 `tel-1` 1기, 더블클릭했을 때 현재 화면에 보이는 `tel-1`, `tel-2`, `tel-3` 3기만 선택되고 화면 밖 `tel-4`는 제외되는 것을 확인했다. Google 지도 상태 `ready`, 브라우저 콘솔 오류 0건이다.
- 검증 화면: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-rts-v66-enemy-double-selection-2560x1313.jpg`
- 검증 기록: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-rts-v66-enemy-selection-validation.json`

## 2026-07-28 — 대규모 해협 결전·같은 전투 다시하기 v71

- `scenario=large_fleet_battle` 대규모 해전 모드를 추가했다. 기본 아군은 구축함 3척·전투기 8대·헬기 6대이며, 적 고속정 30척이 3개 방향에서 진입하고 유조선 4척 중 3척 이상을 지키는 7분 임무다.
- 모든 편대 수량은 표시용 `xN`이 아니라 개별 선택·명령·피격·파괴가 가능한 독립 객체로 생성된다. 총 51개 객체(아군 17·적군 30·유조선 4)를 실제 Google 3D 호르무즈 전장에서 검증했다.
- 대규모 전투에서 고정밀 모델을 그대로 51개 복제하면 성능 헌법을 넘기므로, 선두 구축함은 정밀 Meshy 모델을 유지하고 원거리 부대는 한 번의 드로콜로 그리는 전략 LOD를 사용한다. 유조선·구축함 LOD의 선체·상부구조·배관·갑판 형상을 보강하고, 헬기 6대의 로터는 모두 개별 회전한다.
- 대규모 선택 시 이름표 과밀을 줄이고 선택한 부대·유조선·교전 중 표적만 우선 표시한다. 6기 초과 다중 선택에서는 개별 사거리 원을 생략해 명령 가독성과 성능을 유지한다.
- 브라우저 실측은 드로콜 81/90, 삼각형 14,825/120,000, 바다 유닛 육지 침범 0건, Google 3D 지도 `ready`, 콘솔 오류 0건으로 통과했다.
- 결과창을 `같은 전투 다시하기`와 `상황실 복귀`로 분리했다. 다시하기를 누르면 현재 시나리오·언어·Google 지도 설정은 유지하고 QA 자동종료 값은 제거한 뒤 전력 편성 화면으로 돌아간다. 실제 재전투 URL에서 `replay=1`, 대규모 기본 편성 3/8/6 복원, 결과창 닫힘을 확인했다.
- 검증 화면: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-rts-v71-large-fleet-battle-2560x1313.jpg`
- 결과·재전투 화면: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-rts-v71-large-battle-result-retry-2560x1313.jpg`
- 재전투 편성 화면: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-rts-v71-large-battle-retry-briefing-2560x1313.jpg`
- 검증 기록: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-rts-v71-large-battle-retry-validation.json`
- 본편 후반부에도 `대규모 해협 결전` 정책 카드를 추가했다. ACT III 소모전의 11일차부터 세 선택지 중 하나로 우선 제시되며, 선택하면 `large_fleet_battle` 미션으로 직접 진입한다.
- 대규모전 본편 예산은 최소 900M·권장 1,500M·상한 1,800M으로 설정했다. 보유 승인예산이 부족하면 기존 의회 추가예산·강제 증액·지지도 변화 절차를 그대로 사용한다.
- 캠페인 `carrier_screen` 작전을 `planned`에서 `playable`로 전환하고 같은 대규모 전투 시나리오를 연결했다. 직접 확인용 본편 진입 주소는 `http://127.0.0.1:8080/index.html?autostart=1&prologue=complete&startMode=new&mission=large`다.
- `game.bundle.js`를 다시 번들하고 본편 캐시 버전을 v30으로 갱신했다. 카드·미션·캠페인·전투 문자열 JSON 파싱과 본편/전투 직접 주소 HTTP 200을 확인했다.

## 2026-07-29 — 신규 3개 작전 정식 편입·전 시나리오 폴리곤 헌법 복구 v83

- v72~v82에서 추가된 `mine_corridor`·`missile_screen`·`coastal_battery` 시나리오가 `rts-combat.js`에는 완성되어 있었지만 `campaign.json`은 `next`/`planned`로 남아 있어 작전·예산실 로드맵이 실제 구현 상태와 어긋나 있었다. 세 작전을 `playable`로 승격하고 각 작전에 `scenario` 키를 명시해 전투 화면과의 연결을 문서화했다. `다음 개발` 표시는 `tanker_rescue`로 옮겼다. 이제 8개 작전 중 5개가 운용 중이다.
- 5개 시나리오를 실제 브라우저에서 측정한 결과 `대규모 해협 결전`이 삼각형 156,304/120,000으로 성능 헌법을 30% 초과하고 있었다. `coastal_battery` 119,236(99.4%), `missile_screen` 116,059(96.7%)도 상한 직전이었다. 결합병과 확장에서 추가된 고정밀 모델이 LOD 없이 그대로 들어간 것이 원인이다.
- MCM USV는 16종 유닛 중 유일하게 감축본이 없어 원본 40,412 삼각형을 그대로 그렸고, 이 한 척이 대규모 전투 한 프레임의 26%를 차지했다. meshoptimizer 단순화로 전투 전용 LOD를 새로 만들었다(40,412 → 17,767 삼각형, 3,066,856B → 1,986,192B). Meshy 크레딧은 쓰지 않고 로컬에서 생성했다.
- 이미 만들어져 있던 더 가벼운 변형으로 교체했다. IRGC 고속정 `lod3`(12,374) → `strategic-v4`(10,172), VLCC 유조선 `lod3`(13,928) → `strategic-v2`(12,000), 연안 TEL `strategic-v1`(19,545) → `strategic-v2`(17,563).
- `missile_screen`의 공중 위협은 전투기 hero 모델(6,415)을 8기 복제하고 있었다. 편대기 모델(3,833)로 바꿔 8기 합계를 51,320에서 30,664로 줄였다. 무인기 성격에도 편대기 실루엣이 더 맞는다.
- 대규모 전투에서 USV는 `선두 1척은 정밀 모델 유지` 예외 대상에서 제외했다. 정밀 모델은 항모와 선두 구축함만 유지하고 지원 정선은 전략 LOD로 통일한다.

| 시나리오 | 이전 삼각형 | 현재 삼각형 | 상한 대비 | 드로콜 |
|---|---:|---:|---:|---:|
| convoy_shield | 107,110 | 96,648 | 80.5% | 51/90 |
| mine_corridor | 70,780 | 66,924 | 55.8% | 56/90 |
| missile_screen | 116,059 | 94,129 | 78.4% | 51/90 |
| coastal_battery | 119,236 | 111,308 | 92.8% | 46/90 |
| large_fleet_battle | 156,304 | 116,024 | 96.7% | 83/90 |

- 5개 시나리오 모두 실제 브라우저 2560×1313에서 콘솔 오류 0건, 바다 유닛 육지 침범 0건으로 통과했다. `coastal_battery`와 `large_fleet_battle`은 여유가 각각 7.2%, 3.3%뿐이므로 다음에 유닛을 추가할 때는 반드시 재측정한다.
- 본편 미션 라우팅 5종을 브라우저에서 확인했다. `minesweep`→`mine_clearance`→`mine_corridor`, `intercept`→`air_defense`→`missile_screen`, `bombrun`→`strategic_strike`→`coastal_battery`, `large`→`large_fleet_battle`, `swarm`→`surface_swarm`→`convoy_shield`가 모두 일치한다. 본편 부팅도 콘솔 오류 0건, HTTP 4xx/5xx 0건이다.
- 캐시 버전을 갱신했다. `rts-combat.bundle.js` v83, `game.bundle.js` v32, 본편 데이터 fetch v29, `rts-combat.json` version 8.
- 검증 기록: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-rts-v83-scenario-matrix-validation.json`
- 미션 라우팅 검증: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-v83-mission-routing-validation.json`
- 검증 화면: `output/validation/hormuz-rts-v83-<시나리오>-2560x1313.jpg` 5장, `output/validation/hormuz-v83-main-boot-1600x900.jpg`
- 재현용 도구: `tools/validate-rts-scenarios.py`, `tools/validate-mission-routing.py`, `tools/glb-tri-count.py`, `tools/make-battle-lod.mjs`
- LOD 생성 기록: `D:\soccerstarWebSource\GameCreator\hormuz\assets\models\battle-lod-report-v1.json`

## 2026-07-29 — v83 현행 게이트 재검증·Meshy 결합병과 생성 출처 보강

- 새 세션 인수인계 규율을 적용한 직후 현재 작업트리를 기준으로 필수 게이트를 다시 실행했다. 5개 RTS 시나리오는 `convoy_shield` 96,648/51, `mine_corridor` 66,924/56, `missile_screen` 94,129/51, `coastal_battery` 111,308/46, `large_fleet_battle` 116,024/83(삼각형/드로콜)로 모두 성능 헌법을 통과했다. 콘솔·페이지·HTTP 오류와 해상 유닛 육지 침범도 모두 0건이다.
- 본편 라우팅은 `swarm`→`convoy_shield`, `minesweep`→`mine_corridor`, `intercept`→`missile_screen`, `bombrun`→`coastal_battery`, `large`→`large_fleet_battle` 5종이 모두 정본과 일치했다. 캠페인 로드맵은 플레이 가능 5종·다음 개발 1종·기획 확정 2종으로 정합성을 유지한다.
- 사용자가 새 객체는 Meshy API로 만들도록 승인한 범위에서 B-2A, 미 해병 소총수, IRGC 지상 전투원, M27형 소총, AK형 소총 5종을 Meshy T2 Image-to-3D로 생성했다. 보병 2종은 Meshy 리깅과 `Walk_Forward_While_Shooting_inplace` 애니메이션까지 생성했으며 총 91크레딧을 사용했다.
- 원본 작업 ID·크레딧·다운로드 파일은 `assets/meshy-source/combined-arms-v1/meshy-combined-arms-tasks-v1.json`에 보존했다. 런타임 모델은 로컬에서 Meshopt·WebP 최적화했지만 스킨 메시 단순화는 하지 않았다. `tools/glb-tri-count.py --used-only` 결과 신규 런타임 모델의 최대치는 B-2A 5,572, 미 해병 2,518, IRGC 지상 전투원 2,407, 소총 826 이하로 확인됐다.
- 재검증 기록은 `output/validation/hormuz-rts-v83-current-scenario-matrix-validation.json`과 `output/validation/hormuz-v83-current-mission-routing-validation.json`에 저장했다.

## 2026-07-29 — Meshy 적 지상군 전투 LOD v84 (검증 전 기록)

- Google 3D 대규모 전장 근접 측정에서 Meshy 보병 20명의 스킨 애니메이션을 동시에 계산할 때 약 20fps까지 하락하는 병목을 확인했다. 모델·텍스처·개별 유닛 판정은 유지하고, 멀리 배치되는 IRGC 지상 전투원 12명만 Meshy 전투 애니메이션의 한 프레임을 구워 단일 인스턴스 배치로 렌더링하도록 변경했다.
- 미 해병 8명은 `ready`·`low-ready-walk`·`rifle-up-walk`·`aim-fire` 전체 스킨 애니메이션을 유지한다. 적 지상군도 같은 Meshy 캐릭터와 AK형 Meshy 소총, 개별 이동·피격·선택·사격 판정은 유지하며 렌더 뼈 계산과 드로콜만 줄인다.
- `rts-combat.bundle.js` 캐시를 v84로 올렸다. 소스 재번들, 5개 시나리오 게이트, 본편 라우팅, Google 3D 시각·프레임 재측정은 이 기록 다음에 수행한다.

## 2026-07-29 — Meshy 결합병과 프레임 여유 보정 v85 (검증 전 기록)

- v84 Google 3D 근접 실측에서 적 Meshy 보병 12명의 전투 자세 인스턴싱이 정상 동작했고, 활성 스킨 애니메이션은 20개에서 아군 8개로 감소했다. 같은 화면 프레임은 약 20.2fps에서 54.2fps로 회복됐다.
- M2 목표 55fps에 근소하게 못 미쳐 아군 보병 뼈 애니메이션 갱신 주기를 30fps에서 24fps로 낮췄다. 이동·선택·사격·투사체·카메라·Google 지형 렌더는 화면 프레임 그대로 유지하며, 캐릭터 뼈 자세 계산만 분리된다.
- `rts-combat.bundle.js` 캐시를 v85로 올렸다. 재번들·5개 시나리오·라우팅·Google 3D 프레임 재검증은 이 기록 다음에 수행한다.

## 2026-07-29 — 적 보병 인스턴스 화면 밖 렌더 제거 v86 (검증 전 기록)

- v85 근접 카메라에서 적 보병 인스턴스가 화면 밖이어도 12명 전체 28,884삼각형을 계속 그리는 조건을 확인했다. 원인은 인스턴스 묶음의 프러스텀 컬링을 꺼 둔 설정이었다.
- 적 보병 인스턴스의 월드 경계구를 갱신하고 프러스텀 컬링을 켰다. 적 부대가 화면에 들어오면 같은 Meshy 전투 자세를 그리되, 전부 화면 밖이면 해당 묶음의 GPU 드로를 생략한다.
- `rts-combat.bundle.js` 캐시를 v86으로 올렸다. 재번들·전 시나리오·라우팅·Google 3D 프레임 재검증은 이 기록 다음에 수행한다.

## 2026-07-29 — 적 지상군 4개 공간 분대 LOD v87 (검증 전 기록)

- 적 보병 12명을 한 인스턴스 경계로 묶으면 전체 경계구가 넓어 근접 카메라와 계속 교차하는 점을 확인했다. 실제 배치 좌표 순서로 3명씩 4개 공간 분대로 나눠 각 분대를 독립 컬링한다.
- 전체 전장을 볼 때도 적 보병 드로콜은 원래 스킨 메시 12개보다 적은 최대 4개다. 근접 카메라에서는 화면에 들어오는 분대만 렌더한다.
- `rts-combat.bundle.js` 캐시를 v87로 올렸다. 재번들·전 시나리오·라우팅·Google 3D 프레임 재검증은 이 기록 다음에 수행한다.

## 2026-07-29 — v87 Meshy 결합병과 최종 검증

- 전용 Google 3D 검증 22항목을 통과했다. 대규모 전투 승인 예산 4,200M, 선택 가능 병종 7개, 아군 34기, 적군 66기, 유조선 5척, Meshy 보병 20명, B-2 2기, M27·AK 소총 인스턴스 2묶음이 실제 런타임에 배치됐다.
- 미 해병 8명은 `ready`·`low-ready-walk`·`rifle-up-walk`·`aim-fire` 상태를 모두 기록했고, 적 보병 12명은 같은 Meshy 외형과 개별 전투 판정을 유지한 4개 공간 LOD 분대로 확인됐다. Google 3D 전용 근접 검증은 54,260삼각형·34드로콜, 지상·해상 배치 이탈 0, 콘솔·페이지·HTTP 오류 0으로 통과했다.
- 5개 시나리오 전체 게이트도 다시 통과했다. `convoy_shield` 96,648/51, `mine_corridor` 66,924/56, `missile_screen` 94,129/51, `coastal_battery` 111,308/46, `large_fleet_battle` 116,024/75(삼각형/드로콜)다. 대규모 전투 드로콜은 v83의 83에서 75로 줄었다.
- 본편 미션 라우팅 5종과 캠페인 로드맵 정합성도 오류 0으로 통과했다. `tools/glb-tri-count.py --used-only` 결과 런타임 GLB 최대치는 17,767삼각형이며, 신규 Meshy 결합병과 모델은 B-2 5,572·보병 2,518 이하·소총 826 이하이다.
- 검증 기록은 `output/validation/hormuz-rts-v87-meshy-combined-arms-validation.json`, `output/validation/hormuz-rts-v87-scenario-matrix-validation.json`, `output/validation/hormuz-v87-mission-routing-validation.json`에 저장했다. Google 3D 근접 화면은 `output/validation/hormuz-rts-v87-meshy-combined-arms-1920x1080.jpg`, 전 시나리오 화면 5장은 `output/validation/hormuz-rts-v87-<시나리오>-2560x1313.jpg`로 보존했다.

## 2026-07-29 — Meshy 결합병과 제작 목록 정본 보강

- `docs/hormuz-production-inventory.md`에 B-2A·미 해병·IRGC 지상 전투원·M27형 소총·AK형 소총의 Meshy 생성 출처, 91크레딧 사용량, 런타임 감축본, 보병 애니메이션 상태, 적 지상군 공간 LOD 규칙을 추가했다.
- 성능 표의 대규모 전투 드로콜을 v87 최종 실측치 75로 갱신했다. 런타임 구현이나 모델 경로는 바꾸지 않은 문서 정합성 보정이다.

## 2026-07-29 — 직접 대규모 임무 진입 수정 v33

- `index.html?...&mission=large`로 들어가도 새 게임의 과거 사건 선택과 호송대 방어 프롤로그가 먼저 실행되어 대규모 전투가 가려지는 진입 순서 오류를 재현했다.
- `mission` 매개변수가 있으면 이를 명시적인 직접 임무 실행 요청으로 취급해 과거 사건·프롤로그 관문을 건너뛰고 `GameDirector`의 직접 임무 처리로 즉시 넘기도록 수정했다.
- 본편 번들 캐시 버전을 v33으로 올렸다. 재번들 후 `mission=large`가 대규모 전투 전력 편성 화면을 직접 여는지 브라우저에서 검증한다.
- 일반 속도 직접 주소에서 과거 사건 선택 0개, 전투 iframe `scenario=large_fleet_battle`, `campaign=mission`을 확인했다. 전력 편성 화면이 즉시 열렸고 콘솔·페이지·HTTP 오류는 모두 0건이다.
- `tools/validate-mission-routing.py`에 일반 속도 직접 임무 회귀 검사를 추가했다. `output/validation/hormuz-v33-mission-routing-validation.json`에 5개 라우팅·캠페인 로드맵·직접 대규모 임무 진입 통과 결과를 저장했다.

## 2026-07-29 — 대규모 전투 Meshy 외형·지상군 자세 복구 v88 (검증 전 기록)

- 실제 Google 3D 대규모 전투 화면을 다시 확인한 결과 고속정·유조선·TEL·편대 전력을 성능용 기본 도형으로 그려 막대기처럼 보이는 품질 결함, 해안선에 걸친 지상군 배치, 정지 보병의 T포즈를 재현했다.
- Meshy 정본 9종에서 실제 실루엣과 텍스처를 유지한 357~1,074삼각형 초경량 3D LOD를 `gltfpack -sp -sa`로 만들었다. 512px 텍스처를 포함하며 새 객체를 임의 도형으로 다시 만든 것이 아니라 Meshy 생성 모델의 원거리 전투 변형이다.
- `rts-combat.json`을 version 9로 올리고 대규모 전투가 이 Meshy 초경량 LOD를 사용하도록 연결하기 시작했다. 전투 번들 캐시는 v88로 올렸다.
- 지상군은 해안선 중심 판정이 아니라 유닛 반경 전체가 육지 안쪽인지 검사하도록 보강하고, 보병 기본 리그의 0초 T포즈 클립은 유휴 애니메이션으로 채택하지 않도록 수정한다. 재번들·전 시나리오·라우팅·Google 3D 시각 검증은 이 기록 다음에 수행한다.

## 2026-07-29 — Meshy 품질 유지 성능 보정 v89 (검증 전 기록)

- v88 첫 통합은 77개 원거리 유닛을 전부 Meshy 외형으로 복구하고 지상군 육지 내부 배치를 통과했지만 137,844삼각형·95드로콜로 성능 헌법을 초과했다.
- 대규모 전투의 항모·구축함도 기본 도형이 아니라 1,074·811삼각형 Meshy 초경량 LOD로 통일한다. 단독 항모도 인스턴스 배치를 허용해 Meshy 외형을 유지한다.
- 헬기 6대의 로터 날·색상 팁·허브를 각 4드로콜에서 버텍스 색상 단일 메시 1드로콜로 합쳤다. 각 로터의 독립 회전은 그대로 유지한다.
- 데이터 캐시는 version 9, 전투 번들 캐시는 v89로 갱신했다. 최종 성능·시각·전 시나리오 검증은 이 기록 다음에 수행한다.

## 2026-07-29 — 보병 단일 키프레임 T포즈 차단 v90 (검증 전 기록)

- Meshy 기본 리그의 `clip0`가 glTF 검사에서는 0초지만 Three.js 로드 후 0.3초 클립으로 보정되어 기존 지속시간 필터를 통과하는 것을 확인했다. 이 클립은 트랙당 키프레임이 1개뿐인 정적 T포즈다.
- 유휴 클립 채택 조건에 `트랙당 키프레임 2개 이상`을 추가했다. 정지 상태에서는 3.3초 전진 사격 보행 클립의 32% 프레임을 고정해 소총을 든 경계 자세로 시작하고, 이동·공격 시 정상 애니메이션으로 전환한다.
- 전투 번들 캐시는 v90으로 갱신했다. 최종 검증은 이 기록 다음에 수행한다.

## 2026-07-29 — 보병 전투 자세 전환 검증 보강 v91 (검증 전 기록)

- 전용 검증에서 일부 해병이 가까운 적을 즉시 조준해 `rifle-up-walk` 상태가 기록되기 전에 `aim-fire`로 넘어가는 검증 순서 문제를 확인했다.
- 검증 모드에서만 16방향 중 육지 안쪽 이동 거리가 가장 긴 지점을 골라 공격 이동시키고, 충분히 이동한 뒤 실제 지상 표적 공격으로 전환한다. 일반 플레이의 AI·이동·공격 규칙은 바꾸지 않는다.
- 전투 번들 캐시를 v91로 올렸다. 재번들 뒤 Meshy 외형·육지 배치·T포즈 차단·성능 헌법·본편 라우팅을 전부 다시 검사한다.

## 2026-07-29 — v91 대규모 전투 Meshy 외형·배치 최종 검증

- 대규모전 전략 유닛 79기(8종)와 헬기 6대가 모두 기존 Meshy 생성 모델의 전투용 LOD를 사용하는지 검사했다. 절차형 막대기·상자 폴백은 0기다.
- 보병 20명은 모두 Meshy 외형을 유지한다. 아군 해병 8명은 단일 키프레임 T포즈를 유휴 상태로 쓰지 않고 `combat-freeze` 경계 자세에서 시작하며, `low-ready-walk`와 `rifle-up-walk` 전환을 모두 통과했다.
- 지상군의 반경 전체 육지 내부 배치와 이동 보정을 검사해 바다 배치 0기, 해안선 걸침 0기를 확인했다. 해상 유닛 육지 침범도 0기다.
- 5개 시나리오 성능 게이트를 통과했다. `convoy_shield` 96,648/48, `mine_corridor` 66,924/53, `missile_screen` 94,129/48, `coastal_battery` 111,308/43, `large_fleet_battle` 116,097/74(삼각형/드로콜)다. 전부 콘솔·페이지·HTTP 오류 0건이다.
- 본편 미션 라우팅 5종, 캠페인 로드맵, 일반 속도 직접 대규모 임무 진입도 모두 통과했다.
- 검증 기록: `output/validation/hormuz-rts-v91-meshy-combined-arms-validation.json`, `output/validation/hormuz-rts-v91-scenario-matrix-validation.json`, `output/validation/hormuz-v91-mission-routing-validation.json`.
- 검증 화면: `output/validation/hormuz-rts-v91-meshy-combined-arms-1920x1080.jpg`와 5개 시나리오별 `output/validation/hormuz-rts-v91-*-2560x1313.jpg`.

## 2026-07-29 — 대규모전 유조선·이란 전력 가시성 보정 v92 (검증 전 기록)

- Google 3D 전체 전장 화면에서 유조선과 이란 고속정·TEL이 로딩은 됐지만 실제 지도 축척과 어두운 텍스처 때문에 검은 점처럼 보여 라벨만 남는 문제를 재현했다.
- 충돌 반경·이동·사거리 계산은 그대로 두고 렌더 크기만 병종별 1.65~3.2배 확대한다. Meshy 텍스처를 유지하면서 팀 색상의 약한 발광을 더해 밝은 Google 위성 지도와 어두운 바다 양쪽에서 선체가 보이도록 했다.
- 적 공격과 이동을 멈춘 `qa=strategic-visibility` 고정 화면을 추가했다. 전투 번들 캐시는 v92로 올렸으며 재번들·Google 3D 육안·성능 헌법·전 시나리오 검증은 다음에 수행한다.

## 2026-07-29 — Meshy 압축 좌표 인스턴싱 복구 v93 (검증 전 기록)

- v92 수치 진단에서 유조선 인스턴스 배율이 `0.000372`, 구축함이 `0.000226`으로 붕괴한 것을 확인했다. gltfpack이 POSITION/NORMAL을 normalized 정수로 압축했는데, 인스턴스 생성 코드가 이 geometry에 월드 행렬을 직접 덮어쓰면서 양자화 범위를 벗어난 것이 원인이다.
- Meshy geometry의 압축 좌표는 수정하지 않고, `sourceMesh.matrixWorld`를 각 인스턴스 행렬 뒤에 결합하도록 교체했다. 충돌·이동·전투 판정과 삼각형 수는 그대로이며 실제 선체 크기와 중심·바닥 정렬만 복원된다.
- 전투 번들 캐시를 v93으로 올렸다. Google 3D 고정 화면에서 유조선·고속정·TEL·항모·구축함 외형과 겹침을 다시 확인한 뒤 병종별 표시 배율을 최종 조정한다.

## 2026-07-29 — 대규모전 병종별 화면 배율 정규화 v94 (검증 전 기록)

- 압축 좌표 복구 후 v92의 임시 확대값까지 정상 적용되면서 유조선·고속정·TEL이 지나치게 커진 화면을 확인했다.
- 기본 Meshy 정규화 크기를 기준으로 고속정·TEL은 1.0배, 유조선 1.05배, 전투기·폭격기·항모 1.1배, 구축함 1.15배, USV 1.2배로 조정했다. 실제 지도 축척보다 식별성은 높되 서로 겹쳐 전장을 가리는 수준은 피한다.
- 전투 번들 캐시를 v94로 올렸다. 고정 화면 육안 확인 후 동일 수치를 자동 검증에 고정한다.

## 2026-07-29 — Meshy 전략 유닛 가시성 회귀 게이트 v95 (검증 전 기록)

- 데이터상 Meshy 로딩 성공만 검사하면 이번처럼 실제 geometry가 점으로 축소되는 결함을 놓칠 수 있어, 압축 geometry에 `sourceMatrix`가 결합됐는지와 병종별 표시 배율 1.0~1.2 범위를 런타임 스냅샷에 추가했다.
- 결합병과 검증 도구가 해병 근접 화면에 이어 `qa=strategic-visibility` Google 3D 고정 전장을 다시 열고, 유조선·고속정·TEL을 포함한 전략 8종·79기의 Meshy 행렬과 전용 1920×1080 화면을 저장하도록 확장했다.
- 전투 번들 캐시를 v95로 올렸다. 재번들 후 전체 성능·라우팅과 함께 최종 검증한다.

## 2026-07-29 — 해병 애니메이션 검증 결정성 보강 v96 (검증 전 기록)

- 가시성 수정과 무관하게 해안 가까운 해병 2명이 검증용 공격 이동 목표를 찾지 못해 정지 조준으로 남으면서 `rifle-up-walk` 기록 대기가 간헐적으로 시간 초과하는 것을 재현했다.
- `qa=marine-animation`에서만 공격 이동 단계 4.5초 동안 이동 판정을 강제해 실제 Meshy 총 든 보행 클립 전환을 8명 모두 기록하도록 했다. 일반 전투 이동·애니메이션에는 적용되지 않는다.
- 전투 번들 캐시를 v96으로 올렸다. 가시성·애니메이션 결합 검증을 다시 실행한다.

## 2026-07-29 — 대규모전 전력 절반 경량화 v97 (검증 전 기록)

- 대규모전 105개 객체를 한 화면에서 AI·선택 판정·라벨까지 갱신해 성능 헌법의 삼각형·드로콜 기준을 통과해도 실제 조작이 버벅이는 문제를 반영했다.
- 전투 모드는 유지하되 기본 아군을 34기에서 18기, 이란 전력을 66기에서 33기, 유조선을 5척에서 3척으로 줄였다. 고속정 24척·지상군 6명·발사 차량 3기, 아군 구축함 2척·전투기 5대·헬기 3대·항모 1척·USV 2척·폭격기 1대·해병 4명으로 편성한다.
- 기본 승인 예산은 4,200에서 2,700으로, 추가 예산은 800에서 500으로 조정했다. 승리 조건도 적 33기 제압과 유조선 3척 중 2척 생존으로 동기화했다.
- 데이터 캐시는 version 11, 전투 번들 캐시는 v97로 올렸다. Google 3D 실화면·전 시나리오·본편 라우팅·Meshy 성능 검증은 이 기록 다음에 수행한다.

## 2026-07-29 — v97 대규모전 절반 경량화 최종 검증

- 실제 Google 3D 전장에서 기본 아군 18기·이란 전력 33기·유조선 3척, 총 54개가 배치되는지 확인했다. 이전 총 105개보다 48.6% 적으며, 전략 유닛 41기와 헬기 3대는 모두 Meshy LOD를 사용하고 절차형 폴백은 0기다.
- 대규모전 전체 화면은 63,129삼각형·55드로콜로 통과했다. v91의 116,097삼각형·74드로콜보다 각각 45.6%·25.7% 감소했다. 결합병과 근접 화면도 40,941삼각형·44드로콜이며, 육지·바다 배치 이탈과 콘솔·페이지·HTTP 오류는 모두 0건이다.
- 5개 전투 성능 게이트와 본편 미션 라우팅 5종, 캠페인 로드맵, `mission=large` 직접 진입을 다시 통과했다. 브라우저의 외부 지도 자원 완료를 기다리다 생기는 검사 시간 초과를 막기 위해 라우팅 검사의 진입 대기 기준은 전체 `load` 대신 DOM 준비 완료로 안정화했다.
- 검증 기록은 `output/validation/hormuz-rts-v97-meshy-combined-arms-validation.json`, `output/validation/hormuz-rts-v97-scenario-matrix-validation.json`, `output/validation/hormuz-v97-mission-routing-validation.json`에 저장했다. Google 3D 전체 전장 화면은 `output/validation/hormuz-rts-v97-strategic-visibility-1920x1080.jpg`로 보존했다.

## 2026-07-29 — Google 3D Meshy 원본 텍스처 복원 v98 (검증 전 기록)

- 실제 Google 3D 대규모전 화면을 다시 캡처해 구축함이 청록색 반투명 판처럼, 이란 고속정이 주황 단색처럼 보이는 결함을 재현했다. 원인은 원거리 Meshy LOD의 원본 PBR 텍스처 위에 팀 색 발광을 강제로 덮고, 황혼용 청록·주황 조명을 밝은 Google 위성 지도에도 그대로 적용한 것이었다.
- Google 3D에서는 Meshy 베이스컬러 맵을 sRGB·중성 흰색으로 유지하고 팀 색 발광을 0으로 제거했다. 팀 구분은 기존 라벨·선택 링·레이더로 유지한다. 로컬 야간 지형에서만 색 없는 0.12 밝기 보정을 남긴다.
- Google 지도 활성 시에는 중성 주광 조명과 1.06 노출을 적용해 회색 선체·흰 유조선·사막색 TEL의 원래 색을 보존한다. 원본 베이스컬러 맵·흰 기본색·불투명·발광 0 조건을 자동 검증 항목으로 추가했다.
- 데이터 캐시는 version 12, 전투 번들 캐시는 v98로 올렸다. 실제 브라우저 전후 화면과 5개 시나리오 성능 게이트를 다음에 다시 검증한다.

## 2026-07-29 — UV 손상 없는 전략 LOD 재생성 v99 (검증 전 기록)

- v98에서 팀 발광을 제거하자 기존 `battle-ultra-v1`의 근본 결함이 선명하게 드러났다. `gltfpack -sp -sa`가 텍스처 아틀라스의 UV 경계를 넘어 면을 합쳐 원본 이미지 조각이 선체에 검은 얼룩처럼 재배치돼 있었다.
- Meshy 원본 베이스컬러를 단순화 전에 각 버텍스 `COLOR_0`로 굽고, UV와 이미지 텍스처를 제거한 뒤 실루엣을 감축하는 v2 제작기를 추가했다. UV가 없어 단순화 뒤에도 색상 아틀라스가 뒤틀리지 않으며, 원본 Meshy 형태와 회색·함정색·사막색 계열은 유지한다.
- 구축함 비교 시안에서 기존 811삼각형 LOD의 검은 줄무늬 대신 시험 1,629삼각형 모델이 깨끗한 회색 선체로 표시되는 것을 실제 브라우저에서 확인했다. 같은 방식을 9종 전체에 적용하고 성능 상한 안에서 최종 비율을 검증한다.

## 2026-07-29 — 전략 LOD 잡색 제거·군용 PBR 팔레트 v100 (검증 전 기록)

- v99 전체 전장 화면에서 UV 아틀라스 뒤틀림은 사라졌지만 원본 이미지의 잡색을 버텍스에 그대로 굽는 방식 때문에 구축함·유조선 표면에 회색 반점이 남은 것을 확인했다.
- Meshy 형상·법선·실루엣은 그대로 유지하고 손상된 이미지 텍스처와 UV는 제거한다. 구축함·항모·항공기는 중성 군함색, 고속정은 저채도 올리브, TEL은 사막색, 유조선은 따뜻한 회색을 사용한다. 높이와 위쪽 법선에만 16% 이내의 부드러운 명암을 줘 단색 장난감 느낌 없이 깨끗한 PBR 표면으로 만든다.
- 데이터 캐시는 version 14, 전투 번들 캐시는 v100으로 올렸다. 9종 LOD를 다시 생성한 뒤 Google 3D 실제 화면에서 검은 얼룩·형광 팀 색·투명 표면이 모두 사라졌는지 확인한다.

## 2026-07-29 — 전략 LOD 선형 색공간 노출 보정 v101 (검증 전 기록)

- v100 실제 전장 캡처에서 텍스처 얼룩은 완전히 사라졌지만, 화면색 기준 팔레트 값을 선형 버텍스 색상에 직접 넣어 군함과 유조선이 흰색으로 과노출된 것을 확인했다.
- 군함·항공기·유조선·고속정·TEL 9종 팔레트를 선형 색공간 값으로 다시 낮췄다. 결과는 회색 군함, 어두운 B-2, 저채도 올리브 고속정, 따뜻한 회색 유조선, 사막색 TEL로 구분된다.
- 데이터 캐시는 version 15, 전투 번들 캐시는 v101로 올렸다. Google 타일 로딩이 끝난 고정 전장과 자동 검증 화면을 최종 확인한다.

## 2026-07-29 — v101 Google 3D 전략 재질 최종 검증

- 실제 브라우저와 저장 스크린샷을 확인해 기존 `battle-ultra-v1`의 검은 텍스처 조각, 청록·주황 팀 발광, 반투명처럼 보이던 표면이 모두 사라졌음을 확인했다. 유조선·구축함은 깨끗한 회색, 고속정은 저채도 올리브, TEL은 사막색으로 표시된다.
- 전략 Meshy 8종 41기 모두 `COLOR_0` 군용 팔레트 사용, 베이스컬러 이미지 텍스처 없음, 머티리얼 기본색 흰색, 발광 0, 불투명 조건을 통과했다. 압축 좌표 원본 행렬·표시 배율 1.0~1.2도 유지한다.
- Google 3D 결합 검증은 44,584삼각형·44드로콜, 전체 대규모전은 67,045삼각형·55드로콜이다. 5개 전투 성능 헌법, 본편 라우팅 5종, 직접 대규모전 진입, 육지·바다 배치, 콘솔·페이지·HTTP 오류 0을 모두 통과했다.
- 최종 기록은 `output/validation/hormuz-rts-v101-meshy-combined-arms-validation.json`, `output/validation/hormuz-rts-v101-scenario-matrix-validation.json`, `output/validation/hormuz-v101-mission-routing-validation.json`에 저장했다. 최종 Google 3D 화면은 `output/validation/hormuz-rts-v101-strategic-visibility-1920x1080.jpg`다.

## 2026-07-29 — 전장 이동 복구·전체 지도 드래그·Google 조명 재조정 v102 (검증 전 기록)

- 구축함이 이동 명령을 받아도 거의 정지해 보이던 상태를 실제 입력으로 재현했다. 화면 표시 크기를 그대로 해안 충돌 안전 반경에 사용해 선체보다 지나치게 넓은 구역을 육지로 판정하던 것이 원인이므로, 실제 선폭 기준 항해 반경과 반응성 있는 선회·가속으로 교정한다.
- 마우스 휠 드래그와 `Alt+왼쪽 드래그`로 전장 전체를 이동하는 카메라 패닝을 추가한다. Three.js 유닛과 Google 3D 지도 중심을 같은 월드 좌표로 갱신하며, 기존 왼쪽 드래그 다중 선택은 유지한다.
- Google 3D 위에서 초경량 Meshy 군용 팔레트가 흰색으로 날아가던 과노출을 줄인다. 지도 자체는 밝게 유지하면서 유닛의 회색·올리브·사막색 재질과 입체 명암이 다시 보이도록 중성 조명 강도와 노출만 낮춘다.
- 선택 패널에 `이동 중`, `공격 중`, `명령 대기` 등 현재 명령 상태를 즉시 표시한다. 데이터 캐시는 version 16, 스타일 캐시는 v53, 전투 번들 캐시는 v102로 올린다.
- 스크린샷 검증용 `qa=strategic-visibility` 주소를 사용자가 그대로 열어도 이동·공격 명령을 내리는 순간 정지 상태를 자동 해제한다. 고정 화면 검증은 입력 전까지 그대로 유지한다.
- 중단 직전 실제 Google 3D 검증에서 구축함은 이동 명령·속도·좌표 변화가 모두 있었지만 6초 측정 이동량이 0.249로 체감 기준 0.35에 못 미쳤다. 검증 기준을 낮추지 않고, 해상 유닛이 수동 명령을 받는 순간 목표 방향으로 20% 선회하고 순항속도의 18%를 확보하도록 보강했다. 저프레임 환경에서 게임 시간이 과도하게 느려지지 않도록 프레임 델타 상한도 0.05초에서 0.08초로 조정한다.

## 2026-07-29 — v102 전체 지도 드래그·전투 이동·재질 최종 검증

- 실제 Google 3D 화면에서 오른쪽 클릭과 마우스 휠 드래그를 실행하는 상호작용 검증 14개 항목을 모두 통과했다. 구축함 6초 이동량은 보강 전 0.249에서 0.985로 약 4배 늘었고, 전투기 4.638·헬기 2.636·유조선 13.232 월드 단위 이동을 확인했다.
- 마우스 휠 드래그 283px에 따라 카메라 중심이 16.912 월드 단위 이동하고 Google 지도 중심도 `56.05000,26.42000`에서 `56.31888,26.26909`로 함께 바뀌었다. 검증용 정지 주소도 첫 이동 명령과 동시에 정상 재생으로 전환됐다.
- Google 조명은 과노출을 낮추되 지도를 다시 어둡게 만들지 않는 수준으로 확정했다. 전체 전략 유닛은 깨진 이미지 UV를 쓰지 않는 기존 Meshy 형상·법선·군용 PBR 정점색 LOD를 유지하며, 검은 텍스처 조각·형광색·흰색 날림 없이 회색·올리브·사막색 입체 명암을 표시한다.
- 5개 전투 성능 게이트는 `convoy_shield` 96,648/48, `mine_corridor` 66,924/53, `missile_screen` 94,129/48, `coastal_battery` 111,308/43, `large_fleet_battle` 67,045/55(삼각형/드로콜)로 모두 통과했다. 콘솔·페이지·HTTP 오류와 해상 유닛 육지 침범은 0건이다.
- 본편 라우팅 5종과 대규모전 직접 진입이 모두 통과했다. Meshy 결합병과 31개 품질 조건도 44,584삼각형·44드로콜로 통과했고, 사용 중인 GLB 28종의 단일 모델 최대치는 17,767삼각형이다.
- 최종 기록은 `output/validation/hormuz-rts-v102-interaction-validation.json`, `output/validation/hormuz-rts-v102-meshy-combined-arms-validation.json`, `output/validation/hormuz-rts-v102-scenario-matrix-validation.json`, `output/validation/hormuz-v102-mission-routing-validation.json`에 저장했다.

## 2026-07-29 — 전략 텍스처 복구·Google 카메라 투영 일치 v103 (검증 전 기록)

- 대규모 전투 초경량 LOD가 v99부터 원본 UV와 이미지 텍스처를 제거해 항공모함·구축함·유조선이 단색처럼 보이던 원인을 수정한다. 전 유닛 초경량 Meshy 형상에는 병종별 256px 절차형 갑판·패널 디테일 맵을 추가하고, 구축함·항공모함·유조선은 병종당 선택 또는 대표 1기에 한해 원본 Meshy UV와 512px 베이스컬러를 보존한 정밀 LOD를 겹치지 않게 교체 표시한다.
- 정밀 LOD는 UV 경계를 넘는 permissive simplification과 품질을 무시하는 aggressive simplification을 모두 금지해 기존 검은 아틀라스 조각 재발을 차단한다. 세 모델은 각각 구축함 9,320·항공모함 10,484·유조선 13,888삼각형이며, 나머지는 기존 초경량 인스턴스를 유지해 12만 삼각형 상한을 지킨다.
- Google `Map3DElement`의 실제 `fov`를 Three.js 카메라 45도와 동일하게 고정한다. 마우스 휠 확대도 지도 갱신 제한을 우회해 즉시 동기화하며, 중간 버튼 드래그는 기존 동일 중심 좌표와 함께 같은 시야각·거리 투영을 사용한다.
- 전투 UI의 12~13px 글자를 최소 14px로 올리고 고정폭 영문 글꼴을 Pretendard·SUIT 계열 한국어 산세리프로 교체한다. 유닛 이름표는 PC·모바일 모두 15px로 고정한다.
- 데이터 캐시는 version 17, 스타일 캐시는 v54, 전투 번들 캐시는 v103으로 올렸다. 재번들 뒤 텍스처·투영 동기화·상호작용·5개 시나리오·본편 라우팅을 전부 검증한다.

## 2026-07-29 — v103 전략 텍스처·지도 투영 최종 검증

- 실제 Google 3D 고정 전장에서 전략 Meshy 8종 모두 베이스컬러 디테일 맵과 정점색을 함께 사용하고, 구축함·항공모함·유조선 대표 1기는 UV 경계를 보존한 원본 Meshy 512px 텍스처 정밀 LOD로 표시되는지 확인했다. 검은 아틀라스 조각과 형광 팀 색은 재발하지 않았다.
- 실제 마우스 입력 16개 조건을 통과했다. 중간 버튼 283px 드래그 뒤 Google 중심은 `56.05000,26.42000`에서 `56.31888,26.26909`로 이동했고, 휠 확대 뒤 Google range는 145,978m에서 171,143m로 바뀌었다. Google과 Three.js 카메라 FOV는 이동·확대 전후 모두 45도로 일치한다.
- 대규모 전투 전체 화면은 100,737삼각형·58드로콜로 성능 헌법의 83.9%·64.4%다. 나머지는 `convoy_shield` 96,648/48, `mine_corridor` 66,924/53, `missile_screen` 94,129/48, `coastal_battery` 111,308/43이며 모두 통과했다.
- Meshy 결합병과 32개 품질 조건, 육지·해상 배치, 본편 미션 라우팅 5종, 직접 대규모 임무 진입, 콘솔·페이지·HTTP 오류 0을 모두 확인했다.
- 최종 기록은 `output/validation/hormuz-rts-v103-interaction-validation.json`, `output/validation/hormuz-rts-v103-meshy-combined-arms-validation.json`, `output/validation/hormuz-rts-v103-scenario-matrix-validation.json`, `output/validation/hormuz-v103-mission-routing-validation.json`에 저장했다. 전체 화면은 `output/validation/hormuz-rts-v103-strategic-visibility-1920x1080.jpg`, 실제 입력 후 화면은 `output/validation/hormuz-rts-v103-interaction-1920x1080.jpg`다.

## 2026-07-29 — 항공모함 비행갑판 UV 보존 교체 v104 (검증 전 기록)

- v103 항공모함 근접 화면에서 비행갑판의 긴 활주로 선이 지그재그로 휘는 현상을 재현했다. 런타임 회전·크기 행렬이 아니라 10,484삼각형 LOD 생성 과정에서 내부 UV가 단순화된 것이 원인이다.
- 항공모함만 기존 정상 Meshy 전략 모델의 14,103삼각형과 UV 정점을 그대로 보존하고 텍스처만 512px로 줄인 `battle-detail-v4` 전용 모델로 교체했다. 구축함·유조선과 런타임 표시 로직은 변경하지 않았다.
- UV 보존본을 근접 재검사하니 원본 Meshy 베이스컬러에도 굽은 갑판 선이 남아 있었다. 항모 상판에만 실제 비행갑판 비율의 저비용 보정 데칼을 겹쳐 중앙 점선·캐터펄트·착함 유도선을 직선으로 다시 그렸고, 원본 선이 비치지 않도록 갑판 바탕까지 함께 정리했다.
- 데이터 캐시는 version 18, 전투 번들 캐시는 v104로 올렸다. 재번들 뒤 항공모함 근접 화면, 대규모 전장 성능, 5개 시나리오와 본편 라우팅을 검증한다.

## 2026-07-29 — v104 항공모함 갑판 텍스처 최종 검증

- `qa=carrier-texture` 고정 화면에서 항공모함 1을 단독 선택해 전체 갑판을 근접 확인했다. 기존 지그재그 활주로 선은 보이지 않고 중앙 점선·캐터펄트·착함 유도선이 직선으로 표시된다.
- 대규모 전장은 104,362삼각형·59드로콜로 120,000·90 상한을 통과했다. 나머지 4개 전장도 통과했으며 5개 시나리오의 콘솔·페이지·HTTP 오류는 모두 0건이다.
- 본편 미션 라우팅 5종과 Meshy 결합병 32개 조건도 전부 통과했다. 최종 근접 화면은 `output/validation/hormuz-rts-v104-carrier-texture-close-1920x897.jpg`, 전용 판정은 `output/validation/hormuz-rts-v104-carrier-texture-validation.json`에 저장했다.

## 2026-07-29 — 항공모함 외곽 형상 복원 v105 (검증 전 기록)

- v104의 갑판 전체 보정판이 원본 Meshy 선체보다 넓은 평면으로 갑판을 덮어 선수·외곽선이 각진 판처럼 변하는 결함을 근접 화면에서 확인했다.
- 전체 평면과 굵은 외곽 테두리를 제거했다. 원본 메시에서 위쪽을 향하고 실제 갑판 높이에 있는 삼각형만 자동 추출해 같은 형상 위에 재질을 겹치므로 원래 선수·선미·비행갑판 윤곽을 그대로 유지한다.
- 직선 중앙 점선·캐터펄트·착함 유도선은 별도 평면·막대가 아니라 추출한 실제 갑판 삼각형의 평면 UV 재질 안에 넣었다. 따라서 표시가 갑판 외곽으로 돌출하지 않는다. 전투 번들 캐시는 v105로 올렸으며 항모 근접·전체 전장·성능 회귀를 다시 검증한다.

## 2026-07-29 — v105 항공모함 원형 복원 최종 검증

- 항공모함 전용 QA 화면에서 기존 원본과 비교해 선수·선미·비행갑판 외곽이 Meshy 메시 원형을 유지하는지 직접 확인했다. v104의 큰 각진 갑판 평면과 굵은 외곽선, 갑판 밖으로 돌출하던 표시 물체는 모두 제거됐다.
- 원본 메시에서 실제 갑판 높이의 상향 삼각형 1,638개만 추출했고, 4,914개 UV 정점에 중앙 점선·캐터펄트·착함 유도선을 재질로 투영했다. 표시선은 실제 갑판 삼각형 경계에서 잘리므로 선체 밖으로 나오지 않는다.
- 항모 단독 QA는 80,904삼각형·49드로콜, 대규모 전체 전장은 105,994삼각형·59드로콜로 성능 상한을 통과했다. 5개 전장, 본편 라우팅 5종, Meshy 결합병 32개 조건이 모두 통과했고 콘솔·페이지·HTTP 오류는 0건이다.
- 최종 근접 화면은 `output/validation/hormuz-rts-v105-carrier-shape-1920x1080.jpg`, 판정 기록은 `output/validation/hormuz-rts-v105-carrier-shape-validation.json`에 저장했다.

## 2026-07-29 — Meshy 저폴리곤 상시 텍스처 전략 유닛 v106 (검증 전 기록)

- 사용자 승인 25크레딧으로 구축함·유조선·MCM 무인수상정·F/A-18E·B-2 5종을 Meshy 리메시했다. 목표는 각각 3,000·3,500·3,000·2,000·2,500면이며 실제 결과는 3,057·3,605·2,973·1,877·2,448삼각형이다.
- 4K 베이스컬러·노멀을 포함한 원본 리메시 GLB를 WebP 1,024px·Meshopt 압축으로 웹 최적화했다. 최종 파일은 176~329KB이고 5종 모두 단일 메시·단일 재질·베이스컬러·노멀 맵을 유지한다.
- 대규모 전투에서 선택한 대표 1기만 정밀 텍스처로 교체하던 구축함·유조선 경로를 제거했다. 5종은 선택·비선택과 무관하게 동일한 저폴리곤 Meshy 텍스처 모델을 GPU 인스턴스로 공유한다.
- 데이터 캐시는 version 19, 전투 번들 캐시는 v106으로 올렸다. Google 3D 실화면의 선택 전·후 텍스처 지속성, 전 시나리오 성능·라우팅·사용 모델 삼각형을 다음 단계에서 검증한다.

## 2026-07-29 — v106 Meshy 상시 텍스처 모델 최종 검증

- 실제 Google 3D 고정 전장에서 구축함·유조선·MCM 무인수상정·F/A-18E·B-2 5종이 모두 Meshy 베이스컬러·노멀 텍스처를 사용하는지 확인했다. 구축함 1기를 선택한 뒤에도 5종 전 인스턴스의 텍스처와 표시 수가 그대로 유지되고, 선택 해제 상태용 무텍스처 모델로 교체되지 않는 34개 조건을 통과했다.
- 결합병과 전장은 70,573삼각형·44드로콜이다. 전 시나리오는 `convoy_shield` 96,648/48, `mine_corridor` 66,924/53, `missile_screen` 94,129/48, `coastal_battery` 111,308/43, `large_fleet_battle` 108,775/57(삼각형/드로콜)로 120,000·90 상한을 통과했다.
- 본편 카드→미션→RTS 시나리오 5종과 `mission=large` 직접 진입, 캠페인 로드맵 정합성도 통과했다. 콘솔·페이지·HTTP 오류와 해상 유닛 육지 침범은 0건이다.
- 런타임에는 176~329KB 웹 GLB 5개만 남겼다. 25크레딧 원본 GLB와 4K 베이스컬러·노멀 텍스처는 삭제하지 않고 `assets/meshy-source/remesh-v106/`으로 분리 보관했다.
- 최종 화면은 `output/validation/hormuz-rts-v106-strategic-visibility-1920x1080.jpg`, 선택 후 화면은 `output/validation/hormuz-rts-v106-strategic-texture-selected-1920x1080.jpg`이며, 판정 기록은 `output/validation/hormuz-rts-v106-meshy-combined-arms-validation.json`, `output/validation/hormuz-rts-v106-scenario-matrix-validation.json`, `output/validation/hormuz-v106-mission-routing-validation.json`이다.

## 2026-07-29 — 본편 상황실·전투 Meshy 상시 텍스처 적용 v34 (검증 전 기록)

- `index.html` 본편 상황실이 별도로 불러오던 구축함·유조선·F/A-18E·MCM 무인수상정의 구형 고용량 GLB를 v106 Meshy 저폴리곤 베이스컬러·노멀 텍스처 모델로 교체했다.
- 본편 로컬 3D 지도에서는 네 모델의 원본 색을 흰 기본색·sRGB 텍스처·발광 0으로 보존한다. 기존 청록 발광이 표면 텍스처를 덮지 않으며, 팀 구분은 지도 UI와 전술 표식이 담당한다.
- 본편 Google 3D 상황실의 확대 구축함도 동일한 3,057삼각형 웹 GLB로 교체했다. 본편 인트로 전투와 임무 iframe에는 `v=106`을 명시해 구버전 전투 HTML 캐시가 열리지 않게 했다.
- 본편 번들 캐시는 v34로 올렸다. 본편 상황실 모델 재질·Google 3D 모델 경로·전투 iframe 버전·성능·라우팅을 다음 단계에서 검증한다.

## 2026-07-29 — 본편 v34 Meshy 텍스처·Google 3D 최종 검증

- 본편 로컬 상황실에서 구축함·유조선·F/A-18E·MCM 무인수상정의 신규 GLB 요청이 모두 HTTP 200이고, 각 모델이 베이스컬러·노멀 맵·흰 기본색·발광 0을 유지하는지 확인했다. 기존 유조선 3척 배치도 그대로다.
- Google 공식 3D 모델 규격은 glTF 확장을 지원하지 않으므로 Meshopt·WebP 확장본을 그대로 쓰지 않았다. 구축함 3,057삼각형을 표준 PBR·JPEG 1,024px·확장 0개인 733KB `google-v34.glb`로 별도 패키징해 실제 Google 3D 상황실에 표시되는 것을 화면으로 확인했다.
- 본편 로컬 상황실은 15,274삼각형·21드로콜로 성능 헌법을 통과했다. 전투 5종도 최대 `coastal_battery` 111,308삼각형, 대규모전 108,775삼각형·57드로콜로 모두 통과했다.
- 본편 미션 5종 라우팅과 `mission=large` 직접 진입이 통과했고, 전투 iframe URL에 `v=106`이 포함된다. 콘솔·페이지·HTTP 오류와 해상 유닛 육지 침범은 0건이다.
- 본편 화면은 `output/validation/hormuz-main-v34-meshy-texture-showcase-1920x1080.jpg`, Google 화면은 `output/validation/hormuz-main-v34-google-3d-remesh-destroyer-1920x1080.jpg`다. 판정 기록은 `output/validation/hormuz-main-v34-meshy-texture-validation.json`, `output/validation/hormuz-rts-main-v34-scenario-matrix-validation.json`, `output/validation/hormuz-main-v34-mission-routing-validation.json`에 저장했다.

## 2026-07-29 — 쉬운 아군 편성·기동 미사일 회피 v107 / 본편 v35 (검증 전 기록)

- 전투 준비 화면의 명칭을 `전투기·헬기·구축함·항공모함·무인수상정·폭격기·지상병력`으로 단순화한다. 7종 카드를 항상 보여 주고, 이번 작전에 필요한 전력은 기본 출전 수량을 넣으며 필요 없는 전력만 0대로 표시한다.
- 일반 호송전에는 항공모함·무인수상정을, 기뢰전에는 무인수상정을, 연안 타격에는 폭격기·지상병력을 기본 편성한다. 공중방어와 대규모전도 작전별 권장 편성을 기본값으로 사용하며, 플레이어는 예산 안에서 0대부터 다시 조정할 수 있다.
- 신규 전력이 실제 편성 비용에 포함되도록 본편 미션·작전 예산을 동기화한다. 출전·선택 안 함·이번 작전 제외 상태를 카드에서 즉시 구분하고, 14px 이상 한국어 표시를 유지한다.
- 적 유도탄은 무한 추적하지 않는다. 아군이 우클릭 급선회·분산 이동하면 제한된 선회력·시야각·유도 시간 때문에 표적을 놓치고 수면에 떨어질 수 있게 한다. 접근 시 한국어 회피 안내를 표시한다.
- 데이터 캐시는 version 20, 스타일 캐시는 v55, 전투 번들 캐시는 v107, 본편 번들 캐시는 v35로 올렸다. 5개 전장 성능·본편 연결·실제 편성·미사일 회피를 다음 단계에서 검증한다.

## 2026-07-29 — v107 아군 기본 편성·미사일 회피 최종 검증

- 5개 작전 모두에서 7종 카드가 숨김 없이 표시되고 쉬운 한국어 이름, 최소 14px 글자, 기본 출전 수량, `0대` 제외 후 재추가를 통과했다. 일반전은 구축함 1·전투기 2·헬기 1·항공모함 1·무인수상정 1을 기본 편성하고 폭격기 0·지상병력 0으로 시작한다.
- 기뢰전은 무인수상정 2, 연안전은 폭격기 1·지상병력 2, 공중방어는 항공모함 1, 대규모전은 7종 전부가 기본 편성된다. 신규 병종의 작전별 기본 포함과 본편 예산 상한을 `missions.json`·`campaign.json`에 동기화했다.
- 적 미사일 시험에서 목표 전투기가 급기동으로 추적 시야를 벗어나 `guidedEvades=1`, 체력 `70→70`을 기록했다. 접근 안내 문구와 추적 종료 후 빗나감 판정도 통과했다.
- 일반전의 신규 무인수상정이 성능 상한을 넘기던 문제는 17,767삼각형 구형 LOD를 2,973삼각형 Meshy 텍스처 리메시본으로 교체해 해결했다.
- 5개 전장은 `convoy_shield` 116,514/61, `mine_corridor` 62,622/55, `missile_screen` 110,446/55, `coastal_battery` 111,481/56, `large_fleet_battle` 108,775/57(삼각형/드로콜)로 모두 120,000·90 상한을 통과했다. 콘솔·페이지·HTTP 오류와 해상 유닛 육지 침범은 0건이다.
- 본편 카드→미션→RTS 5종, 직접 대규모전 `v=107`, 캠페인 로드맵이 모두 통과했다. 최종 기록은 `output/validation/hormuz-rts-v107-final-scenario-matrix-validation.json`, `output/validation/hormuz-v107-final-mission-routing-validation.json`, `output/validation/hormuz-rts-v107-final3-force-selection-evasion-validation.json`이며 편성 화면은 `output/validation/hormuz-rts-v107-final3-force-selection-1920x1080.jpg`다.

## 2026-07-30 — 나포 유조선 3단계 구출 작전 v108 / 본편 v36 (검증 전 기록)

- 작전 04 `tanker_rescue`를 라락섬 남동 해역의 실제 플레이 가능한 RTS 임무로 추가했다. 고속정 6척 차단 → 구조 헬기 5초간 접근 유지 → 확보된 유조선의 안전 해역 이탈 엄호 순으로 진행한다.
- 구조 대상 유조선은 확보 전에는 정지하고, 구조 완료 뒤에만 실제 항로를 따라 이동한다. 전장에는 단계별 노랑·청록·초록 구조 원과 한국어 진행 안내를 표시하며, 헬기를 0대로 편성하면 작전 시작을 막고 필요한 이유를 안내한다.
- 기존 Meshy 유조선·고속정·MH-60R·구축함·전투기·무인수상정 모델만 재사용해 유료 크레딧은 쓰지 않았다. 고속정 6척과 구조 유조선은 저폴리곤 Meshy 전략 모델을 GPU 인스턴스로 공유한다.
- `missions.json`·`campaign.json`에 작전 예산 $480M 권장/$600M 상한과 성공·실패 수치 변화를 연결했다. `선원 구출 준비` 카드와 `유조선 나포` 사건의 `구출 작전`, 직접 주소 `mission=rescue`가 같은 임무를 호출하도록 본편 라우팅을 확장했다.
- 데이터 캐시는 version 21, 전투 번들 캐시는 v108, 본편 데이터 캐시는 v31, 본편 번들 캐시는 v36으로 올렸다. 재번들·6개 전장 성능·본편 라우팅·구조 단계 전환·Google 3D 실화면 검증은 이 기록 다음에 수행한다.

## 2026-07-30 — v108 나포 유조선 구출 작전 최종 검증

- 구출 작전 기본 편성은 구축함 1·전투기 1·헬기 1·무인수상정 1이며 권장 예산을 실제 편성비와 맞는 $425M으로 확정했다. 앞 검증 전 기록의 $480M 표기는 이 값으로 정정한다.
- 실제 Google 3D 라락섬 남동 해역에서 고속정 6척 차단 → 헬기 구조 원 5초 유지 → 유조선 안전 해역 이탈 3단계와 단계별 노랑·청록·초록 표식을 확인했다.
- 편성 검사에서 6개 작전 모두 7종 카드·쉬운 한국어 명칭·0대 제외·기본 출전값을 통과했다. 구출 작전은 헬기 0대일 때 시작을 막으며, 단계 전환 `approach→egress`와 최종 성공 판정도 통과했다.
- 본편 라우팅은 `swarm`·`minesweep`·`intercept`·`rescue`·`bombrun`·`large` 6종과 카드·사건 연결이 모두 일치했다. 직접 주소 `mission=large`와 `mission=rescue`도 과거 선택 화면 없이 각각 v108 전투 iframe을 열었다. 두 무거운 전투 페이지를 동시에 유지해 생기던 검증 시간 초과는 페이지별 WebGL 컨텍스트를 즉시 닫도록 검사기를 수정해 제거했다.
- 6개 전장은 `convoy_shield` 116,514/61, `tanker_rescue` 114,268/47, `mine_corridor` 62,622/55, `missile_screen` 110,446/55, `coastal_battery` 111,481/56, `large_fleet_battle` 108,775/57(삼각형/드로콜)로 120,000·90 상한을 전부 통과했다. 콘솔·페이지·HTTP 오류와 해상 유닛 육지 침범은 0건이다.
- 적 유도탄 급기동 회피는 `guidedEvades=1`, 체력 `70→70`으로 유지됐다. 사용 중인 GLB 32종의 최대치는 환경 크레인 감축본 22,777삼각형이며, 4만 삼각형대 Meshy 원본은 런타임에 없다.
- 최종 기록은 `output/validation/hormuz-rts-v108-final2-scenario-matrix-validation.json`, `output/validation/hormuz-rts-v108-final-force-selection-evasion-validation.json`, `output/validation/hormuz-v108-final-mission-routing-validation.json`에 저장했다. 최신 기본 편성(헬기 1대)을 적용한 실제 Google 3D 화면은 `output/validation/hormuz-rts-v108-final-google-tanker-rescue-1920x1080.jpg`다.

## 2026-07-30 — 전 진영 유닛 관통 방지 v109 / 본편 v37 (검증 전 기록)

- 기존 이동 분리는 같은 팀끼리만 적용돼 아군 함정이 민간 유조선이나 적 함정을 장애물로 인식하지 않았다. 유조선 항로 이동도 유닛 충돌 검사를 건너뛰어 정지한 나포 유조선을 다른 함정이 뚫고 지나갈 수 있었다.
- 수면·지상·공중의 동일 영역 유닛은 진영과 관계없이 각 모델의 충돌 반경 합계를 최소 간격으로 사용한다. 접근 단계에서 감속과 좌우 우회를 적용하고, 다음 위치가 상대 내부로 들어가면 이동을 차단한다.
- 한 프레임에 양쪽 유닛이 동시에 접근해 이미 간격이 깨진 경우를 위해 사후 겹침 분리를 추가했다. 고정 물체와 구조 전 나포 유조선은 움직이지 않고 상대 유닛만 밀려나며, 수면 함정은 육지로 밀리지 않는다.
- 민간 유조선의 항로 이동에도 같은 차단 검사를 연결했다. 구조 헬기는 공중 유닛이라 수면의 유조선 위 구조 원에 정상 진입할 수 있고, 서로 고도가 충분히 다른 공중 유닛도 불필요하게 충돌하지 않는다.
- 전투 번들 캐시는 v109, 본편 RTS 연결은 v109, 본편 번들 캐시는 v37로 올렸다. 나포 유조선 횡단 재현·6개 전장 성능·본편 라우팅 검증은 이 기록 다음에 수행한다.

## 2026-07-30 — v109 전 진영 유닛 관통 방지 최종 검증

- 구축함을 나포 유조선 정중앙 반대편으로 이동시키는 30초 횡단 시험을 자동화했다. 두 선체의 충돌 반경 합계 7.7362를 기준으로 실제 최소 간격 7.5815, 비율 0.980을 유지했고 선체 내부 진입은 0건이었다.
- 구조 전 나포 유조선은 시험 내내 이동량 0을 유지했다. 구축함은 감속·우회한 뒤 유조선 반대편까지 통과했으며, 최종 활성 겹침도 0건이다.
- 같은 검증에서 쉬운 아군 편성·0대 제외·적 미사일 급기동 회피·구출 작전 `approach→egress` 성공 판정이 함께 통과했다. 콘솔·페이지·HTTP 오류는 0건이다.
- 6개 전장은 `convoy_shield` 116,514/61, `tanker_rescue` 114,268/47, `mine_corridor` 62,622/55, `missile_screen` 110,446/55, `coastal_battery` 111,481/56, `large_fleet_battle` 108,775/57(삼각형/드로콜)로 120,000·90 상한을 전부 통과했다. 해상 유닛 육지 침범도 0건이다.
- 본편 카드→미션→RTS 6종, `mission=large`·`mission=rescue` 직접 진입, v109 iframe 연결과 캠페인 로드맵을 모두 통과했다.
- 최종 기록은 `output/validation/hormuz-rts-v109-final-force-selection-evasion-validation.json`, `output/validation/hormuz-rts-v109-final-scenario-matrix-validation.json`, `output/validation/hormuz-v109-final-mission-routing-validation.json`이다. 로컬 전장 화면은 `output/validation/hormuz-rts-v109-final-unit-collision-1920x1080.jpg`, 실제 Google 3D 화면은 `output/validation/hormuz-rts-v109-final-google-unit-collision-1920x1080.jpg`다.

## 2026-07-30 — 곡괭이산 B-2·GBU-57 제한 타격 v110 / 본편 v38 (검증 전 기록)

- 공개자료를 재조사해 곡괭이산은 나탄즈 농축시설 자체가 아니라 남쪽에 별도로 건설 중인 지하시설이며, IAEA가 내부 용도를 확인하지 못했다는 사실 경계를 `docs/hormuz-pickaxe-mountain-research-v1.md`에 정리했다.
- GBU-57은 30,000파운드급 지하시설 관통탄이고 공개 운용 기준 B-2에서만 투하한다. B-2는 전투기가 아니라 스텔스 전략폭격기이며, 2025년 실제 작전의 B-2 7대·GBU-57 14발 운용 기록을 게임 규칙의 근거로 삼는다.
- 신규 `pickaxe_mountain` 임무는 B-2가 반드시 1대 이상 있어야 시작되며, 한 대당 GBU-57 2발만 사용한다. 목표는 “핵시설 완전 파괴”가 아니라 확인된 지하 진입부 2곳 봉쇄이고 내부 피해평가는 미확정으로 남긴다.
- 본편의 `B-2 정밀 폭격` 카드와 `핵시계` 사건은 기존 연안 이동식 발사 차량 임무가 아니라 신규 곡괭이산 임무로 연결한다. 기존 연안 포대 제압은 별도 정밀타격 임무로 유지한다.
- 전투 번들 캐시는 v110, 전투 데이터는 v22, 본편 데이터 캐시는 v32, 본편 번들은 v38로 올렸다. 신규 전장·B-2 전용 편성·2발 제한·본편 라우팅·전체 성능 검증은 이 기록 다음에 수행한다.

## 2026-07-30 — v110 곡괭이산 제한 타격 최종 검증

- 편성 화면은 B-2만 출격 가능하며 0대일 때 `GBU-57은 B-2만 운반`한다는 이유와 함께 시작을 막는다. 실제 전투에서 GBU-57 2발이 진입부 2곳에 각각 충돌했고, 세 번째 발사는 거부됐다.
- 진입부 2곳만 필수 목표로 계산한다. 주변 방공 TEL 3기가 살아 있어도 `2/2 봉쇄`로 승리하며 결과는 `내부 피해와 용도는 확인되지 않았다`고 표시한다.
- 기존 해협 브리핑 이미지를 곡괭이산 전용 B-2 접근 이미지와 진입부 봉쇄 결과 이미지로 교체했다. 로컬 지형에는 밝은 암반 텍스처·산악 능선·표적 분지를 추가했고 Google 3D 지도 위에도 반투명 입체 능선을 합성한다.
- `tools/validate-pickaxe-strike.py`에서 B-2 전용 편성, 2발 충돌, 탄약 제한, 목표 전용 판정, Meshy B-2 로딩, 전용 이미지, Google 3D 연결, 런타임 오류 0건을 모두 통과했다.
- 7개 전장은 `convoy_shield` 116,514/61, `tanker_rescue` 114,268/47, `mine_corridor` 62,622/55, `missile_screen` 110,446/55, `coastal_battery` 111,481/56, `pickaxe_mountain` 69,149/25, `large_fleet_battle` 108,775/57(삼각형/드로콜)로 120,000·90 상한을 전부 통과했다.
- 편성 기본값·0대 제외·적 미사일 회피·유조선 구출·유닛 관통 방지 회귀 검증도 통과했다. 본편 `mission=pickaxe` 직접 진입과 카드·사건→미션→RTS 7종 라우팅은 콘솔·페이지·HTTP 오류 0건으로 통과했다.
- 전체 완료 감사 결과는 9개 작전 중 7개 플레이 가능이다. `base_retaliation`, `ceasefire_cover`는 아직 planned이고, Firebase 랭킹(M3)과 공개 패키징(M4)은 미완료이므로 전체 게임을 완료로 표시하지 않았다. 정본은 `docs/hormuz-completion-audit-2026-07-30.md`다.
- 최종 기록은 `output/validation/hormuz-rts-v110-release-scenario-matrix-validation.json`, `output/validation/hormuz-rts-v110-final-force-selection-evasion-validation.json`, `output/validation/hormuz-v110-release-mission-routing-validation.json`, `output/validation/hormuz-rts-v110-final-pickaxe-strike.json`에 저장했다.

## 2026-07-30 — 메인 타이틀 화면 v29

- Codex 내장 ImageGen으로 기존 시네마틱 시안의 색감과 완성도를 계승한 16:9 전용 키아트를 생성했다. 왼쪽은 제목·버튼용 여백, 오른쪽은 항공모함 전단·호송 유조선·해협의 일출이 보이도록 구성했다.
- `index.html` 첫 진입 화면을 전용 키아트, 한국어 작전 카피, 두 단계 버튼 정보, 주요 게임 요소 카드로 재설계했다. 기존 3D 월드와 HUD가 제목 뒤에서 비쳐 보이던 문제를 제거했다.
- 데스크톱과 모바일에서 서로 다른 배경 초점·명암 그라데이션을 사용하고, 모바일 버튼과 본문 글자 크기를 보장했다. CSS 캐시 버전을 v29로 올렸다.
- 생성 원본 PNG, 웹 최적화 WebP, 재생성 가능한 최종 프롬프트 문서를 함께 보관했다.
- 실제 로컬 서버에서 1600×900·430×932 화면을 캡처해 제목, 본문, 버튼, 주요 기능 카드가 잘리지 않는 것을 확인했다. `역사 선택 후 시작` 버튼도 기존 역사 분기 화면으로 정상 진입했다.

## 2026-07-30 — 가상 대통령 메인 타이틀 v30

- 기존 함대 키아트의 왼쪽 제목 여백과 호르무즈 해협 구도를 유지하면서, 오른쪽 전경에 트럼프 스타일의 결단력 있는 가상 미국 대통령을 추가했다. 실제 인물의 정확한 얼굴 복제 대신 게임 고유 캐릭터로 구성했다.
- 항공모함 전단과 호송 유조선이 인물 뒤에서도 식별되도록 함대를 중앙 쪽으로 재구성했다. 원본 PNG와 약 161KB의 웹 최적화 WebP를 함께 보관했다.
- `index.html` 이미지 프리로드와 `game.css` 배경 경로를 대통령 버전으로 교체하고 CSS 캐시 버전을 v30으로 올렸다.
- 첫 모바일 검수에서 중앙 해협만 보이고 대통령이 화면 밖으로 잘리는 것을 확인했다. 모바일 배경 초점을 오른쪽 88%로 옮겨 대통령 얼굴과 해협을 함께 보여주고 CSS 캐시 버전을 v31로 올렸다.
- 두 번째 모바일 검수에서는 작전 상태 문구가 인물 머리 위에 겹쳐 보여 왼쪽 상단의 어두운 하늘 영역으로 옮겼다. 최종 CSS 캐시 버전은 v32다.
- 최종 1600×900 검수에서 가상 대통령, 항공모함, 호송 선박, 제목과 시작 버튼이 동시에 식별됐다. 430×932에서는 대통령 얼굴을 상단 키비주얼로 살리고 하단 타이틀 UI가 잘리지 않는 것을 확인했다.

## 2026-07-30 — 가상 양국 지도자 뒷모습 타이틀 v33

- 공개 배포 시 초상권·퍼블리시티권·허위 후원 오인 위험을 낮추기 위해 실존 인물과 유사한 정면형 대통령 이미지를 런타임에서 제거했다.
- 기존 해협·항공모함 전단 키아트를 유지하면서, 오른쪽에는 얼굴과 측면 윤곽이 전혀 보이지 않는 가상 미국 대통령의 뒷모습을 배치했다. 해협 건너 이란 산악에는 얼굴 없는 가상 이란 지도자 실루엣을 두어 양측의 전략 대치를 암시했다.
- 두 인물에서 실제 정치인의 고유 헤어스타일, 얼굴, 배지, 국기, 선거 구호와 이름을 제거했다. 항공모함·호송 유조선과 청록·황금 작전 궤적은 그대로 유지했다.
- `index.html` 프리로드와 `game.css` 배경 경로를 v3 WebP로 교체하고 CSS 캐시 버전을 v33으로 올렸다. 실존 인물 유사 버전의 프롬프트 문서는 삭제하고 가상 지도자 전용 프롬프트 문서로 교체했다.
- 이전 v2 PNG·WebP는 공개 자산 폴더에서 제거해 `.internal/legal-review/`로 이동했다. 런타임과 공개 배포 대상에서는 참조되지 않는다.
- 1600×900 검수에서 양국 지도자의 뒷모습, 해협, 항공모함과 제목 UI가 함께 식별됐다. 430×932에서도 미국 대통령의 뒷모습과 산악 위 이란 지도자 실루엣이 동시에 보이며 얼굴·측면 윤곽은 노출되지 않았다.
- 공개용 프롬프트 문서에서도 특정 실존 정치인의 이름을 제거하고 `어떤 공인과도 정확히 닮지 않음`이라는 일반 제약만 남겼다.

## 2026-07-30 — 식별 가능한 대통령 뒷모습 타이틀 v34

- 사용자의 추가 지시에 따라 얼굴을 계속 완전히 숨기면서도 미국 대통령의 뒤 실루엣을 더 알아보기 쉽게 조정했다.
- 금발 머리의 부피와 뒤로 넘긴 결, 넓고 각진 어깨, 두꺼운 상체와 목, 약간 앞으로 기운 권위적인 자세를 강화하고 체격을 약 15% 키웠다.
- 해협·항공모함·호송 선박·작전 궤적과 산악 쪽 가상 이란 지도자 실루엣은 유지했다. 실제 이름, 음성, 발언, 선거 구호, 국기와 얼굴은 사용하지 않았다.
- `index.html` 프리로드와 `game.css` 배경 경로를 v4 WebP로 교체하고 CSS 캐시 버전을 v34로 올렸다.

## 2026-07-30 — 원거리 상대 지도자 원근 수정 v35

- 사용자 캡처에서 산맥과 합쳐져 거대한 유령처럼 보이던 원거리 상대 지도자를 확인했다.
- 잘못된 상반신 합성물을 제거하고, 해안 전망대 바닥에 발을 딛고 선 정상 비례의 전신 후면 실루엣으로 교체했다.
- 난간·바닥 접점·그림자를 추가해 떠 있는 인상을 없앴고, 특정 실존 인물 얼굴이 아닌 가상 이란 지도부의 상징으로 유지했다.
- 오른쪽 미국 대통령의 금발·넓은 체격 후면 실루엣과 함대·해협·작전 궤적은 유지했다.
- `index.html` 프리로드와 `game.css` 배경 경로를 v5 WebP로 교체하고 CSS 캐시 버전을 v35로 올렸다.
- 로컬 서버에서 시작 화면과 PNG·WebP·CSS 응답이 모두 HTTP 200인지 확인하고 실제 브라우저 화면을 캡처했다.

## 2026-07-30 — 초기 고품질 배경 + 대통령 단독 후면 타이틀 v36

- 사용자의 재지시에 따라 지도자 2인 구도를 폐기하고, 인물이 없던 초기 고품질 해협 키아트를 다시 배경 정본으로 사용했다.
- 해협·산악·수면 반사·항공모함 전단·유조선·작전 항로를 유지한 채 오른쪽 끝에 금발·넓은 체격의 미국 대통령 후면 실루엣 한 명만 추가했다.
- 이란 측 인물, 원거리 사람, 유령형 합성물은 모두 제거했다.
- 항공모함 비행갑판과 양쪽 호위함이 인물에 가려지지 않도록 인물을 오른쪽 22% 이내로 제한했다.
- `index.html` 프리로드와 `game.css` 배경 경로를 v6 WebP로 교체하고 CSS 캐시 버전을 v36으로 올렸다.
- 실제 브라우저 시작 화면에서 항공모함 전단과 대통령 후면 인물이 동시에 선명하게 보이고 콘솔 경고·오류가 없는 것을 확인했다. HTML·CSS·WebP 응답도 모두 HTTP 200이다.

## 2026-07-30 — 스텔스 폭격기 전방축·해병 소총 결착 v111 (검증 전 기록)

- 일반 명칭 `폭격기`를 `스텔스 폭격기`로 바꾸고 B-2 단위 명칭을 `B-2A 스피릿 스텔스 폭격기`로 통일했다.
- B-2 Meshy GLB의 기수가 로컬 `-X`축을 향하는 것을 원본 정점 분포로 확인했다. 기존 `modelYaw=0`을 `+90도`로 보정해 게임 진행축과 시각 기수 방향을 일치시켰다.
- 미 해병 조준 애니메이션에서 기존 총열과 양손 방향이 약 53.5도 어긋난 원인을 확인했다. 오른손 권총손잡이와 왼손 앞손잡이 방향을 기준으로 M27 위치·회전을 다시 계산했다.
- 이란 지상군의 AK 결착값도 별도 골격의 양손 위치를 기준으로 다시 계산했다.
- 런타임 스냅샷에 B-2 시각축 정렬값, 해병 총열·양손 정렬값과 오른손 손잡이 오차를 추가했다. 전투 데이터 캐시는 v23, 전투 번들은 v111로 올렸다.

## 2026-07-30 — 스텔스 폭격기·해병 소총 결착 v111 검증 완료

- 본편 전투 진입 캐시도 `RTS_RUNTIME_VERSION=111`, `game.bundle.js?v=39`로 올려 수정된 전투 화면이 즉시 로드되게 했다.
- 실제 런타임 측정에서 B-2 시각 기수와 이동 방향 내적은 `1.000`, 아군 해병 4명의 총열·양손 정렬 내적은 모두 `0.996`, 손잡이 결착 오차는 모두 `0.000`으로 확인했다.
- 시나리오 7종, 미션 라우팅, 전력 선택·미사일 회피, Meshy 연합전력 회귀 검증이 모두 통과했다.
- 임무 선택 UI가 별도로 보유하던 전투 캐시 상수도 v111로 맞추고, 라우팅 검증의 기대 버전도 v111로 갱신했다.

## 2026-07-30 — 소총 수직 결착·GBU-57 2발·항모 함재기 공격 v112 (검증 전 기록)

- 사용자 캡처에서 해병 M27 권총손잡이가 옆으로 누운 것을 재현했다. 기존 총열·양손 정렬은 유지하면서 소총 로컬 아래축을 세계 바닥 방향에 투영해 롤 회전과 손잡이 오프셋을 다시 계산했다.
- 수정 전 손잡이 아래축 정렬값은 `-0.128`이었으며 새 계산값은 `0.994`다. 이란 지상군 AK도 같은 방식으로 별도 골격값을 계산했다.
- B-2 한 대의 GBU-57은 코드상 2발이었지만 첫 진입부 파괴 후 공격 명령이 끝나 남은 1발을 알아보기 어려웠다. 부대 목록·선택 정보에 `GBU-57 2/2발`을 표시하고 첫 타격 뒤 5.2초 동안 두 번째 진입부 우클릭 안내를 띄우게 했다.
- GBU-57 게임 피해 `260`은 진입부 체력 `230`보다 높아 한 발로 진입부 하나를 봉쇄한다. 약해 보이던 표현은 전용 먼지기둥·지표 충격파·강한 카메라 진동·대형 폭발음으로 보강했다.
- 미 해군 공식 역할에 맞춰 항공모함의 장거리 주 공격을 선체 ESSM/RAM 발사에서 `F/A-18E/F 함재기 타격편대` 출격으로 변경했다. 선체 직접 사격은 6 거리 이내 CIWS 근접방어만 남겼다.
- 전투 데이터 캐시는 v24, 전투 번들은 v112, 본편 전투 연결은 v112, 본편 번들 캐시는 v40으로 올렸다.

## 2026-07-30 — 소총·GBU-57·항모 함재기 공격 v112 최종 검증

- 해병 M27 손잡이 아래축 정렬은 `0.997`, 이란 지상군 AK는 `0.997`로 바닥 방향을 향하며 총열·양손 정렬과 손잡이 결착도 함께 통과했다.
- B-2 한 대가 GBU-57 2발을 서로 다른 지하 진입부 2곳에 투하했고, 2곳 모두 봉쇄됐다. 첫 타격 후 다음 목표를 지정하면 두 번째 공격 진입이 즉시 이어지고 전용 관통탄 먼지기둥·충격파 효과가 발생한다.
- 항공모함의 주 공격은 `F/A-18E/F 함재기 타격편대` 2기 출격으로 동작하고, 목표 타격 후 전장을 통과해 복귀한다. 항모 선체 무장은 CIWS 근접방어로만 분리됐다.
- 시나리오 7종은 `convoy_shield` 116,514/61, `tanker_rescue` 114,268/47, `mine_corridor` 62,622/55, `missile_screen` 110,446/55, `coastal_battery` 111,481/56, `pickaxe_mountain` 69,149/25, `large_fleet_battle` 108,775/57(삼각형/드로콜)로 120,000·90 상한을 모두 통과했다. 지상 유닛의 바다 배치는 0건이다.
- 본편 카드·직접 진입·전투 iframe v112 라우팅 7종, 전력 기본값·0대 제외·적 미사일 회피·유조선 구출·유닛 충돌 방지, Meshy 객체·텍스처·애니메이션 회귀검사가 모두 통과했다.
- 실제 Chrome의 Google 3D 전장에서 아군 18기, 유조선 3척, 항공모함과 지상군이 실제 지형 위에 배치되고 전투가 시작되는 것을 확인했다. 항공모함 목록의 `함재기 타격편대 2기 출격 준비` 표시와 브라우저 오류·경고 0건도 확인했다.
- 최종 기록은 `output/validation/hormuz-rts-v112-scenario-matrix-validation.json`, `output/validation/hormuz-v112-mission-routing-validation.json`, `output/validation/hormuz-rts-v112-force-selection-evasion-validation.json`, `output/validation/hormuz-rts-v112-meshy-combined-arms-validation.json`, `output/validation/hormuz-rts-v112-stealth-bomber-marine-alignment-validation.json`, `output/validation/hormuz-rts-v112-carrier-air-wing-validation.json`에 저장했다.

## 2026-07-30 — 미·이란 지상군 어깨 조준 자세 v113 (검증 전 기록)

- 사용자 캡처를 기준으로 이란 지상군 AK가 얼굴 옆에서 수직으로 서고, 미 해병 M27이 복부 높이에 놓이는 두 문제를 별도로 재현했다.
- 이란군 전략 LOD를 구울 때 손뼈 쿼터니언을 소총 로컬 회전이 덮어쓰던 합성 순서 오류를 수정했다. 손의 위치·회전을 유지한 뒤 소총 오프셋을 로컬 변환으로 합성한다.
- 양측 전투 애니메이션에서 총열이 부대의 실제 목표 방향을 향하도록 시각 전방축 보정값을 추가했다.
- 미 해병은 원본 전투 애니메이션의 손 높이가 낮아 보이는 문제를 해결하기 위해 양팔의 상완·전완 뼈를 찾고, 전투 중 양손을 같은 높이만큼 올리는 2관절 CCD IK 보정을 추가했다. 소총은 오른손에 결착된 상태로 함께 올라가고 왼손 앞손잡이 지지는 유지한다.
- 전투 데이터는 version 22, 전투 번들은 v113, 본편 연결은 v113, 본편 번들 캐시는 v41로 올렸다. 양측 병사 한 명씩만 근접 배치하는 `qa=marine-posture` 전용 검수 장면도 추가했다.

## 2026-07-30 — 미·이란 지상군 어깨 조준 자세 v113 최종 검증

- 첫 손 높이 보정은 미 해병 소총을 가슴까지 올렸지만 개머리판이 어깨에서 떨어져 있어 폐기했다. 최종 구현은 오른쪽 어깨와 개머리판을 기준점으로 삼아 오른손을 권총손잡이에 결착하고, 왼손은 소총 앞손잡이에 별도 IK로 결착한다.
- 소총 세계 회전은 목표 방향을 +X 총열축, 세계 위쪽을 +Y축으로 고정한다. 따라서 병사가 걷거나 사격 애니메이션이 바뀌어도 총열이 옆이나 위로 돌아가지 않는다.
- 전용 근접 검증에서 미 해병은 목표 방향 정렬 `1.000`, 손잡이–오른손 오차 `0.000`, 앞손잡이–왼손 오차 `0.001`, 개머리판–어깨 오차 `0.000`, 손잡이 아래축 정렬 `1.000`을 기록했다.
- 이란 지상군은 손뼈 회전과 AK 로컬 회전을 순서대로 합성해 수직 결착을 제거했다. 목표 방향 정렬 `1.000`, 손잡이 아래축 정렬 `0.997`로 측정됐다.
- `validate-marine-firing-posture.py`를 새로 추가해 양측 병사의 Meshy 모델·애니메이션 상태·어깨·양손·총열·수직축을 한 번에 검증하고 1920×1080 근접 화면을 남기게 했다.
- 시나리오 7종과 본편 미션 라우팅 7종·직접 진입 3종이 오류 0으로 통과했다. 시나리오 상한은 최대 116,514/120,000 삼각형, 61/90 드로콜이다.
- 스텔스 폭격기·GBU-57 회귀검사와 Google 3D 연합전력·전략 LOD·텍스처 회귀검사도 모두 통과했다. 대규모 전투는 70,573/120,000 삼각형, 44/90 드로콜로 측정됐다.
- 시각 비교용 중간 실패 캡처 5장은 검증 폴더에서 삭제하고 최종 통과 화면과 JSON만 남겼다.

## 2026-07-30 — KayKit 소총 사격 리타기팅·해병 손목 교정 v114 (검증 전 기록)

- 기존 로컬 `Shoot.fbx`가 소총 사격이 아니라 축구 킥 동작인 것을 확인해 사용하지 않았다. OpenGameArt에 CC0로 공개된 `KayKit Character Animations 1.2` 원본과 라이선스를 프로젝트에 동봉하고 `Shooting(2h)` 클립을 사격 타이밍·반동 참조로 사용했다.
- KayKit의 6본 단순 골격과 Meshy 해병의 전신 골격이 달라 직접 클립 복사가 불가능해, 참조 클립을 숨은 리그에서 재생하면서 Meshy 상완·전완에 양손 CCD IK를 적용하는 절차적 리타기팅으로 연결했다.
- 소총 결착점이 오른손 뼈의 약 0.017 축척을 상속해 개머리판·손잡이·총구가 어깨 주변 몇 mm 안으로 압축되던 근본 원인을 수정했다. 소총 제어 앵커를 축척 1인 시각 루트로 옮기고 0.86m 정규화 소총 소켓을 월드 좌표로 계산한다.
- 개머리판을 오른쪽 어깨에 먼저 고정한 뒤 방아쇠 손과 지지손을 각각 손잡이에 결착한다. 두 손의 로컬 바인드 회전을 매 프레임 복원해 해병 1의 손이 안쪽으로 말리던 변형을 제거했다.
- 비전투·이동 상태에서도 소총이 발밑에 남지 않도록 오른손의 월드 위치·회전을 축척 1 제어 앵커에 복제하는 저준비 자세 결착을 추가했다.
- 전투 데이터는 version 23, 전투 번들은 v114, 본편 전투 연결은 v114, 본편 번들 캐시는 v42로 올렸다. 정면·지지손 측면 1600×1000 확대 화면과 실제 소켓 길이·양손·손목·개머리판을 검사하는 검증 항목도 추가했다.

## 2026-07-30 — KayKit 소총 사격 리타기팅·해병 손목 교정 v114 최종 검증

- 해병 1 근접 사격에서 목표 방향·양손 방향 정렬은 각각 `1.000`, 방아쇠 손·지지손 결착 오차는 각각 `0.002m`, 개머리판–어깨 오차는 `0.024m`다. 오른손·왼손 손목 직선도는 `0.998`·`0.995`로 손이 안쪽으로 말리지 않는다.
- 이동 사격 중인 해병 4명도 방아쇠 손·지지손 결착 오차가 각각 `0.009~0.013m`, 개머리판–어깨 오차는 `0.022~0.024m`로 모두 통과했다. 네 명 모두 `ready → low-ready-walk → rifle-up-walk` 전환을 거쳤고 T포즈 시작은 0건이다.
- 무료 원본의 `Shooting(2h)` 클립 이름·출처·재생 시각이 런타임 스냅샷에서 확인됐고, 정면·지지손 측면 확대 화면에서도 M27 개머리판·방아쇠 손·앞손잡이 지지가 육안으로 확인됐다.
- Meshy 연합전력 검사 36항목, 스텔스 폭격기·GBU-57·해병 회귀검사 15항목, 소총 근접 자세 14항목이 전부 통과했다. 브라우저 콘솔·페이지·HTTP 오류는 0건이다.
- 시나리오 7종과 본편 미션 라우팅 7종·직접 진입 3종도 전부 통과했다. 최대 장면은 116,514/120,000 삼각형·61/90 드로콜이며 지상 유닛의 바다 배치는 0건이다.
- 런타임 모델 검사는 33종·최대 22,777 삼각형이다. 금지된 4만 삼각형대 `-web-v1.glb`는 런타임 경로에 없으며, 기존 항만 크레인 22,777 삼각형 경고 한 건은 이번 해병 변경과 무관하게 유지된다.

## 2026-07-30 — 이란 지상 전투원 전략 LOD 사격 자세 v115 (검증 전 기록)

- 사용자 재검수에 따라 이란 혁명수비대 지상 전투원을 확대 확인했다. 대규모 전투용 전략 LOD가 `Walk_Forward_While_Shooting` 원본 클립의 40% 프레임을 그대로 굽고 있어 양팔과 AK가 얼굴 위로 들린 잘못된 자세가 6명 모두에게 복제되는 원인을 확인했다.
- 이란군 전략 LOD도 해병과 동일하게 중립 보행 프레임을 먼저 적용하고, 개머리판–어깨·방아쇠 손–손잡이·지지손–앞손잡이 순서의 양팔 IK를 적용한 뒤 정적 메시로 굽도록 변경했다.
- 보행 주기 21개 지점을 검사해 양발 높이 차가 `0.003`인 10% 프레임을 이란군 고정 사격 자세의 하체 기준으로 선택했다. 기존 18% 프레임보다 과도한 보폭과 들린 다리를 줄였다.
- 이란군 전용 `qa=enemy-posture` 정면·측면 근접 검수 화면과 전략 LOD의 양손·개머리판 결착 수치를 추가했다.
- 전투 데이터는 version 24, 전투 번들은 v115, 본편 전투 연결은 v115, 본편 번들 캐시는 v43으로 올렸다.

## 2026-07-30 — 이란 지상 전투원 AK 조준축·결착 최종 교정 v115

- 전략 LOD를 굽는 순간에 실전 `visualRoot` 조준 보정각(-0.724rad)을 재현하고, 총기 월드 조준을 계산한 뒤 그 역회전을 로컬 총기 앵커에 저장했다. 런타임에서 인물 조준각이 다시 적용돼 AK가 몸을 대각선으로 가로지르던 이중 회전 원인을 제거했다.
- 인물 정점은 조준 보정각이 중복되지 않도록 전략 포즈 루트의 역행렬로 다시 로컬 공간에 굽는다. 전략 LOD는 대기·공격 상태 모두 동일한 사격 준비 방향을 유지한다.
- 이란 전투원 전용 정면·측면 근접 검증 화면과 손잡이·지지손·개머리판 결착 수치 검사를 추가했다.
- 최종 실측은 목표 방향 정렬 1.000, 총기 수직 정렬 1.000, 방아쇠손 오차 0.039m, 지지손 오차 0.039m, 개머리판-어깨 오차 0.022m다. 신규 이란 전투원 검사 6건과 런타임 오류 검사가 모두 통과했다.
- Meshy 합동전력 검사 36항목, 7개 RTS 시나리오, 본편 미션 라우팅 7종과 직접 진입 3종이 모두 통과했다. 대규모 전투는 70,573 삼각형·44 드로콜, 시나리오 최대치는 116,514/120,000 삼각형·61/90 드로콜이다.
- 런타임 GLB 33종 중 최대 모델은 22,777 삼각형이며, 금지된 4만대 `-web-v1.glb` 런타임 사용은 없다.

## 2026-07-30 — 연안 포대 이란 지상 경비대·해병 단독 돌격 밸런스 v116 (검증 전 기록)

- `coastal_battery`가 이동식 발사 차량 4기만 배치하고 이란 지상군은 전혀 배치하지 않아, 지상 유닛을 공격하지 않는 TEL의 현실적 표적 제한과 결합되면서 해병 2명이 무저항으로 포대를 제거할 수 있던 원인을 확인했다.
- 기존 Meshy 이란 혁명수비대 지상 전투원과 AK 모델을 재사용해 포대 경비 분대 2개를 TEL 4기 주변에 배치했다. 각 화면 유닛은 병사 1명이 아니라 경비 분대 1개를 대표하며, TEL은 대함·대공 임무를 유지하고 경비 분대가 접근하는 아군 해병을 직접 방어한다.
- 임무 목표 집계를 모든 적군이 아니라 `tel` 4기만 세도록 고정했다. 경비병을 먼저 제거해도 발사 차량을 파괴하지 않으면 승리하지 않는다.
- 대규모 전투에서 검증된 이란 지상군 전략 LOD를 연안 포대에도 적용해 신규 4명 때문에 삼각형 상한을 넘지 않도록 했다.
- 브리핑에 `이동식 발사 차량 4기 · 포대 경비 분대 2개`, 해병 단독 투입의 불리함, 공중·해상 지원 권고를 한·영으로 명시했다.
- 전투 데이터는 version 25, 전투 번들은 v116, 본편 전투 연결은 v116, 본편 번들 캐시는 v44로 올렸다.
- 실제 런타임에서 아군을 해병 2명으로만 제한하고 전투를 빠르게 진행해 패배 여부를 검사하는 `validate-coastal-battery-ground-defense.py`를 추가했다. 배치 수·TEL 전용 목표 집계·전략 LOD·지상 배치·런타임 오류도 함께 검증한다.
- 결합 전력 검증 중 전투기·스텔스 폭격기가 첫 타격 뒤 목표 바로 위에 머물며 재공격 거리를 만들지 못하는 문제를 확인했다. 공중 유닛이 최소 발사 거리 안으로 들어오면 측면 이탈 후 재진입하도록 공격 비행 로직을 보강했다.

## 2026-07-30 — 연안 포대 지상 방어·공중 재진입 v116 최종 검증

- 해병 2명 단독 돌격은 5.6초에 전멸했고 TEL 파괴는 0/4기였다. 경비 분대는 1개가 생존했으며, 경비 분대를 제거해도 목표 집계는 TEL만 세는 것을 확인했다.
- 기본 결합 전력(구축함 1·전투기 2·스텔스 폭격기 1·해병 2)은 공중 재진입을 반복해 44.1초에 TEL 4/4기를 파괴하고 승리했다.
- 연안 포대는 TEL 4기·Meshy 이란 경비 분대 2개가 모두 육지에 배치됐고, 전략 LOD 1개 배치가 활성화됐다. 콘솔·페이지·HTTP 오류는 0건이다.
- 7개 시나리오 성능 게이트는 전부 통과했다. 연안 포대는 118,187/120,000 삼각형·61/90 드로콜이며, 전체 최대 장면도 116,514/120,000 삼각형·61/90 드로콜 안이다.
- 본편 미션 라우팅 7종과 직접 진입 3종이 v116으로 통과했다. 미사일 회피·유조선 구출·유닛 충돌 방지 회귀검사도 통과했다.
- 런타임 GLB 33종의 최대는 기존 항만 크레인 22,777 삼각형이다. 금지된 4만 삼각형대 Meshy 원본은 런타임 경로에 없다.

## 2026-07-30 — 연안 포대 이란 경비 분대 걷기 애니메이션 v117 (검증 전 기록)

- 사용자 재검수에서 연안 포대 이란 경비 분대가 위치는 이동하지만 다리를 움직이지 않는 현상을 확인했다.
- 원인은 v116 성능 최적화에서 대규모 전투 전용 정적 사격 자세 LOD를 연안 포대의 경비 분대 2개에도 적용한 것이다. 해당 LOD는 스키닝과 AnimationMixer를 제거하므로 이동 애니메이션이 재생될 수 없었다.
- 연안 포대는 경비 분대가 2개뿐이므로 전략 LOD 대상에서 제외했다. 이제 Meshy 리깅 모델과 `Walk_Forward_While_Shooting` 애니메이션 클립을 직접 재생하며, 대규모 전투의 다수 이란군만 기존 정적 전략 LOD를 유지한다.
- 전투 번들은 v117, 본편 전투 연결은 v117, 본편 번들 캐시는 v45로 올렸다.
- 연안 포대 전용 검증에 경비 분대별 리깅·Mixer·걷기/전투 클립 존재 여부와 이동 중 상태, 이동 거리, 활성 클립 시간, 다리뼈 쿼터니언 변화량을 추가했다. 단순히 위치만 바뀌는 현상은 이제 검증을 통과할 수 없다.

## 2026-07-30 — 연안 포대 이란 경비 분대 걷기 애니메이션 v117 최종 검증

- 경비 분대 2개 모두 Meshy 리깅 모델, 독립 AnimationMixer, `walking_man` 걷기 클립과 `Walk_Forward_While_Shooting_inplace` 이동사격 클립이 연결됐다. 정적 전략 LOD 배치는 0개다.
- 1.05초 이동 구간에서 두 분대가 각각 2.886m·1.973m 이동했고, 활성 걷기 클립 시간은 0.121초·0.063초, 왼쪽 허벅지뼈 회전 변화는 0.4812rad·0.4443rad로 측정됐다. 두 분대 모두 `rifle-up-walk` 상태를 실제로 거쳤다.
- 해병 2명 단독 패배와 기본 결합 전력 승리 밸런스는 그대로 통과했다. 콘솔·페이지·HTTP 오류는 0건이다.
- 7개 시나리오 성능 게이트는 전부 통과했다. 애니메이션 원본을 켠 연안 포대는 118,187/120,000 삼각형·62/90 드로콜이다.
- 본편 미션 라우팅 7종과 직접 진입 3종이 v117로 통과했다. 런타임 모델 규율 검사도 통과했으며 기존 항만 크레인 22,777 삼각형 경고만 유지된다.

## 2026-07-31 — 임무별 최소 여유 추천 편성 v118 (검증 전 기록)

- 전력 선택 기본값이 “안전하게 이길 수 있는 수준”을 넘어 항공모함·스텔스 폭격기·보조 전력을 거의 최대치로 포함하던 문제를 수정했다.
- 추천 기준을 `임무 핵심 역할 + 작은 안전 여유`로 바꾸고, 사용자가 불안할 때 필요한 전력만 추가하도록 한·영 안내 문구를 바꿨다.
- 호송 방어는 구축함 1·전투기 2·헬기 1(1,410 → 465), 유조선 구출은 구축함 1·전투기 1·헬기 1(425 → 380), 기뢰 제거는 헬기 1·무인수상정 2(385 → 145), 공중 방어는 구축함 1·전투기 2(1,365 → 410)로 낮췄다.
- 연안 포대는 전투기 2·스텔스 폭격기 1·지상병력 2(770 → 530), 대규모 전투는 구축함 2·전투기 3·헬기 2·항공모함 1·무인수상정 1·지상병력 2(2,460 → 1,830)로 낮췄다.
- 곡괭이산은 GBU-57 운반에 필요한 B-2 1대가 임무 필수이므로 320을 그대로 유지했다.
- 검증 도구에 임무별 추천 예산·기존 대비 절감률·최소 30% 예산 여유 검사를 추가했다. 전투 데이터는 version 26, 전투 번들은 v118, 본편 전투 연결은 v118, 본편 번들 캐시는 v46으로 올렸다.

## 2026-07-31 — 임무별 최소 여유 추천 편성 v118 최종 검증

- 7개 임무 모두 새 기본 편성·0대 제외·한글 안내·승인 예산 30% 이상 여유 조건을 통과했다. 기존 대비 절감률은 호송 67.0%, 구출 10.6%, 기뢰 62.3%, 공중 방어 70.0%, 연안 포대 31.2%, 대규모 전투 25.6%다.
- 적 미사일 수동 회피, 유조선 구출, 유닛끼리 관통하지 않는 이동 회귀검사도 모두 통과했고 콘솔·페이지·HTTP 오류는 0건이다.
- 연안 포대의 새 기본 편성(전투기 2·스텔스 폭격기 1·지상병력 2)은 44.1초에 TEL 4기를 파괴해 승리했다. 지상병력 2명 단독 편성은 8.6초에 전멸해 과잉 방어력 없이 결합 전력의 필요성이 유지된다.
- 7개 전장 성능 검사는 최대 110,719/120,000 삼각형·56/90 드로콜로 통과했고, 지상 유닛의 바다 배치는 0건이다.
- 본편 카드→임무→RTS 시나리오 연결 7종과 대규모·구출·곡괭이산 직접 진입이 iframe v118로 통과했다.
- 런타임 GLB 33종 중 최대는 기존 항만 크레인 22,777 삼각형이며, 금지된 4만 삼각형대 Meshy 원본은 런타임 경로에 없다.
- `validate-recommended-force-balance.py`를 추가해 일반 교전 4종을 새 추천 편성 그대로 빠른 교전시켰다. 호송 27.0초(8/8), 기뢰 34.8초(7/7), 공중 방어 37.8초(8/8), 대규모 전투 165.6초(33/33)에 모두 승리했다.
- 변경하지 않은 곡괭이산 B-2 1대도 GBU-57 2발로 진입부 2곳을 실제 봉쇄하고 세 번째 발사는 거부되는 것을 v118에서 재검증했다. 이번 추천 편성 검증에서는 외부 Google 3D 재검사를 생략했고, 로컬 전투·Meshy B-2·목표 판정·런타임 오류만 검사했다.

## 2026-07-31 — 해병 A 공격 명령·휠 확대 제한·연안 경비 증원 v119 (검증 전 기록)

- 해병을 선택하고 A를 누른 뒤 적을 좌클릭하면 `적 정보 보기`가 공격 이동보다 먼저 실행되어 아군 선택이 해제되는 원인을 확인했다. A 명령 상태에서는 적 좌클릭을 즉시 집중 공격으로 우선 처리하고, 땅 좌클릭은 기존 공격 이동으로 유지했다.
- 지상병력이 공격 이동 중 해상·공중 표적을 가까운 적으로 잘못 고르지 않도록 지상 유닛의 자동 표적을 지상 표적으로 제한했다.
- 마우스 휠 확대는 시나리오 전체 보기 카메라 높이·거리의 50%까지만 접근할 수 있게 제한했다. F키 전술 카메라는 별도 기능으로 유지한다.
- 연안 포대의 이란 경비 분대를 2개에서 4개로 늘리고 TEL 4기 주변에 분산 배치했다. 브리핑의 한·영 위협 수치도 경비 분대 4개로 맞췄다.
- 전투 데이터는 version 27, 전투 번들은 v119, 본편 전투 연결은 v119, 본편 번들 캐시는 v47로 올렸다.

## 2026-07-31 — 해병 A 공격 명령·휠 확대 제한·연안 경비 증원 v119 최종 검증

- 실제 키보드·마우스 입력으로 해병 1명을 선택하고 `A → 이란 경비 분대 좌클릭`을 실행했다. 해병 선택은 유지되고 적 정보 보기는 열리지 않았으며 `attack:coastal-guard-1` 직접 공격 명령이 들어갔다.
- 해병의 지상 표적 공격은 허용되고 해상·공중 표적은 거부됐다. 공격 이동 자동 탐색도 같은 표적 제한을 사용한다.
- 연안 전장 전체 보기 48/42에서 휠을 최대한 확대해도 카메라 높이·거리가 24/21에서 멈춰 정확히 50% 제한을 통과했다.
- 이란 포대 경비 분대 4개와 TEL 4기가 모두 육지에 배치됐다. 경비 4개 모두 Meshy 리깅·걷기·이동 사격 애니메이션이 작동했다.
- 해병 2명 단독 돌격은 6.0초에 전멸했고 이란 경비 4개·TEL 4기는 전부 생존했다. 기본 결합 전력은 44.1초에 TEL 4기를 파괴해 임무 승리 가능성을 유지했다.
- 시나리오 7종 성능 게이트를 통과했다. 가장 무거운 연안 포대는 114,984/120,000 삼각형·60/90 드로콜이며, 지상 유닛의 바다 배치와 브라우저 오류는 0건이다.
- 본편 임무 라우팅 7종과 대규모·구출·곡괭이산 직접 진입이 iframe v119로 통과했다. 일반 추천 편성 4종의 승리 회귀검사와 런타임 GLB 규율 검사도 통과했다.

## 2026-07-31 — 승리 후 3초 전장 감상·이동 시간 v120 (검증 전 기록)

- 결과 팝업은 3초 늦게 표시되지만 승리 판정 즉시 `paused=true`가 적용돼 유닛·명령·오디오가 모두 멈추던 원인을 확인했다.
- 승리 또는 패배 확정 후 결과 팝업 전 3초를 별도 전장 감상 시간으로 분리했다. 이 시간에는 카메라와 유닛 애니메이션, 기존 발사체와 이동 효과가 1배속으로 계속 움직인다.
- 3초 동안 아군 선택과 우클릭 이동, A 공격 이동, 위치 방어·정지·이탈 명령을 계속 사용할 수 있다. 새 사격은 중단해 판정이 끝난 뒤 추가 공격이 발생하지 않도록 했다.
- 전투 시간과 남은 시간은 승리 판정 순간 값으로 고정하고, 별도 `postBattleElapsed`만 증가한다. 결과 팝업이 나타나는 시점에 완전히 일시정지하고 이동·엔진 오디오를 종료한다.
- 전투 데이터는 version 28, 전투 번들은 v120, 본편 전투 연결은 v120, 본편 번들 캐시는 v48로 올렸다.

## 2026-07-31 — 승리 후 3초 전장 감상·이동 시간 v120 최종 검증

- 실제 브라우저 입력으로 승리 확정 직후 선택한 전투기에 우클릭 이동 명령을 내렸다. 결과 창이 닫힌 감상 구간에서 명령이 접수됐고 전투기는 0.85초 동안 3.166m 이동했다.
- 승리 순간 전투 경과 시간과 남은 시간은 각각 0초·240초로 고정됐으며, 별도 감상 시간은 3.04초까지 진행됐다. 이 구간에는 명령 버튼과 카메라 조작을 계속 사용할 수 있다.
- 성능이 낮은 장치에서도 결과 창이 먼저 떠서 움직임을 잘라 먹지 않도록 `3초 실제 대기 + 3초 전장 시뮬레이션`을 모두 충족한 뒤 결과를 표시한다. 정상 프레임에서는 약 3초이며, 프레임이 매우 낮으면 짧아지는 대신 조금 더 길어진다.
- 결과 창이 표시된 뒤에는 전장이 일시정지되고 0.5초 추가 관찰 동안 유닛 이동량이 0m였다. 새 공격은 승리 판정 순간부터 차단하고 기존 발사체·이동·애니메이션만 감상 구간 동안 유지한다.
- 전용 검증의 런타임·입력·이동·시간 고정·결과 지연·최종 정지·오류 7개 항목이 모두 통과했고 콘솔·페이지·HTTP 오류는 0건이다.
- 7개 시나리오 성능 게이트와 본편 임무 라우팅 7종·직접 진입 3종이 모두 v120으로 통과했다. 최대 전장은 연안 포대 114,984/120,000 삼각형·60/90 드로콜이며 지상 유닛의 바다 배치는 0건이다.
- 런타임 GLB 33종의 최대는 기존 항만 크레인 22,777 삼각형이다. 4만 삼각형대 Meshy 원본은 런타임 경로에 없으며 기존 크레인 감축 권고만 유지된다.

## 2026-07-31 — 본편 상황실 Google 3D 전술 결합 배경 v121 (검증 전 기록)

- 본편 상황실의 `실제 지도 보기`가 로컬 Three.js 전술 장면을 완전히 끄고 Google 지도만 별도 표시해 전술 유닛·항로가 사라지는 구조를 확인했다. Google 카메라도 로컬 카메라와 분리돼 지도를 조작한 뒤 엉뚱한 바다만 남을 수 있었다.
- Google 3D HYBRID 실제 지형을 배경 레이어로 두고, Meshy 함정·유조선·항공기·무인수상정·위협 표식·IMO 항로를 투명 Three.js 전술 레이어로 위에 계속 렌더링하도록 결합했다.
- 기존 전술 3D 화면의 드래그·휠·자동 카메라를 단일 조작 기준으로 유지하고, 중심 좌표·거리·기울기·방향·시야각을 Google 지도 카메라에 실시간 동기화한다. Google 지도 자체 입력은 막아 두 카메라가 다시 어긋나지 않도록 했다.
- Google 모드에서는 로컬 하늘·바다·육지·산악 메시만 숨기고 전술 자산과 항로는 유지한다. API 또는 지도 로딩 실패 시 기존 로컬 전술 지형으로 자동 복귀한다.
- 본편 런타임은 v121, 게임 CSS 캐시는 v38, 본편 번들 캐시는 v49로 올렸다.

## 2026-07-31 — 본편 상황실 Google 3D 전술 결합 배경 v121 최종 검증

- 사용자가 지정한 본편 상황실 상태에서 Google 3D HYBRID 실제 지형을 기본 배경으로 표시하고, 국가안보회의 3개 선택지·상단 국정 지표·하단 속보 UI를 함께 촬영해 시각 검수했다.
- Google 지도 중심은 55.50000, 27.00397, 초기 범위는 약 1,266km로 걸프 전역과 호르무즈 해협을 함께 보여준다. Google 지명 중복을 줄이기 위해 별도 게임 지명표는 `호르무즈 해협` 하나만 유지했다.
- 로컬 하늘·바다·육지·산악 6개 객체는 Google 모드에서 전부 숨겨졌고, 투명 WebGL 전술 레이어·Meshy 유닛 그룹·IMO 항로 그룹·유조선 3척은 계속 표시됐다.
- 마우스 드래그 검증에서 Three.js 전술 카메라가 `[0,80,124]`에서 `[-129.73,53.39,48.43]`으로 이동할 때 Google 지도 방향도 0도에서 68.75도로 함께 변해 두 레이어의 동기화를 확인했다.
- 본편 장면은 93,088/120,000 삼각형·29/90 드로콜로 성능 상한을 통과했다. Meshy 구축함·유조선·전투기·무인수상정의 기본색·노멀 텍스처와 HTTP 200 응답도 유지됐다.
- 전용 검증 15개 항목, 전투 시나리오 7종, 본편 미션 라우팅 7종과 직접 진입 3종이 모두 통과했다. 콘솔·페이지·HTTP 오류와 지상 유닛의 바다 배치는 0건이다.
- 런타임 GLB는 32종이며 최대는 기존 항만 크레인 22,777 삼각형이다. 금지된 4만 삼각형대 Meshy 원본은 런타임 경로에 없다.

## 2026-07-31 — Google 3D 지도·전술 객체 프레임 동기화 v122 (검증 전 기록)

- 전술 객체와 Three.js 카메라는 매 프레임 움직이지만 Google 지도 카메라는 90ms 간격으로 제한되어 지형이 뒤늦게 따라오던 원인을 확인했다.
- Google 카메라 갱신 제한을 90ms에서 16ms로 줄이고, 중심·거리·기울기·방향·시야각 변화 감지 임계값도 프레임 단위 이동을 잡도록 낮췄다.
- 마우스 드래그 중에는 Three.js 카메라 위치를 갱신한 같은 입력 이벤트에서 Google 카메라를 즉시 강제 동기화하도록 연결했다.
- 매 갱신마다 불필요하게 카메라 애니메이션 정지를 호출하던 코드를 제거해 지도 렌더러의 추가 지연 가능성을 없앴다.
- 본편 런타임은 v122, 번들 캐시는 v50으로 올렸으며 실제 드래그 중 동기화 횟수를 자동 검증하도록 검사기를 보강했다.

## 2026-07-31 — Google 3D 지도·전술 객체 프레임 동기화 v122 최종 검증

- Google 3D 결합 검사 16개 항목이 전부 통과했다. 동기화 모드는 `frame`, 제한값은 16ms이며 자동 드래그 8단계에서 카메라 동기화가 9회 발생했다.
- 자동 검사에서 Google 지도 방향은 0도에서 68.75도로 변했고 Three.js 전술 카메라도 함께 이동해 지도와 객체의 결합 좌표가 유지됐다.
- 실제 Chrome 게임 탭에서도 드래그 한 번에 동기화가 11회 추가되고 지도 방향이 0도에서 80.21도로 변하는 동안 함선·항로·표식이 지형과 함께 회전하는 것을 확인했다.
- 본편 결합 화면은 콘솔·페이지·HTTP 오류 없이 통과했으며 검사 시점 렌더 부하는 15,274/120,000 삼각형, 21/90 드로콜이었다.
- 전투 시나리오 7종, 본편 미션 라우팅 7종과 직접 진입 3종이 모두 통과했다. 지상 유닛 바다 배치와 런타임 오류는 0건이다.
- 런타임 모델은 32종이며 최대 22,777 삼각형이다. 금지된 4만 삼각형대 Meshy 원본은 런타임 경로에 없다.

## 2026-07-31 — 본편 항공모함 안전 수역 재배치·지도 전환 버튼 제거 v123 (검증 전 기록)

- 본편 항공모함 중심이 동경 55.18도·북위 25.83도의 UAE 연안에 놓여 있었고, 확대된 선체가 약 1도 이상을 차지하면서 육지에 걸치던 원인을 확인했다.
- 항공모함을 페르시아만 중앙의 넓은 수역인 동경 53.65도·북위 26.25도로 옮기고, 선체를 해안선과 거의 평행하게 보이도록 회전값을 -0.18 라디안으로 바로잡았다.
- 본편 배치 객체에 모델 키와 실제 경위도 기준점을 기록해 항공모함이 지정 안전 수역을 벗어나거나 육지를 침범하는지 자동 검증할 수 있게 했다.
- 사용자 요청에 따라 우측 `로컬 3D 지형` 전환 버튼과 전용 CSS를 제거했다. Google 지도 로딩 실패 시 로컬 지형으로 자동 복구하는 내부 폴백은 유지한다.
- 본편 런타임은 v123, 게임 CSS 캐시는 v39, 번들 캐시는 v51로 올렸다.

## 2026-07-31 — 본편 항공모함 안전 수역 재배치·지도 전환 버튼 제거 v123 최종 검증

- 실제 Google 3D 상황실에서 항공모함이 페르시아만 중앙 수역에 놓이고 확대된 선체 전체가 육지와 겹치지 않는 것을 시각 검수했다.
- Natural Earth 해안선과 항공모함 실제 모델 표면 780지점을 비교한 결과 육지 침범은 0지점이었다. 선체 범위는 동경 53.09557~54.21212도, 북위 26.06770~26.40933도다.
- 화면과 DOM에서 `로컬 3D 지형` 버튼이 모두 제거됐고, Google 3D 실제 지형은 기본 상태로 정상 연결됐다. 자동 로컬 폴백 로직은 유지됐다.
- 본편 결합 검사 18개 항목, 전투 시나리오 7종, 미션 라우팅 7종과 직접 진입 3종이 모두 통과했다.
- 전투 시나리오의 콘솔·페이지·HTTP 오류와 지상 유닛 바다 배치는 0건이다. 최대 전장은 연안 포대 114,984/120,000 삼각형, 60/90 드로콜로 성능 상한 안이다.
- 런타임 모델 32종 중 최대는 기존 항만 크레인 22,777 삼각형이며 4만 삼각형대 Meshy 원본은 런타임 경로에 없다.

## 2026-07-31 — 새로고침 Google 3D 전용 로딩 게이트 v124 (검증 전 기록)

- 본편 모델 로딩 화면은 먼저 닫히고 Google 3D 지도는 뒤늦게 준비돼, 새로고침 직후 로컬 3D 지형이 잠시 노출된 다음 Google 지형으로 바뀌던 원인을 확인했다.
- Google 지도를 기본 사용하는 경우 초기 상태를 `google-loading`으로 고정하고, 로컬 지형 객체·배경·캔버스를 처음부터 숨기도록 했다.
- Google 지도가 준비되면 먼저 Google 모드를 활성화해 로컬 지형이 숨겨진 상태를 확정한 뒤 대기 상태와 로딩 화면을 제거한다. 따라서 전환 중 로컬 지형 프레임이 노출되지 않는다.
- Google 연결 실패 때만 대기 상태를 제거하고 로컬 지형으로 자동 복구하는 기존 폴백 동작은 유지한다.
- Google 전용 대기 화면을 전체 배경으로 확장해 지도 준비 중임을 명확히 보이게 했다.
- 본편 런타임은 v124, 게임 CSS 캐시는 v40, 번들 캐시는 v52로 올렸다.

## 2026-07-31 — 통항률 연동 선박 밀도·축척 조정 v124 (검증 전 기록)

- 본편 통항 선박이 통항률과 무관하게 고정 3척으로 표시되던 구조를 0~6척 가변 풀로 바꿨다.
- 현재 통항률을 0~100% 범위로 보정한 뒤 6척 기준 비율로 환산한다. 0%는 0척, 30%는 2척, 50%는 3척, 75%는 5척, 100%는 6척이 표시된다.
- 통항률 구간에 따라 척수가 바뀔 때 활성 선박을 전체 항로에 균등하게 다시 배치해 서로 겹치지 않게 했다.
- 선박 모델 배율은 기존 0.52에서 0.26으로 정확히 절반 축소했다.
- 본편 상태 변경 함수를 전장과 연결해 정책·전투·의회 결과로 통항률이 바뀌면 표시 선박 수도 즉시 갱신되도록 했다.

## 2026-07-31 — v124 1차 검증 실패 보정

- 1차 자동 검사에서 통항률별 계산 결과 0·2·3·5·6척은 맞았지만, 상태 계산이 모델 로딩보다 먼저 끝난 경우 나중에 생성된 선박 6척이 모두 비활성으로 남는 비동기 순서 문제를 발견했다.
- 목표 척수뿐 아니라 실제 표시 중인 척수도 비교해 불일치하면 반드시 가시성과 항로 위치를 다시 적용하도록 보정했다.
- Google 대기 클래스를 적용할 때 기존 0.45~0.55초 투명도 전환이 남아 초기 프레임에서 로컬 캔버스가 보이는 것을 확인했다. 대기 상태에서는 전환을 끄고 캔버스를 즉시 강제 비표시하도록 수정했다.

## 2026-07-31 — 항로 표시 저채도·저투명 처리 v125 (검증 전 기록)

- 통항 선박을 절반 크기로 줄인 뒤 기존 항로 중심선 78%·차선 70% 불투명도와 불투명 화살표가 선체를 덮어 유조선 식별을 방해하는 문제를 확인했다.
- 항로 중심선은 24%, 양쪽 차선은 14%, 방향 화살표는 16% 불투명도로 낮췄다.
- 항로 선과 화살표의 깊이 쓰기를 끄고 렌더 우선순위를 뒤로 보냈으며, 높이를 유조선 선체 아래로 내려 선박 위를 가로지르지 않게 했다.
- 본편 런타임은 v125, 번들 캐시는 v53으로 올렸다.

## 2026-07-31 — Google 초기 게이트·통항 선박·투명 항로 v125 최종 검증

- 실제 브라우저 새로고침 직후 Google 지도가 준비되기 전에는 로컬 지형과 Three.js 캔버스를 모두 숨겼다. 준비 상태가 되면 Google 3D 지도를 먼저 활성화한 뒤 로딩 화면을 닫으므로 로컬 지형이 잠깐 보이는 현상이 재현되지 않았다.
- 현재 통항률에 따라 유조선이 0·2·3·5·6척으로 변하고, 통항 선박 크기는 기존 0.52에서 0.26으로 정확히 절반 축소되는 것을 자동 검증했다.
- 항로 중앙선·차선·방향 표시 투명도는 각각 24%·14%·16%이며, 깊이 기록을 끄고 선박보다 뒤에 그려 유조선을 가리지 않는 것을 화면과 런타임 값으로 확인했다.
- 본편 검증 21개 항목을 모두 통과했다. 화면 삼각형은 89,483/120,000, 드로콜은 28/90이며 콘솔·페이지·HTTP 오류는 0건이다.
- 전투 시나리오 7종과 미션 라우팅 7종, 직접 진입 3종을 모두 통과했다. 최대 전투 장면은 연안 미사일 포대 114,984/120,000 삼각형·60/90 드로콜이며 지상 유닛의 바다 배치는 0건이다.
- 런타임 GLB 32종 중 최대 모델은 항만 크레인 22,777 삼각형이다. 금지된 4만 삼각형대 Meshy 원본은 런타임 경로에 없다.

## 2026-07-31 — 카사브 해안 이격 항로·모바일 자동전투 v126/v121 (검증 전 기록)

- 본편 유조선 항로가 카사브·무산담 해안과 최단 약 6.7km까지 가까워지는 구간을 확인했다. IMO 정본 좌표는 보존하고 게임 화면용 안전 통항 좌표를 데이터에 추가해 해안 최단 이격 목표를 10km 이상으로 높였다.
- 유조선 이동 경로와 중앙 항로선·차선·방향 표시가 동일한 안전 통항 좌표를 사용하도록 연결했다. 본편 런타임은 v126, 데이터 캐시는 v33, 번들 캐시는 v54로 올렸다.
- 모바일 전투 조작 보조를 위해 자동전투 켜기·끄기 버튼과 아군 역할별 자동 표적 배분·이동·구조 헬기 진입·유도탄 회피 로직을 추가하는 작업에 착수했다. 전투 런타임 목표 버전은 v121이다.

## 2026-07-31 — 카사브 해안 이격 항로·모바일 자동전투 v126/v121 최종 검증

- Natural Earth 해안선을 기준으로 본편 유조선 항로의 카사브·무산담 인접 최단 이격을 약 6.7km에서 10.2km로 늘렸다. 정본 IMO 좌표는 그대로 두고 화면용 안전 통항 좌표를 별도 데이터로 관리하며, 유조선·항로선·차선·방향 표시가 모두 같은 좌표를 사용한다.
- 본편 자동 검증 22개 항목을 모두 통과했다. 통항률 30%에서 유조선 2척, 축척 0.26, 항로 이격 10.2km, Google 3D 준비 완료, 콘솔·페이지·HTTP 오류 0건이다.
- 전투 명령 바에 `자동전투` 버튼을 추가했다. 켜면 모든 아군이 병과별 우선 표적을 분산 선택해 이동·공격하고, 적 유도탄 근접 시 측면 회피한다. 나포 유조선 구출 작전에서는 적 차단 뒤 헬기가 구조 원으로 자동 진입한다.
- 자동전투 중 버튼은 녹색과 `켜짐` 접근성 상태로 표시한다. 다시 누르거나 직접 이동·공격·방어·정지·이탈 명령을 내리면 수동 지휘로 복귀한다. 키보드 B키도 같은 기능을 수행한다.
- 모바일 430×932에서는 명령 바를 4열 2행으로 정리하고 자동전투 버튼을 2칸 너비로 배치했다. 글자 음절이 낱개로 갈라지지 않도록 줄바꿈도 보정했다.
- 자동전투를 실제로 켠 상태에서 전투 시나리오 7종을 모두 통과했다. 최대 장면은 연안 미사일 포대 114,984/120,000 삼각형·60/90 드로콜이며, 모바일 호송대 장면은 31,372/120,000 삼각형·37/90 드로콜이다. 콘솔 오류와 바다 유닛 육지 배치는 모두 0건이다.
- 본편 미션 진입 iframe도 전투 런타임 v121을 사용하도록 통일했다. 카드→미션→시나리오 7종과 직접 진입 3종이 모두 통과했고, 런타임 GLB 32종 중 최대는 기존 항만 크레인 22,777 삼각형이다.
- 최종 캐시는 본편 번들 v54·데이터 v33, 전투 번들 v121·CSS v57·전투 데이터 v30이다.

## 2026-07-31 — 해협 중앙 통항로·검은 초기 로딩 게이트 v127 (검증 전 기록)

- 사용자 지정 Google 3D 상황실 주소에서 통항 선박이 여전히 카사브·무산담 쪽에 바짝 붙어 보이는 문제를 다시 확인했다.
- 게임용 통항 경로를 북쪽으로 크게 옮겨 무산담과 이란 섬 사이의 해협 중앙 수역을 지나도록 9개 기준 좌표를 전면 조정했다. Natural Earth 해안선 정점 기준 경로 최단 이격 예상값은 약 17.6km다.
- 초기 진입 때 제목 화면·HUD·Google 지도 대기 배경이 겹쳐 보이지 않도록 HTML 최초 상태부터 검은 전용 로딩 게이트를 켰다.
- 로딩 게이트는 검은 배경, HORMUZ 표식, 회전 표시, 이동 진행 막대만 표시한다. Google 3D 지도가 준비되거나 로컬 폴백이 확정될 때까지 닫히지 않는다.
- 본편 런타임은 v127, 데이터 캐시는 v34, 게임 CSS 캐시는 v41, 번들 캐시는 v55로 올렸다.

## 2026-07-31 — 해협 중앙 통항로·검은 초기 로딩 게이트 v127 최종 검증

- Natural Earth 해안선 기준 화면용 통항 경로 최단 이격은 로컬·Google 3D 양쪽 모두 17.6km로 측정됐다. 기존 v126의 10.2km보다 7.4km 늘었으며, 유조선과 항로 표시는 같은 중앙 좌표열을 사용한다.
- Google 지도 준비 중 화면은 배경 `rgb(0, 0, 0)`, 불투명도 1, 표시 계층 300으로 확인했다. HORMUZ 표식과 이동 진행 막대만 노출되고 지도·HUD·제목 화면의 중간 상태는 검은 게이트 아래에 가려진다.
- Google 준비 완료 뒤에는 본편 런타임 v127과 Google 3D 전술 결합 화면이 정상 표시됐다. 콘솔·페이지·HTTP 오류는 모두 0건이다.
- 본편 검증 22개 항목을 전부 통과했다. 장면 부하는 89,483/120,000 삼각형, 28/90 드로콜이다.
- 미션 라우팅 7종과 직접 진입 3종도 모두 통과해 중앙 항로 및 초기 로딩 변경이 캠페인·전투 진입을 깨뜨리지 않았음을 확인했다.

## 2026-07-31 — 병과별 스마트 자동전투 v122 (검증 전 기록)

- 자동전투 중 전투기와 스텔스 폭격기가 표적 머리 위에서 선회하면서 발사 정렬에 실패하는 원인을 확인했다. 기존 로직은 너무 가까우면 매 프레임 새 이탈점을 만들고, 조금 멀어지면 즉시 표적으로 다시 꺾어 명확한 공격 경로를 만들지 못했다.
- 전투기·스텔스 폭격기에 `재배치 → 정면 진입 → 유효 거리 무장 투발 → 원거리 이탈 → 무장 준비 후 재진입` 공격 편대 상태 기계를 추가했다.
- GBU-57 투하 임무는 투하 가능 거리대를 별도로 사용하며, B-2는 한 발 투하 후 안전 거리까지 이탈했다가 두 번째 표적을 향해 재진입한다. 무장을 모두 사용하면 표적 머리 위로 돌아오지 않고 안전 수역에서 대기한다.
- 헬기는 미사일 유효 거리대를 유지하며 기수를 표적으로 돌린 뒤 사격하고, 너무 가까우면 측방 이탈한다. 구축함·항공모함·무인수상정도 자동전투 중 과도하게 밀착하지 않도록 병과별 거리 비율을 적용했다.
- 같은 표적에 필요 이상의 아군이 몰리지 않도록 이미 배정된 공격기 수와 예상 타격 횟수를 표적 우선순위에 반영했다.
- 자동검증 스냅샷에 진입·발사·이탈·재진입·실패한 공격 경로 수와 현재 공격기별 단계를 기록하도록 보강했다.
- 전투 런타임과 번들 캐시는 v122, 전투 데이터 캐시는 v31, 본편 번들 캐시는 v56으로 올렸다.

## 2026-07-31 — 병과별 스마트 자동전투 v122 최종 검증

- 전투기와 스텔스 폭격기는 이제 표적 위를 계속 선회하지 않는다. 재배치 → 진입 → 유효 사거리·기수 정렬 확인 → 발사 → 고정 안전지점 이탈 → 무장 대기 → 재진입의 병과별 공격 항로를 반복한다.
- 전투기는 편대별 표적 분산과 예상 명중 수를 반영해 과잉 집중을 줄였고, 스텔스 폭격기는 곡괭이산 임무에서 지하시설 진입부를 TEL보다 우선한다. GBU-57 두 발로 진입부 두 곳을 정확히 타격한 뒤 안전 이탈하며 임무가 성공하는 것을 확인했다.
- 헬기는 유효 사거리 밖에서 접근하고 너무 가까우면 측면 이탈한 뒤 기수를 돌려 재공격한다. 항공모함·구축함·무인수상정도 병과별 적정 교전거리를 유지하도록 보강했다.
- 자동전투 검증 스냅샷에 진입·발사·이탈·재진입·공격 중단·헬기 거리조절 횟수와 현재 항공기 공격 단계를 기록하도록 추가했다.
- RTS 시나리오 7종 전체 통과: 호송 방어, 유조선 구출, 기뢰 회랑, 미사일 방어, 연안 포대, 곡괭이산, 함대 전투. 콘솔 오류 0건, 페이지 오류 0건, HTTP 오류 0건, 해상 유닛 육지 배치 0건이다.
- 자동 항공 공격 확인 수치: 호송 방어 발사 1·이탈 6·재진입 4, 유조선 구출 발사 1·이탈 3·재진입 2, 미사일 방어 발사 2·이탈 6·재진입 2, 연안 포대 발사 3·이탈 7·재진입 4, 곡괭이산 발사 2·이탈 3·재진입 1, 함대 전투 발사 2·이탈 8·재진입 5.
- 본편 임무 라우팅 7종과 직접 진입 3종도 모두 v122로 연결됨을 확인했다. 콘솔·페이지·HTTP 오류는 모두 0건이다.

## 2026-07-31 — 시작 화면 즉시 표시·백그라운드 로딩 v128 (검증 전 기록)

- 기본 `index.html`에서 검은 전장 로딩 화면이 제목 화면을 가리던 구조를 제거했다. 제목 배경·문구·버튼 골격은 HTML과 CSS만으로 즉시 표시한다.
- 1.6MB 본편 번들은 브라우저가 제목 화면을 두 프레임 먼저 그린 뒤 비동기로 내려받고 실행하도록 변경했다.
- 데이터 JSON 10종, 로컬 3D 월드, Google 3D 지도는 제목 화면 뒤에서 준비한다. 특히 Google 지도 초기화 완료를 더 이상 시작 메뉴와 첫 선택의 선행 조건으로 기다리지 않는다.
- 번들이 준비되기 전에는 시작 버튼을 잠시 비활성화하고 현재 준비 상태를 한글로 표시한다. 이벤트 연결 직후 버튼을 활성화하며, 사용자가 선택하면 중복 입력을 막고 작전 준비 상태를 표시한다.
- 자동 시작·직접 임무 검사 주소에서는 기존 검은 로딩 화면을 유지해 중간 3D 화면이 노출되지 않도록 분기했다.
- 본편 런타임은 v128, 본편 번들 캐시는 v57, 게임 CSS 캐시는 v42로 올렸다.

## 2026-07-31 — 시작 화면 즉시 표시·백그라운드 로딩 v128 최종 검증

- 실제 브라우저 신규 접속 직후 제목 화면 표시, 검은 로딩 화면 숨김, 시작 버튼 준비 중 상태를 확인했다. 이 시점에는 아직 본편 런타임 값이 없어 무거운 번들이 제목 화면을 막지 않았음을 확인했다.
- 2초 뒤 본편 런타임 v128과 시작 버튼이 준비된 상태에서도 Google 지도 상태는 `loading`이어서, 지도 초기화가 제목 화면 뒤에서 독립적으로 진행되는 것을 확인했다.
- Google 지도 준비 전 `새 작전 시작`을 눌러도 제목 화면이 닫히고 역사 선택 화면이 즉시 채워졌다. 브라우저 오류와 치명 오류는 0건이다.
- 미션 라우팅 7종과 직접 임무 진입 3종을 재검사해 모두 통과했다. 콘솔·페이지·HTTP 오류는 모두 0건이다.

## 2026-07-31 — sidak.kr 외부 테스트 배포 v128

- 공개 주소 `https://sidak.kr/autodev/GameCreator/hormuz/`에 본편 v128과 RTS v122를 SFTP로 배포했다. sidak 서버의 SFTP 절대경로는 `/GameCreator/hormuz`다.
- 런타임 참조를 재귀 분석해 117개 파일, 55,404,392바이트만 게시했다. `.internal`, Meshy 원본·참고 이미지, `docs`, `output`, `tools`, 시안·지도 테스트 HTML은 공개 대상에서 제외했다.
- IIS 정적 파일 제공을 위해 프로젝트 전용 `web.config`를 추가했다. GLB는 `model/gltf-binary`, WebP는 `image/webp`, JSON은 UTF-8 JSON으로 응답한다.
- 외부 HTTPS에서 메인 HTML, 본편 번들, 전투 JSON, 타이틀 WebP, 항공모함 GLB가 모두 HTTP 200으로 응답하는 것을 확인했다.
- 실제 외부 브라우저에서 시작 화면 즉시 표시, 검은 로딩 화면 숨김, 본편 런타임 v128, Google 3D `ready`, 시작 버튼과 역사 선택 화면 진입을 확인했다.
- 외부 RTS 호송대 방어 화면도 런타임 v122, Google 3D `ready`, 로딩 완료, 캔버스 2개, 콘솔 오류 0건으로 확인했다.
- 첫 시도에서 다른 사커스타 SFTP 서버에 생성했던 `/autodev/GameCreator/hormuz` 117개 파일은 최초 업로드 수량·바이트를 대조한 뒤 모두 삭제했고 빈 상위 폴더도 정리했다. 원본과 올바른 sidak 배포본에는 영향이 없다.

## 2026-07-31 — 선택 단계별 안전 저장·이어하기 v129 (검증 전 기록)

- 기존 저장은 모든 수치를 기록했지만 전투·이란 대응·협상 도중 새로고침하면 그날 브리핑으로 되돌아가 이미 반영된 선택 효과가 중복 적용될 수 있었다.
- 라이브 상태 저장과 별도로 `hormuzCheckpointV2` 안전 체크포인트를 추가했다. 역사 선택 완료, 첫 호송대 방어 대기, 일일 브리핑, 정책 선택 확정, 미국 측 조치 완료, 이란 대응 확정, 일일 결산 완료 지점마다 완성된 상태만 저장한다.
- 이어하기는 체크포인트의 날짜·세부 단계부터 재개하며, 기존 v1 저장은 안전하게 해당 날짜 브리핑 또는 진행 중인 역사 선택 단계로 이전한다.
- 통항·유가·국민 지지·국제 지지·탄약·확전·가용 예산·여당 지지와 의회·전투·협상·역사 선택 이력을 모두 복원 대상으로 유지한다.
- 시작 화면에 마지막 저장 시각·진행 단계·핵심 수치 8종·최근 선택을 표시하고, 저장이 있을 때는 `이어하기`를 우선 배치한다. `처음부터 새로 시작`은 기존 진행을 교체하기 전에 확인한다.
- 본편 런타임은 v129, 본편 번들 캐시는 v58, 게임 CSS 캐시는 v43으로 올렸다.

## 2026-07-31 — 선택 단계별 안전 저장·이어하기 v129 최종 검증·외부 배포

- 새 작전에서 과거 주요 사건 1단계를 선택하고 결과를 확인한 뒤 새로고침했다. 시작 화면에 마지막 저장 단계와 통항·유가·국민 지지·국제 지지·탄약·확전·가용 예산·여당 지지가 표시됐고, 이어하기는 이미 끝낸 1단계를 반복하지 않고 2단계부터 열었다.
- 저장된 작전이 있을 때 `처음부터 새로 시작`을 누르면 확인 창이 뜨며, 취소했을 때 저장과 이어하기 버튼이 유지되는 것을 확인했다.
- DAY 7 정책 선택 확정 상태에서 핵심 수치 8종, 의회·여당 수치, 정책 내용, 역사 선택, 75건의 선택 이력과 공개 정보 플래그가 그대로 복원됐다. 새 게임 확정 뒤에는 이전 체크포인트가 제거됐다.
- 정책 집행 도중 새로고침을 모의한 결과 정책 효과와 선택 이력은 정확히 한 번만 반영됐다. 이미 완료된 일일 결산 체크포인트는 다시 계산되지 않았고 다음 DAY 브리핑으로 이동했다.
- 기존 `hormuzProgressV1`의 전투 중 저장은 수치를 보존한 채 해당 DAY 브리핑 안전 지점으로 이전됐다. 엔딩을 본 저장은 이어하기 목록에 남지 않는다.
- 저장·이어하기 전용 Chromium 검증과 미션 라우팅 7종·직접 진입 3종을 모두 통과했다. 콘솔·페이지·HTTP 오류는 0건이다.
- 공개 테스트 서버 `https://sidak.kr/autodev/GameCreator/hormuz/`의 `index.html`, `assets/css/game.css`, `assets/js/game.bundle.js`를 v129로 갱신했다. SFTP 절대경로는 `/GameCreator/hormuz`이며 업로드 후 SHA-256을 원본과 대조했다.
- 공개 HTTPS에서 본편 런타임 129, 즉시 표시되는 시작 화면, 저장 요약, 새로고침 뒤 이어하기와 다음 역사 사건 복원을 다시 확인했다. 페이지 오류는 0건이다.

## 2026-07-31 — 임무별 전장 차별화·자동전투 전환·Google 확대 동기화 v130/v123 (검증 전 기록)

- 자동전투를 켜기 전에 수동 공격 명령이 있으면 기존 명령이 그대로 남아 자동 공격 상태기가 시작되지 않는 회귀를 실제 속도에서 재현했다. 자동전투 전환 시 기존 명령을 안전하게 초기화하고, 장시간 이동·발사 진척이 없으면 표적을 재배정하도록 보강하는 작업에 착수했다.
- 페르시아만 서부 공중방어에서 Google 3D 카메라가 최대 범위에 도달한 뒤 Three.js 카메라만 멀어지는 현상과 이란 공중위협기 방향값 반전을 확인했다.
- 호르무즈 해협과 겹치던 대규모 전투를 자스크 남쪽 오만만 가상 교전구역으로 분리하는 작업에 착수했다.
- 본편 런타임은 v130, 전투 런타임·번들 캐시는 v123, 전투 데이터는 version 31·캐시 v32로 올렸다.
- 본편의 전투 URL 생성 상수가 `main.js`와 `ui/game_ui.js` 두 곳에 중복돼 직접 임무 진입만 v122를 유지하던 것을 확인해 둘 다 v123으로 통일했다.

## 2026-07-31 — 전장별 차별화·자동전투·서부 공중방어 v130/v123 최종 검증

- 7개 전투가 같은 호르무즈 해협과 고속정 위주로 반복되지 않도록 전장 좌표, 지도 범위, 적 전력 구성을 전수 분리했다. 대규모 전투는 자스크 남쪽 오만만 합동교전구역으로 옮기고 고속정·무인수상정·공중위협기를 혼성 배치했으며, 유조선 구조도 고속정과 무인수상정의 혼성 공격으로 변경했다.
- 자동전투를 켜기 전에 수동 공격 명령이 남아 있으면 자동 AI가 그 명령을 유효한 명령으로 오인해 선회만 반복하던 원인을 수정했다. 자동전투 전환 시 각 아군의 명령을 안전하게 초기화하고, 이동·발사 진척이 일정 시간 없으면 경로를 비운 뒤 새 표적을 배정한다.
- 전투기와 스텔스 폭격기의 최종 공격 진입에서는 충돌 회피 보정이 기수 정렬을 방해하지 않도록 직선 진입 모드를 추가했다. 곡괭이산 작전에서는 지하시설 입구가 남아 있는 동안 B-2가 다른 표적을 고르지 않게 하고, 제한 탄약을 모두 쓴 항공기는 다시 공격 명령을 받지 않게 했다.
- 페르시아만 서부 호송선단 공중방어에서 Google 3D 지도의 최대 축소 거리와 Three.js 객체 카메라 범위를 같은 한계로 묶었다. 한계 도달 뒤 휠을 더 굴려도 지도만 멈추고 객체만 작아지는 현상이 재발하지 않는다.
- 이란군 공중위협기가 반대 방향을 바라보던 모델 yaw 덮어쓰기를 제거하고 아군 전투기와 같은 정상 전방축 보정을 사용하게 했다.
- RTS 시나리오 7종 전체 검증을 통과했다. 콘솔·페이지·HTTP 오류는 모두 0건이며, 해상 유닛의 육지 배치도 0건이다. 대규모 전투는 기존 33개 적 유닛에서 20개 혼성 적 전력으로 조정해 식별성과 성능을 함께 개선했다.
- 본편 임무 연결 7종과 직접 진입 3종을 모두 통과했고, 모든 전투 URL이 RTS v123을 사용한다. Google 3D 서부 공중방어 별도 검증에서도 최대 축소 후 추가 휠 입력에 카메라 값이 변하지 않고 공중위협기 방향이 정상임을 확인했다.
- 런타임 사용 GLB 32종을 검사했다. 금지된 4만 삼각형대 Meshy 원본은 런타임에 없으며, 이번 변경에서 새 모델 파일은 추가하지 않았다.

## 2026-07-31 — v130/v123 공개 테스트 서버 갱신

- 공개 테스트 주소 `https://sidak.kr/autodev/GameCreator/hormuz/`에 본편 v130, RTS v123, 전투 데이터 version 31을 반영했다. SFTP 절대경로는 `/GameCreator/hormuz`이다.
- `index.html`, `rts-combat.html`, 본편 번들, RTS 번들, RTS 데이터 5개 파일을 원자적으로 갱신했고 업로드 뒤 원격 SHA-256을 로컬 원본과 대조해 모두 일치했다.
- 공개 HTTPS에서 시작 화면, 본편 번들 v59, 서부 공중방어 전장, RTS 번들 v123, Google 3D 준비 완료를 확인했다. 자동전투를 켠 뒤 아군 상태가 `공격 중`으로 전환되고 명중 피드백이 발생했으며 브라우저 오류는 0건이다.
- 공개 RTS 번들과 데이터의 HTTP 응답 SHA-256이 배포 원본과 일치했다. HTML 두 파일은 Cloudflare 분석 스크립트 주입 때문에 응답 해시만 달라지지만, 브라우저에서 의도한 v130/v123 런타임이 정상 실행되는 것을 확인했다.

## 2026-07-31 — 3배 장기전·대통령 발언 순환 v131 (검증 전 기록)

- 기존 최단 성공 루트가 과거 선택 6회, DAY 1~4 정책 4회, 협상 조항 4회로 약 14개 선택에 불과한 것을 확인했다. 성공 엔딩에 필요한 최소 선택을 42개 이상으로 늘리는 작업에 착수했다.
- 하루 한 번이던 국가안보회의 선택을 군사, 외교·협상, 경제·정보의 세 단계로 분리했다. 각 단계는 전용 카드군에서 3개 선택지를 제시하며, 세 결정을 모두 마친 뒤 이란의 대응으로 넘어간다.
- 종전 협상 잠금 시점을 DAY 4에서 DAY 12로 늦췄다. DAY 12 외교 결정을 통해 가장 빠르게 협상하더라도 과거 선택 6회와 협상 조항 4회를 포함해 최소 45개의 선택을 거쳐야 한다.
- 장기전 상한을 DAY 18에서 DAY 54로 늘리고, 본편 런타임을 v131, 본편 번들 캐시를 v60, 데이터 캐시를 v35, CSS 캐시를 v44로 올렸다.
- 2026년 3~7월 미국 대통령의 이란 전쟁 관련 공식 문서와 주요 보도를 조사했다. “곧 끝난다”, “2~3주”, “강하게 타격”, “휴전 종료와 협상 병행”, “외교 실패 시 군사행동”의 변화 양상을 9개 한국어 게임 대사로 재구성했다.
- 대통령은 기존 뒷모습 이미지로만 등장하며 실제 얼굴 정면·측면과 실제 음성을 사용하지 않는다. 이란 대응 뒤 매일 말풍선과 전날 메시지 비교를 표시하는 연출을 추가하는 작업에 착수했다.

## 2026-07-31 — 3배 장기전·대통령 발언 순환 v131 최종 검증·공개 배포

- 하루 정책 결정을 군사, 외교·협상, 경제·정보의 3단계로 분리했다. 최종 협상은 DAY 12부터 열리고 캠페인 상한은 DAY 54가 되며, 최단 성공 경로는 기존 14회에서 45회로 3.21배 늘었다.
- 100회 자동 시뮬레이션에서 모든 실행이 종료됐고 종료일은 DAY 12~23, 평균 DAY 13.3이었다. 성공 엔딩이 DAY 12 이전에 발생하지 않는 불변 조건도 통과했다.
- 대통령 뒷모습 이미지 위에 최근 발언 취지를 재구성한 한국어 말풍선을 매일 표시하고, 직전 발언과 현재 발언을 함께 비교하도록 했다. 총 9개 대사는 출처 날짜와 URL을 포함하며 실제 음성·정면 얼굴은 사용하지 않는다.
- 저장·이어하기 검증에서 하루 3단계의 중간 선택 위치와 대통령 발언 단계를 포함한 체크포인트 복원을 확인했다. 캠페인 진행 검증은 최단 성공 45회, 발언 9개, 브라우저 오류 0건으로 통과했다.
- RTS 시나리오 7종 전체와 본편 미션 라우팅 7종·직접 진입 3종을 재검증했다. 모든 전투가 통과했고 콘솔·페이지·HTTP 오류와 육지 오배치는 0건이었다.
- 공개 테스트 서버 `https://sidak.kr/autodev/GameCreator/hormuz/`에 본편 v131을 배포했다. 실행 파일 9종의 SFTP 업로드 후 원격 SHA-256을 대조해 모두 일치했고, 공개 HTTPS에서도 v131·하루 3단계·DAY 12/54·대통령 발언 화면을 다시 검증했다.

## 2026-07-31 — 반복 전투 방지·적응형 적 전력 v132/v124 (검증 전 기록)

- 최근 2회 전투와 같은 시나리오를 군사 정책 카드와 이란 대응 양쪽에서 우선 제외해, 이름만 다른 이벤트가 동일 전투로 연속 연결되던 구조를 수정했다.
- 전투 날짜, 확전 수위, 이란 태세, 같은 전투 반복 횟수, 최근 승패와 연승을 본편에서 RTS로 전달하고 이를 바탕으로 전장 난이도를 1~5단계로 산정한다.
- 난이도가 오르면 적 체력·화력·교전 시작 속도와 증원 수가 함께 상승한다. 최고 단계는 기본 대비 체력 134%, 화력 125%이며 전투별 특성에 맞는 혼성 증원이 투입된다.
- 같은 전투가 다시 발생해도 적 진입 방향과 배치 변형이 순환되고, 직전 승리에서 가장 많이 투입한 아군 병과에 대응하는 적 증원을 우선 편성한다.
- 전투 브리핑에 현재 난이도, 증원 규모, 적 체력·화력 보정, 대응 편성 여부를 한국어로 표시한다.
- 본편 런타임은 v132, RTS 런타임은 v124, RTS 데이터는 version 32로 올렸다.

## 2026-07-31 — 반복 전투 방지·적응형 적 전력 v132/v124 최종 검증·공개 배포

- 군사 정책과 이란 대응을 각각 무작위 100회 표본 검사했다. 최근 2회 전투와 같은 시나리오가 연속으로 선택된 사례는 0건이었다.
- 최고 난이도 7개 전장을 실제 브라우저에서 전수 검사했다. 모든 전장에서 적 병과가 2종 이상 혼성 편성됐고 전투별 증원 1~4기, 체력 134%, 화력 125%, 이전 아군 주력 병과 대응 편성이 적용됐다.
- 최고 난이도에서도 삼각형은 최대 119,965/120,000, 드로콜은 최대 62/90으로 성능 헌법을 지켰다. 해상 유닛 육지 배치와 지상 유닛 해상 배치는 0건이며, 전장 변형 0과 2의 실제 적 배치가 달라지는 것도 확인했다.
- 기존 기본 난이도 RTS 7종의 자동전투·무기 발사·임무 성공 조건을 다시 검사해 모두 통과했다. 콘솔·페이지·HTTP 오류와 해상 유닛 육지 배치는 모두 0건이었다.
- 저장·이어하기, 45회 이상 장기전 진행, 대통령 발언, 본편 임무 연결 7종과 직접 임무 진입 3종도 모두 통과했다. 100회 캠페인 자동 실행의 종료·점수·날짜 불변 조건도 유지됐다.
- 런타임 GLB 32종을 검사했다. 금지된 4만 삼각형대 Meshy 원본은 사용하지 않으며 이번 변경에서 새 모델 파일은 추가하지 않았다.
- 공개 테스트 서버 `https://sidak.kr/autodev/GameCreator/hormuz/`에 본편 v132, RTS v124, RTS 데이터 version 32를 원자적으로 배포했다. 실행 파일 5종의 SFTP 업로드 후 원격 SHA-256과 공개 HTTPS 응답 해시가 모두 원본과 일치했다.
- 공개 브라우저에서 본편 런타임 v132와 시작 화면, RTS 런타임 v124를 확인했다. 최고 난이도 호송 방어를 실제 시작했을 때 아군 4기·적 11기·유조선 3척, 난이도 5/5, 증원 3기, 전투 중 상태가 표시됐고 브라우저 오류는 0건이었다.

## 2026-08-01 — AI 통합 인수인계 HTML 작성

- v132/v124까지의 실제 구현 상태를 다른 AI가 바로 이어서 사용할 수 있도록 `docs/hormuz-ai-handover-2026-08-01.html`로 통합했다.
- 정본 문서 우선순위, 실행 주소, 소스 구조, 54일 캠페인, RTS 7종, 조작 계약, 적응형 난이도, Google 3D, Meshy·이미지·음향, 저장, 확정 규칙, 빌드·검증, SFTP 배포, 미완료 우선순위와 다음 AI 시작 지시문을 한 문서에 담았다.
- 완료와 미완료를 분리해 `base_retaliation`, `ceasefire_cover`, 본편 BGM·지휘관 음성, Firebase 랭킹이 아직 남아 있음을 명시했다.
- `AGENTS.md` 상단에 최신 HTML 포인터를 추가해 새 AI가 기존 확정 결정과 최신 구현 상태를 함께 읽도록 했다.
- 내부 목차 16개, 상대 문서 링크, 로컬 HTTP 200, UTF-8 한글 표시, 브라우저 오류 0건을 확인했다. 430×932 모바일에서 본문 16px, 고정 목차 해제, 가로 넘침 0으로 검증했다.

## 2026-08-01 — 한/영 이중 언어 · IP 기반 자동 선택 · 개발자 페이지 v133

- 페이지를 언어별로 나누지 않고 같은 DOM에서 문구만 교체하는 구조로 만들었다. 새 엔진은 `assets/js/core/i18n.js` 하나이며, `t()` 사전 조회, `pick()` 데이터 이중 필드 선택, `data-i18n` 정적 마크업 치환, 언어 변경 구독을 담당한다.
- 언어 결정 우선순위는 `?lang=` → localStorage 저장값 → 접속 IP 국가 → 타임존·브라우저 언어 → 기본 영어다. **한국(KR)만 한국어이고 나머지 국가는 모두 영어로 접속된다.**
- 정적 사이트라 서버가 없으므로 IP 판별은 Cloudflare `/cdn-cgi/trace`의 `loc` 값을 쓴다. 키가 필요 없고 응답이 한 줄 평문이다. 배포 도메인이 Cloudflare 뒤에 있으면 동일 출처에서 끝나고, 아니면 `www.cloudflare.com`으로 넘어간다. 로컬 개발 서버에서는 항상 404이므로 동일 출처 후보를 아예 건너뛴다.
- 이 조회는 시작 화면을 막지 않는다. 네트워크가 필요 없는 타임존·브라우저 언어로 먼저 그리고, IP 결과가 오면 사용자가 직접 고르지 않은 경우에만 조용히 바꾼다. 부팅 직후에는 번들·모델·텍스처가 동시에 내려와 첫 시도가 자주 끊기므로 4초 상한으로 두 번 시도하고, 두 번 모두 실패했을 때만 실패를 기록한다.
- 타이틀 화면과 개발자 페이지 오른쪽에 한/영 토글을 붙였다. 여기서 고른 값만 저장되며 IP 판별을 이긴다. 전환하면 페이지를 새로 열지 않고 정적 마크업과 계기판을 즉시 다시 그린다. 전투 iframe에는 `lang` 파라미터로 넘긴다.

### 번역 범위

| 대상 | 방식 | 규모 |
|---|---|---:|
| 본편 UI 문자열 | `strings.ko/en.json` 키 | 401키 |
| 카드·이벤트·엔딩·역사·임무·작전·발언 | `<필드>En` 가산 필드 | 7파일 |
| 지명 라벨 | `geo_features.json` `nameEn` | 20곳 |
| 전투 화면 | 기존 `rts-combat.json` ko/en | 220키 |
| 정적 마크업 | `data-i18n` / `data-i18n-attr` | 2페이지 |

- 데이터는 기존 구조를 깨지 않는 가산 방식이다. `pick()`은 영어일 때 `titleEn`이 있으면 그것을, 없으면 한국어 원문을 쓴다. 번역이 빠진 항목이 생겨도 화면이 비지 않는다.
- 전투 화면은 이미 ko/en 220키가 완비돼 있었다. 남아 있던 aria-label과 지형 연결 문구만 사전으로 옮기고 `applyStaticStrings()`를 추가했다.
- 대통령 발언은 원문 의미를 게임 대사로 압축한 재구성이라는 성격을 영문에서도 유지했다. 실제 인물의 음성·얼굴 정면을 쓰지 않는다는 고지도 함께 번역했다.

### 개발자 페이지

- `developer.html`과 `assets/css/developer.css`를 새로 만들고, 본편 오른쪽 아래에 항상 떠 있는 원형 버튼을 붙였다. crimeGame의 `social-fixed-link` 패턴을 가져오되 HORMUZ 색 규율로 다시 짰다.
- 개발자가 만든 게임 3종을 함께 싣는다. SON 키우기 타이쿤(Google Play), 마크의 마지막 수리(crimeGame 공개 주소), HORMUZ(현재 게임). 카드 썸네일은 기존 이미지에서 정사각으로 잘라 `assets/images/developer/`에 만들었다.
- 이 페이지도 같은 i18n 엔진을 쓴다. 별도 영문 페이지를 만들지 않았다.

### 검증

- 새 도구 `tools/validate-i18n.py`를 추가했다. 사전 키 정합성, 데이터 영문 누락, 실제 브라우저 ko/en 화면의 잔여 한글과 미치환 키를 한 번에 잡는다. 실패 시 exit code 1이다.
- 실화면 6종(타이틀·개발자·전투 × ko/en) 모두 통과했다. 영문 화면의 잔여 한글은 0이며, 언어 토글의 `한국어` 표기만 예외로 둔다.
- Cloudflare 응답을 가로채 국가만 바꾼 검사에서 `loc=US`와 `loc=JP`는 영어로, `loc=KR`은 한국어로 전환됐다. 브라우저 언어와 타임존을 한국으로 고정한 상태에서도 IP가 이기는 것을 확인했다.
- 작전·예산실과 의회 표결 화면을 두 언어로 열어 잔여 한글 0, 콘솔 오류 0을 확인했다.
- 기존 게이트도 다시 돌렸다. RTS 7종 전부 통과(최대 96,329/120,000), 미션 라우팅 7종 일치, 작전 로드맵 정합성 통과.
- 지오IP 탐색용 404는 정상 폴백이므로 검증 도구가 실패로 세지 않도록 걸렀다.

### 캐시 버전

`game.bundle.js` v62 · `game.css` v46 · 본편 데이터 v37 · `rts-combat.bundle.js` v125 · 전투 데이터 v34 · `rts-combat.json` version 33

### 검증 기록

- 한/영: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-v133-i18n-validation.json`
- 화면 6종: `output/validation/hormuz-v133-i18n-<화면>-<언어>-1440x900.jpg`
- IP 판별: `output/validation/hormuz-v133-i18n-geoip-<국가>-1440x900.jpg`
- 개발자 페이지 전체·모바일: `output/validation/hormuz-v133-i18n-developer-full-<언어>.jpg`, `hormuz-v133-i18n-developer-mobile-en.jpg`
- RTS 게이트: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-rts-v133-scenario-matrix-validation.json`
- 라우팅: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-v133-mission-routing-validation.json`

## 2026-08-01 — 한/영 전수검사·확정 결함 9건 수정 v134

배포 전 전수검사를 돌렸다. 14개 에이전트 병렬 코드 감사(4관점 리뷰 + 발견별 적대 검증) + 실브라우저 전체 캠페인 자동 주행(한/영) + 전투 종결까지 실주행. **9건의 실결함을 찾아 전부 수정했다.**

### 발견·수정한 결함

| # | 심각도 | 결함 | 수정 |
|---|---|---|---|
| 1 | critical | `launchIntroBattle()` 이 인트로 전투 URL 에 `lang` 미전달 — 영어 사용자의 첫 전투가 한국어 | URL 에 `lang` 추가, 실주행으로 en/ko 왕복 확인 |
| 2 | major | 국가안보회의 결정 카드가 `option.name/desc` 원시 접근 — 영어 주행 중 정책 카드 한글 노출 | `pick()` 치환. 영어 전체 주행에서 재발 0 확인 |
| 3 | major | 엔딩 화면 `ended.name/tagline` 한글 고정 | `finalizeEnding` 이 En 필드 동봉, 표시 `pick()` |
| 4 | major | 전투 편성 목록의 유닛 정의 이름 9종(정적)+9종(동적) 한글 고정 | `nameEn` 추가 + `unitDisplayName()` 헬퍼, GBU `발` 카운터 사전화 |
| 5 | major | `t()` 가 사전 로드 실패 시 vars 를 버려 치명 오류 메시지의 진단 정보 소실 | 키 미발견 시 `키 (name=값)` 형태로 vars 동봉 |
| 6 | major | `developer.html` top-level await 무보호 — 사전 로드 실패 시 언어 토글이 소리 없이 죽음 | try/catch + 실패 시 토글 숨김 + console.error |
| 7 | major | `applyDocumentLang()` 이 개발자 페이지 meta description 을 게임 소개로 덮어씀 | `<html data-i18n-title-key/description-key>` 오버라이드 도입 |
| 8 | major | 우하단 개발자 버튼이 하단 속보 밴드와 겹치고 결정 패널을 가림 | 티커 위로 60px 상향 + 감시자가 오버레이 열림 시 `visibility` 로 즉시 숨김. ★transition 기반 숨김은 스로틀 탭에서 stuck(r2025 계열) — visibility 필수 |
| 9 | minor | 엔딩 점수 세부에 `106.95000000000007` 부동소수 노출 / `iranEyebrow` 영문이 용어집(NSC) 위반 | scoring 부분점수 반올림 / "Iranian National Security Council Response" |

- 코드 감사가 낸 오탐 4건은 적대 검증 단계에서 기각됐다(이미 병행 수정된 항목을 리뷰어가 수정 전 코드로 본 것).
- 검증 도구의 런타임 버전 고정핀(124/132)을 126/133 으로 갱신했다. 이 핀은 소스·번들·캐시 동기화 잠금장치이므로 런타임을 올릴 때 반드시 같이 올린다.

### 신규 상시 게이트

`tools/validate-campaign-fullrun.py` — debug=fast 로 과거사건→본편→엔딩까지 한/영 실주행. 영어 주행 중 잔여 한글·미치환 키·콘솔 오류·게임 멈춤을 잡는다. 이번 결함 #2 를 잡은 도구다.

### 최종 게이트 결과 (전부 통과)

- i18n: 사전 402키 정합 · 데이터 8종 En 완비 · 실화면 6종(타이틀/개발자/전투 × ko/en) 잔여 한글 0
- 전체 주행: en(uneasy_truce 1811점)·ko 완주, 콘솔/HTTP 오류 0
- 전투 실주행: convoy_shield 8배속 종결까지 영어 잔여 한글 0
- 인트로 전투 lang 전달: en/ko 왕복 확인
- RTS 7시나리오: 최대 96,365/120,000 · 자동전투/휠줌/지도정체성 판정 포함 전부 통과
- 미션 라우팅 7종 + 직접 진입 3종, 적응형 난이도, 캠페인 페이싱: 통과
- 개발자 버튼: 티커 위 배치·오버레이 열림 시 숨김·닫힘 시 복귀 실측 확인

### 캐시·버전 (배포 기준)

`game.bundle.js` v64 · `game.css` v47 · 본편 데이터 v37 · `rts-combat.bundle.js` v126 · 전투 데이터 v35 · `rts-combat.json` version 34 · MAIN_RUNTIME 133 · RTS_RUNTIME 126

### 검증 기록

- 감사·수정 후 게이트: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-v134-i18n-validation.json` · `hormuz-rts-v134-scenario-matrix-validation.json` · `hormuz-v134-mission-routing-validation.json`
- 전체 주행: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-fullrun-validation.json` + 최종 화면 2장
- 전투 종결 영문: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-v133-i18n-battle-result-en.jpg`

## 2026-08-01 — 공개 서버 배포 v134 (한/영 + 개발자 페이지)

- `tools/deploy-sidak.py` 를 새로 만들어 SFTP 배포를 절차로 고정했다. 비밀번호는 FileZilla 사이트 관리자에서 읽고 코드·문서에 남기지 않는다. 호스트 키를 고정 대조하고, 덮어쓰기 전 원격 원본을 `_backup/<타임스탬프>/` 로 복사하며, 임시 파일로 올린 뒤 rename 하는 원자적 교체를 쓴다. 업로드 후 원격 파일을 다시 읽어 sha256 을 대조한다.
- 22개 파일 4,331,251 바이트를 올렸고 **전부 sha256 일치**를 확인했다. 백업은 `/GameCreator/hormuz/_backup/20260801T012823Z` 에 남겼다.
- 올린 것: `index.html`·`developer.html`(신규)·`rts-combat.html`, `game.css`·`developer.css`(신규), 번들 2종, `core/i18n.js`(developer.html 이 ES 모듈로 직접 가져오므로 원본 필요), 데이터 JSON 11종, 개발자 페이지 이미지 3종.
- 런타임·모델 GLB·오디오·기존 이미지는 변경이 없어 올리지 않았다.

### 배포 후 실제 공개 서버 검증 (전부 통과)

| 검사 | 결과 |
|---|---|
| `index.html?lang=en` | html lang=en · 잔여 한글 0 · 오류 0 |
| `index.html?lang=ko` | html lang=ko · 정상 |
| `developer.html?lang=en` | html lang=en · 잔여 한글 0 |
| `developer.html?lang=ko` | html lang=ko · 정상 |
| `rts-combat.html?lang=en` | 전투 화면 잔여 한글 0 |
| IP 판별 US | → 영어 (source=geoip) |
| IP 판별 KR | → 한국어 (source=geoip) |

- 미치환 키 0, 콘솔 오류 0, HTTP 4xx/5xx 0.
- **동일 출처 `/cdn-cgi/trace` 가 실제로 동작한다** — sidak.kr 이 Cloudflare 뒤에 있어 국가 판별이 외부 요청 없이 1회에 끝난다.

### 공개 주소

- 게임: https://sidak.kr/autodev/GameCreator/hormuz/
- 개발자 정보: https://sidak.kr/autodev/GameCreator/hormuz/developer.html
- 언어 강제: 뒤에 `?lang=en` 또는 `?lang=ko`

### 기록

- 배포 매니페스트: `D:\soccerstarWebSource\GameCreator\hormuz\output\deployment\hormuz-v134-sftp-deploy.json`
- 공개 검증: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-v134-live-validation.json`
- 공개 화면 5장: `output/validation/hormuz-v134-live-<화면>.jpg`

## 2026-08-01 — 곡괭이산 전투 잔여 한글·달성 불가 교착 수정 v135 (배포 완료)

사장님 실플레이 제보 2건을 고쳤다. 둘 다 실제 결함이었다.

### 1. 영어 화면에 '발' 잔존

`renderSelectionSummary()` 의 GBU-57 잔탄 표기가 `발` 을 하드코딩하고 있었다. v134 에서 유닛 상세(`data-unit-detail`) 쪽은 `shotUnit` 키로 고쳤지만 **선택 요약 쪽 두 번째 표기 지점을 놓쳤다.** B-2 를 두 기 선택했을 때 `GBU-57 0/2발 · GBU-57 0/2발` 로 노출됐다.

- 원인: 같은 문구가 두 곳에 있었고 한 곳만 고침. 정적 화면 검사는 전투 시작 직후만 보므로 유닛을 선택해야 나타나는 이 문자열을 잡지 못했다.
- 수정: 선택 요약도 `this.text("shotUnit")` 사용. 한국어 `발`, 영어 빈 문자열.

### 2. 관통탄을 다 쓰면 아무것도 못 하고 260초를 기다려야 함

곡괭이산은 B-2 의 GBU-57 로만 지하 진입부를 봉쇄할 수 있다. 탄을 다 쓰고 목표가 남으면 **승리가 수학적으로 불가능**한데, 기존 종료 조건은 `아군 전멸 · 유조선 손실 · 시간 초과` 뿐이라 플레이어가 할 일이 없는 화면에 260초 동안 갇혔다.

- 수정: `objectiveUnreachable()` 판정을 추가해 `checkOutcome()` 의 실패 조건에 넣었다. `requiredFleetType` 이 지정된 전투에서 **남은 탄 + 비행 중인 폭탄 < 남은 목표 수**가 되면 즉시 실패로 끊는다.
- 실패 문구는 이미 상황에 맞게 있었다. 한국어 `관통탄이 소진됐거나 스텔스 폭격기가 이탈했습니다`, 영어 `The penetrators were expended or the bomber package withdrew`.
- 한정 탄약이 없는 병과(구축함·전투기 등)로 싸우는 전투는 이 판정을 하지 않는다. 살아 있는 한 계속 싸울 수 있기 때문이다.
- 투하해서 아직 날아가는 폭탄은 남은 탄으로 함께 센다. 명중 직전에 실패로 끊기는 오판을 막는다.

### 검증

신규 상시 게이트 `tools/validate-unwinnable-abort.py` 를 추가했다. 탄을 강제 소진시키고 (a) 즉시 종료되는지 (b) 타이머를 기다리지 않는지 (c) 영어 화면에 한국어 수량 단위가 없는지 본다.

| 케이스 | 결과 |
|---|---|
| B-2 2기 전량 소진 | 1초 뒤 종료 (260초 대기 없음) |
| 남은 탄 1 < 목표 2 (부분 소진) | 달성불가 판정 후 1초 뒤 종료 |
| 선택 요약 영문 | `GBU-57 2/2` — 한글 0 |
| 결과 화면 en / ko | `Mission Failure / Limited strike incomplete` · `임무 실패 / 제한 타격 목표 미달` |

기존 게이트도 재실행해 전부 통과했다. i18n 6화면, RTS 7시나리오(최대 96,337/120,000), 곡괭이산 전용 검증, 적응형 난이도.

### 배포

RTS 런타임 126 → 127, `rts-combat.bundle.js` v127, `game.bundle.js` v65. 변경된 4개 파일만 올라갔고 나머지 18개는 sha256 동일로 건너뛰었다. **공개 서버에서 두 언어 모두 재검증 통과.**

- 배포 매니페스트: `D:\soccerstarWebSource\GameCreator\hormuz\output\deployment\hormuz-v135-sftp-deploy.json`
- 교착 해소 검증: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-unwinnable-abort-validation.json`

## 2026-08-01 — 좌상단 HORMUZ = 시작 화면 복귀 v136 (배포 완료)

본편과 전투 화면의 좌상단 `HORMUZ` 를 눌러 시작 화면으로 돌아갈 수 있게 했다. 지금까지는 진행 중 화면에서 빠져나올 방법이 결과 화면 버튼뿐이었다.

- 두 화면 모두 `<button id="brand-home">` 으로 바꿨다. 기존 타이포는 그대로 두고 button 기본 스타일만 걷어냈다. 커서·호버·`:focus-visible` 외곽선을 붙여 조작 가능한 요소임을 드러내고 키보드로도 닿는다.
- 진행 중이면 한 번 물어본다. 본편은 체크포인트가 남아 손실이 없다고 안내하고, 전투는 이번 결과가 기록되지 않는다고 명시한다. 전장을 보다 잘못 눌러 화면이 통째로 바뀌는 사고를 막는다.
- 돌아갈 때 `?lang=` 을 유지한다. 자동 시작 파라미터는 떼야 시작 화면이 뜬다.
- 전투가 본편 안 iframe 으로 떠 있으면 부모 창을 옮긴다. 그러지 않으면 게임 안에 게임이 중첩된다.
- 편성·결과 오버레이가 떠 있는 동안에는 헤더가 흐려져 브랜드가 배경처럼 보인다. 그 단계의 출구는 오버레이 자체 버튼(작전 시작 / 상황실 복귀)이므로 억지로 위로 올리지 않았다. 헤더의 `z-index:20` 이 쌓임 맥락을 만들어 자식만 올리는 방식은 애초에 동작하지도 않는다.

### 배포 목록 누락 사고

첫 배포에서 `assets/css/rts-combat.css` 가 `tools/deploy-sidak.py` 의 목록에 아예 없어 전투 화면 스타일이 구버전으로 남았다. **파일이 빠져도 배포는 성공으로 끝나기 때문에 조용히 지나갈 뻔했다.**

- 목록에 추가하고, 공개 확인 항목에 `rts-combat.css` 의 `brand-home` 과 `game.css` 의 `overlay-hidden` 표지를 넣었다. 이제 목록에서 빠지면 공개 확인이 실패로 잡는다.
- 같은 이유로 `index.html`·`rts-combat.html` 확인에도 `brand-home` 표지를 추가했다.

### 검증

신규 게이트 `tools/validate-brand-home.py` — 본편 진행 중 복귀, 전투 중 취소 잔류/확인 이동, 언어 유지를 본다. 로컬·공개 서버 양쪽 3케이스 전부 통과했다.

기존 게이트도 재실행해 전부 통과했다. i18n 6화면, 전체 주행 한/영 완주, RTS 7시나리오, 미션 라우팅, 달성 불가 종료.

### 캐시

`game.bundle.js` v66 · `game.css` v48 · `rts-combat.bundle.js` v128 · `rts-combat.css` v59 · 전투 데이터 v36 · `rts-combat.json` version 35 · RTS 런타임 128

- 배포 매니페스트: `D:\soccerstarWebSource\GameCreator\hormuz\output\deployment\hormuz-v136-sftp-deploy.json`
- 홈 복귀 검증: `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-brand-home-validation.json`

## 2026-08-01 — 좌상단 HORMUZ 클릭 불가 수정 v137 (배포 완료)

v136 에서 홈 버튼을 붙였다고 보고했지만 **실제로는 눌리지 않았다.** 사장님 재제보로 확인했다.

### 진짜 원인

`.top-hud` 와 `.battle-header` 는 전장 조작(드래그 선택·우클릭 이동)을 가리지 않으려 `pointer-events: none` 이다. 자식인 브랜드 버튼이 이 값을 상속받아, 클릭이 버튼을 통과해 그대로 캔버스로 떨어졌다. 좌표 히트테스트로 확인한 최상위 요소는 `CANVAS#world-canvas` / `CANVAS#rts-canvas` 였다.

- 수정: `#brand-home { pointer-events: auto; }` 를 두 CSS 에 넣어 버튼만 클릭을 되받게 했다.

### 검증이 못 잡은 이유 — 이번 건의 진짜 교훈

v136 의 `validate-brand-home.py` 는 `element.click()` 합성 클릭을 썼다. **합성 클릭은 덮개와 `pointer-events` 를 무시하고 핸들러를 직접 부르기 때문에, 실제로는 눌리지 않는 버튼도 통과로 판정한다.** 그래서 로컬·공개 서버 양쪽에서 PASS 가 나왔는데 실제로는 동작하지 않았다.

- 검증 도구를 **좌표 기반 실제 마우스 클릭**으로 바꿨다. 클릭 전에 `document.elementFromPoint()` 로 그 좌표의 최상위 요소가 정말 버튼인지 먼저 확인하고, 아니면 무엇이 막았는지(`blockedBy`)와 `pointer-events` 계산값을 함께 보고한다.
- **앞으로 클릭 동작 검증에 `element.click()` 을 쓰지 않는다.** 특히 HUD 처럼 `pointer-events:none` 이 걸린 컨테이너 안의 요소는 반드시 실제 포인터로 확인한다.

### 다른 조작 요소 전수 히트테스트

같은 함정이 더 있는지 좌표로 전부 확인했다. 타이틀 화면의 개발자 버튼·언어 토글·시작/역사 버튼, 본편의 브랜드·작전예산·오토캠·개발자 버튼 모두 도달 정상. 티커만 결정 패널이 가리는데 이는 의도된 동작이다.

### 배포

`game.css` v49 · `rts-combat.css` v60. 공개 서버에서 실제 클릭으로 재검증해 두 화면 모두 통과했다.

- 배포 매니페스트: `D:\soccerstarWebSource\GameCreator\hormuz\output\deployment\hormuz-v137-sftp-deploy.json`
- 홈 복귀 검증(실클릭): `D:\soccerstarWebSource\GameCreator\hormuz\output\validation\hormuz-brand-home-validation.json`

## 2026-08-01 — 한국어판 제목 「호르무즈」 · 공개 문안 문서 v138 (배포 완료)

### 한국어판 제목을 한글로

한국어로 접속하면 게임 이름이 「호르무즈」로 보인다. 영어판은 HORMUZ 그대로다.

- 타이틀 화면 대문 제목을 `data-i18n="brand.title"` 로 바꿔 사전에서 가져오게 했다.
- 이름이 제목으로 쓰이는 곳을 한글로 통일했다. 브라우저 탭 제목, 검색용 설명, 개발자 페이지의 제목·설명·소개 문장·게임 카드·바닥 버튼, 부팅 오류 문구.
- **좌상단 워드마크와 로딩 마크는 라틴 표기를 유지했다.** 그 둘은 이름이 아니라 로고로 기능하고, `IBM Plex Mono` 로 조판된 자리다.
- 대문 제목은 한글일 때만 본문 글꼴(Pretendard 계열)과 자간 `-.02em` 을 쓴다. IBM Plex Mono 에 한글이 없어 그대로 두면 대체 글꼴로 떨어지며 자간이 벌어진다. `html[lang="ko"]` 로 분기한다.
- 공개 서버 실측: ko 는 `호르무즈` / Pretendard, en 은 `HORMUZ` / IBM Plex Mono.

### 공개 문안 문서

`docs/release/hormuz-launch-copy-2026-08-01.html` 를 만들었다. crimeGame 의 공개 실행안과 같은 형식이되, 이번에는 **한국어는 Threads, 영어는 Reddit** 으로 채널을 나눴다.

- 한 링크가 접속 국가로 언어를 가르므로 채널별 페이지를 따로 만들 필요가 없다. 레딧에는 판별 실패 대비로 `?lang=en` 을 붙여 올린다.
- 글에 쓸 수 있는 사실 8종을 저장소 데이터에서 직접 세어 넣었다. 전투 7종, 캠페인 54일, 엔딩 10가지, 런타임 GLB 32종, 정책 카드 35장, 과거 사건 6단계, 하루 3회 결정, 2개 언어.
- Threads 문안은 링크를 본문에서 빼고 첫 답글에 다는 구성이다. 메인 게시글·첫 답글·대체 도입부·이어붙일 글 3종.
- Reddit 문안은 반대로 링크를 본문에 넣고 만든 과정과 한계를 먼저 말한다. 서브레딧 5곳의 성격과 주의사항, 제목 후보 3안, r/WebGames·r/playmygame 본문, r/threejs 기술 각도 본문, 자기 답글로 붙일 첫 댓글.
- **r/geopolitics 류는 올리지 않는다**고 명시했다. 홍보 금지에 민감한 주제라 역효과가 크다.
- 복사 버튼 7개, 채널별 권장 시각, 올리기 전 점검 6항목, 공개 뒤 볼 지표를 담았다.
- 과장 금지 항목을 따로 뒀다. “실제 군사 시뮬레이션” 같은 표현은 쓰지 않는다.

### 검증·배포

i18n 6화면, 한/영 전체 주행 완주, 홈 복귀 실클릭 게이트 전부 통과. `game.css` v50 · `game.bundle.js` v67 · 사전 405키.

- 배포 매니페스트: `D:\soccerstarWebSource\GameCreator\hormuz\output\deployment\hormuz-v138-sftp-deploy.json`
- 공개 문안: `D:\soccerstarWebSource\GameCreator\hormuz\docs\release\hormuz-launch-copy-2026-08-01.html`

## 2026-08-01 — Threads 영상 중심 단문 공개안 보강

- Threads 메인 문안을 긴 기능 설명형에서 **15~25초 실제 플레이 영상 + 짧은 행동 유도형**으로 줄였다.
- `바이브코딩으로 만든 3D 전략게임`이라는 표현을 넣고, 영상 시청 뒤 댓글에 `호르무즈`를 남기면 주소를 보내는 구조로 정리했다.
- 댓글 답장도 게임 주소와 한 문장 안내만 남겼다.
- 실존 정치인 이름을 쓰던 대체 도입부를 삭제해 기존 공개 원칙과 충돌하지 않게 했다.
- 영상은 전장 전경 0~4초 → 실제 전투 4~18초 → 결정 화면과 댓글 유도 18~25초 순서로 명시했다.
- 사용자 재검토에 따라 메인 Threads 문안에서 대통령·전력·지도 기능 설명을 모두 빼고, `바이브코딩` 소개와 `호르무즈` 댓글 유도만 남긴 초단문으로 다시 줄였다.

## 2026-08-01 — 플레이 데모 영상 자동 녹화기 v139

마케팅브레인의 `tools/asset_demo_video.py` 가 쓰는 Playwright 네이티브 녹화(`record_video_dir`)를 가져와 호르무즈 전용 녹화기 `tools/record-demo-video.py` 를 만들었다.

### 왜 그대로 못 쓰는가

마케팅브레인 원본은 **보이는 버튼을 아무거나 누르는 범용 드라이버**다. 호르무즈에서는 그 방식이 통하지 않는다. 결정 카드를 무작위로 누르면 흐름이 엉키고, 전투는 부대를 실제로 움직여야 그림이 나온다. 그래서 화면별 전용 대본을 따라간다.

대본: 타이틀 → 과거 사건 3단계(선택지 훑기 포함) → 전투 편성 → 전 부대 선택·공격 이동 → 교전 34초 → 결과.

### 만들면서 잡은 함정 4건

1. **`autostart=1` 은 타이틀을 건너뛴다.** 데모의 첫 장면이 타이틀이므로 이 파라미터를 쓰면 안 된다.
2. **과거 사건의 전투는 iframe 이라 녹화 중 초기화가 안 끝난다.** 본편 월드가 살아 있는 채로 두 번째 WebGL 컨텍스트를 띄우면 90초 안에도 준비되지 않는다(단독 화면은 8초). 과거 구간은 전투가 딸리지 않은 선택지(타임라인 앞 세 단계의 마지막 안)를 고르고, 전투는 `rts-combat.html` 로 직접 진입해 따로 찍는다.
3. **편성 화면 시작 버튼이 화면 밖에 있었다.** 1600×900 에서 `#start-battle` 이 y=1121 이라 좌표 클릭이 허공을 쳤다. `scroll_into_view_if_needed` 후 뷰포트 안인지 확인하고 누른다. 확인 없이 `#briefing-layer` 의 첫 버튼을 집으면 **의회 추가예산 모달**로 빠진다.
4. **첫 프레임이 영어 제목이다.** 녹화는 페이지가 열리는 순간부터 도는데 HTML 정적 기본값은 `HORMUZ` 이고 사전이 오면 `호르무즈` 로 바뀐다. 제목이 실제로 바뀐 시각을 기록해 두었다가 후처리에서 그 앞을 잘라낸다.

### 구조

- `Director` — 대본 진행과 예산 관리. **로딩 대기는 예산에서 돌려준다**(`grant`). 기다림은 연출이 아니므로 시연 시간을 깎으면 안 된다.
- 클릭은 전부 **실제 포인터**(`page.mouse.click`)다. HUD 는 `pointer-events:none` 이 걸린 곳이 있어 합성 클릭은 실제 동작을 보증하지 못한다.
- 후처리 — ffmpeg 를 찾으면 도입부를 잘라내고 업로드용 MP4(H.264, `+faststart`)를 함께 만든다. 없으면 원본 webm 만 남긴다.

### 산출물

| 파일 | 해상도 | 길이 | 용량 | 용도 |
|---|---|---:|---:|---|
| `hormuz-demo-ko-landscape.mp4` | 1600×900 | 103초 | 8.3MB | 스레드·유튜브 |
| `hormuz-demo-en-landscape.mp4` | 1600×900 | 108초 | 9.3MB | 레딧 |
| `hormuz-demo-ko-portrait.mp4` | 1080×1920 | 108초 | 11.1MB | 쇼츠·릴스 |

### 남은 한계

- **세로형은 전투 화면 HUD 가 빽빽하다.** RTS UI 가 세로비를 전제로 만들어지지 않았다. 쇼츠에 쓰려면 전투 구간만 가로로 찍어 크롭하거나, 세로에서는 캠페인 화면 위주로 대본을 다시 짜는 편이 낫다.
- 전투 페이지로 넘어가는 순간 **검은 프레임이 1~2초** 들어간다. 페이지 이동 구간이라 피할 수 없다. 편집으로 자르거나 그 지점에 자막을 넣으면 가려진다.

사용: `python -X utf8 tools/record-demo-video.py --lang en --shape landscape`
공개 서버로도 찍을 수 있다: `--base https://sidak.kr/autodev/GameCreator/hormuz`

## 2026-08-01 — 세로 화면 카메라 프레이밍 · HUD 숨김 v140 (배포 완료)

### 세로 전투 화면 — 영상 문제가 아니라 모바일 사용자 문제였다

세로에서 함대가 좌하단으로 밀리고 화면 대부분이 빈 바다였다. 처음에는 HUD 가 빽빽한 줄 알았으나 실측해 보니 **HUD 는 겹치지 않았고 카메라 프레이밍이 원인**이었다. 이 화면은 세로로 잡은 휴대폰 사용자가 그대로 보는 화면이다.

- 원인: three.js 의 `fov` 는 **세로 기준**이다. `resize()` 는 `camera.aspect` 만 갱신했으므로 세로 화면에서 세로 시야는 그대로인 채 **가로 시야만 좁아졌다.** 9:16 에서 가로로 보이는 폭이 16:9 의 약 1/3 이 된다.
- 수정 1 — 가로 폭 회복: `aspectFraming()` 이 화면비에서 부족분을 계산해 `fov` 를 상한(62°)까지 넓히고, 그래도 모자란 몫만 카메라를 물려(최대 1.75배) 채운다. fov 만 넓히면 원근이 과장돼 함선이 휘어 보이고, 거리만 물리면 유닛이 지나치게 작아지므로 둘을 나눠 쓴다.
- 수정 2 — 시선 각도: 높이·거리를 같은 비율로 늘리면 시야만 커져 화면 위쪽이 하늘로 채워졌다. 세로일수록 카메라를 세워(9:16 에서 높이 ×1.34, 거리 ÷1.34) 시선을 수면 쪽으로 내린다. 카메라 각도가 49° → 64° 가 되며 하늘이 사라지고 세로로 긴 화면이 전장의 앞뒤 깊이를 담는다.
- 확대·추적 값(`cameraHeight`/`cameraDistance`)은 건드리지 않는다. 그리는 순간에만 배율을 곱해 휠 확대 한계 같은 기존 규칙이 흔들리지 않게 했다.

| 화면 | fov | 배율 | 각도 | 유닛 가시 |
|---|---:|---:|---:|---|
| 1600×900 | 45° | 1.00 | 49.2° | 9/9 |
| 1080×1920 | 62° | 1.75 | 64.3° | 9/9 (수정 전 좌하단 치우침) |
| 430×932 | 62° | 1.75 | 64.3° | 9/9 (수정 전 8/9) |

### HUD 숨김

`?hud=` 로 전투 화면의 표시 수준을 정한다. 촬영·스크린샷용이며 `window.__HORMUZ_RTS__.setHud()` 로 중간에도 바꿀 수 있다.

| 값 | 동작 |
|---|---|
| `1`(기본) | 전부 표시 |
| `0` | 계기·패널·명령바·힌트를 숨기고 전장과 유닛 이름표만 남긴다 |
| `none` | 이름표·접지 그림자까지 지운 완전히 깨끗한 전장 |

### 녹화기

`tools/record-demo-video.py` 에 `--hud` 옵션을 붙였다. `--hud 0` 이면 파일명에 `-hud0` 이 붙어 원본과 구분된다.

### 산출물

| 파일 | 해상도 | 용량 |
|---|---|---:|
| `hormuz-demo-ko-landscape.mp4` | 1600×900 | 8.3MB |
| `hormuz-demo-en-landscape.mp4` | 1600×900 | 9.8MB |
| `hormuz-demo-ko-portrait.mp4` | 1080×1920 | 11.1MB |
| `hormuz-demo-ko-portrait-hud0.mp4` | 1080×1920 | 8.8MB |

### 검증·배포

RTS 7시나리오, i18n 6화면, 미션 라우팅, 적응형 난이도, 전체 주행 한/영, 홈 복귀, 달성 불가 종료 전부 통과. 공개 서버에서 모바일 세로(430×932)와 세로 HUD 숨김 모두 유닛 9/9 가시를 확인했다.

`rts-combat.bundle.js` v129 · `rts-combat.css` v61 · `game.bundle.js` v68 · 전투 데이터 v37 · RTS 런타임 129

- 배포 매니페스트: `D:\soccerstarWebSource\GameCreator\hormuz\output\deployment\hormuz-v140-sftp-deploy.json`

## v130 — 좁은 화면 HUD 겹침 제거 (2026-08-01)

세로 카메라를 고친 뒤 찍은 스크린샷에서 별개 결함이 드러났다. 430px 폰에서 전술 지형
배지와 좌우 패널이 목표 문구("이란 고속정 8척 막기 · 유조선 2척 이상 지키기")를 덮어
**지금 무엇을 해야 하는지 읽을 수 없었다.** 아군 목록은 아래가 잘려 4기 중 2기만 보였다.

### 원인 — 하드코딩한 오프셋 대 실제 높이

헤더 높이는 고정이 아니다. 화면이 좁아지면 목표 문구가 줄바꿈하고, 영문은 같은 문장이
한글보다 길어 한 줄을 더 먹는다. 실측하면 **102px(세로 한글) ~ 197.5px(320px 폭 영문)**
까지 벌어지는데 CSS 는 패널 위치를 110px·132px 상수로 박아두고 있었다.

| 화면 | 헤더 실측 | CSS 가정 | 초과 |
|---|---:|---:|---:|
| 430×932 한글 | 158.0px | 132px | +26px |
| 430×932 영문 | 177.5px | 132px | +45.5px |
| 320×568 영문 | 197.5px | 132px | +65.5px |
| 1080×1920 영문 | 141.0px | 110px | +31px |

폰만의 문제가 아니었다. 전술 지형 배지(top:91px)는 **모든 화면에서** 레이더 패널
상단을 22px 덮고 있었고, 세로 1080 영문은 데스크톱 규칙에서도 헤더가 흘러넘쳤다.

### 수정

1. `syncHudMetrics()` 가 헤더·배지·명령바·선택 패널의 실제 높이를 재서 `--hud-top`,
   `--hud-badge`, `--hud-bottom`, `--hud-left-bottom` 으로 내보낸다. CSS 의 위치 상수를
   전부 이 변수로 바꿨다. `ResizeObserver` 로 지켜본다 — 줄바꿈은 resize 이벤트를
   일으키지 않아 window resize 만으로는 못 잡는다.
2. `max-height` 를 `max(72px, calc(...))` 로 감쌌다. 세로가 짧으면 calc 결과가 음수가
   되는데, **음수 max-height 는 선언 자체가 무효가 되어 상한이 통째로 사라진다.**
   눕힌 폰에서 패널이 내용만큼 늘어나 아래를 덮은 원인이 이것이었다.
3. 유닛 이름표를 화면 안으로 밀어 넣는다. 치수는 유닛마다 캐시하고 리사이즈 때만
   버린다 — 초당 29회 도는 루프에서 `offsetWidth` 를 읽으면 강제 리플로우가 난다.
   레이아웃 전(캔버스 폭 0)에는 갱신을 건너뛴다. 그대로 두면 이름표가 좌상단 한 점에
   뭉치고 화면이 멈추면 그대로 남는다.
4. 세로 여유가 없는 화면은 글자 크기(15px 하한)를 건드리지 않고 여백만 줄인다.
   높이 800px 이하 노트북과 폰에서는 함급 설명을 접는다 — 부대 이름은 위 줄에 남는다.
5. 헤더 z-index 를 20 → 22 로 올렸다. 어떤 이유로 겹치더라도 목표 문구가 먼저 읽혀야 한다.
6. 눕힌 폰(높이 500px 이하)은 헤더를 접는다. 헤더 하나가 화면의 42% 를 먹었다.

### 검증

`tools/validate-hud-narrow.py` 신설. 13개 화면 × 한/영 × 지형 2종 = **52 케이스 전부 PASS.**
겹침 픽셀, 화면 밖 이탈, 아군 목록 잘림, 이름표 잘림을 전부 수치로 잰다.

지형을 두 갈래 다 재는 이유가 있다. 처음에는 `google=0`(로컬 지형)으로만 쟀는데,
Google 3D 가 붙으면 배지 문구가 `GOOGLE 3D 실제 전투 지형` 으로 `로컬 전술 지형` 보다
길어져 배지가 커진다. **배지 높이가 아래 레이더 패널의 위치를 정하므로** 한쪽만 재면
실제 서비스 상태를 검증하지 못한다. `--google 1` 로 둘 다 돌린다.

| | 수정 전 | 수정 후 |
|---|---|---|
| 겹침 있는 케이스 | 12/12 | 0/26 |
| 이름표 잘림 | 최대 81.3px | 0 |
| 아군 목록 가려짐 | 162~177px | 0 (높이 700px 미만은 스크롤 허용) |

높이 700px 미만(320×568, 눕힌 폰)은 4기를 다 담을 세로가 물리적으로 없어 스크롤이
정답이다. 그 경우에도 겹침은 0 이다.

RTS 7시나리오, 미션 라우팅, i18n 6화면, 홈 복귀 전부 통과. 삼각형 최대 96,369/120,000.

`rts-combat.bundle.js` v130 · `rts-combat.css` v62 · `game.bundle.js` v69 · RTS 런타임 130

### 배포 (2026-08-02)

공개 서버에 올렸다. 5개 파일 교체(index.html · rts-combat.html · rts-combat.css ·
rts-combat.bundle.js · game.bundle.js), 나머지 18개는 sha256 이 같아 건너뛰었다.
원격 백업 `/GameCreator/hormuz/_backup/20260802T002656Z`.

배포 확인을 로컬 통과로 갈음하지 않았다. **공개 서버 주소로 26 케이스를 다시 재서 전부
통과**했고, 런타임이 `130`, `--hud-top` 이 런타임에서 `164px` 로 계산되는 것을 확인했다.

`deploy-sidak.py` 의 공개 확인 표지에 `--hud-top`(CSS)과 `syncHudMetrics`(번들)를 넣었다.
번들을 다시 만들지 않고 올려 CSS 만 새 것이 되는 사고를 이 표지가 잡는다.

보고서: `docs/hormuz-hud-narrow-fix-2026-08-02.html` — 배포 전/후 공개 서버 화면을
8종 나란히 담았다. 스크린샷은 문서 안에 들어 있다.

## v134 — 본편 작전 화면이 지도를 가리는 양 줄이기 (2026-08-02)

사장님 지적: "기본 작전 상태에서 너무 가린다." 재보니 폰에서 상단 HUD 가 213px,
선택 결과 패널이 뜨면 합쳐서 화면의 55%(360px 폭에서는 76%)가 가려졌다.
지도가 이 게임의 절반인데 절반이 안 보였다.

### 고친 것

| | 전 | 후 |
|---|---:|---:|
| 430×932 대기 가림 | 33.0% | **21.8%** |
| 430×932 지도 온전한 띠 | 496px | **608px** |
| 360×800 대기 가림 | 38.7% | **26.8%** |
| 상단 HUD 높이 | 213px | **147px** |

1. 지표 타일 하나가 63px 였는데 실제 글자는 30px 이었다. 나머지는 전부 빈 여백이었다.
   글자 크기(15px 하한)는 건드리지 않고 최소 높이·여백만 줄였다. 아이콘은 좁은 폭에서
   이름 자리를 빼앗아 접었다 — 이름이 이미 뜻을 말한다.
2. 선택 결과는 최대 5줄까지 늘어났다. 폰에서는 3줄로 제한하고 한 항목을 한 줄로 폈다.
3. 결정 패널에서 **세 번째 선택지가 스크롤해야 보였다.** 영문·360px 폭에서는 아예 못 눌렀다.
   설명을 한 줄로 끊고 여백을 줄였다. 그래도 넘쳐서 상한을 62vh → 68vh 로 올렸다.
   결정 화면은 답을 요구하는 순간이라, 못 본 선택지가 생기는 것보다 그 순간 지도를
   조금 더 덮는 편이 낫다. 대기 상태(하루의 대부분)의 가림은 이 값과 무관하다.
4. 지명 라벨 충돌 회피(`world.js updateLabels`). 숨기지 않고 위아래로 비켜 놓는다.
   숨기기만 하면 지도가 확대됐을 때 17개 중 1개만 남는다. 이미 떠 있던 지명이 자리를
   지키게 해 깜빡임도 없앴다. 라벨 치수는 1초마다 다시 잰다 — 글꼴이 늦게 붙으면
   좁게 잰 상자로 배치한 결과가 그대로 남아 실제로는 겹친 채 표시된다.
5. 데스크톱: 범례가 브랜드·지표 타일 아래로 5px 파고들어 있었고(상시),
   AUTO CAM 이 개발자 버튼과 115×14px 겹쳤다. 세로로 줄을 세웠다.

### 검증

`tools/validate-campaign-hud.py` 신설. 대기·결과·결정 세 상태를 각각 재고,
지형 2종(Google 3D · 로컬) × 4화면 × 한/영 = **16 케이스 전부 PASS.**

합격선은 "HUD 가 몇 %냐" 가 아니라 **"HUD 에 한 번도 걸리지 않는 가로 띠가 화면 높이의
55% 이상"** 으로 잡았다. 지도가 위아래 띠에 잘려 가운데만 남으면 상황을 못 읽는다.

### 검증하다 나온 함정 (다음 사람이 같은 데 빠지지 않도록)

- **`.decision-panel` 은 닫혀 있을 때 `translate(-50%,115%)` 로 화면 밖에 숨는다.**
  그 상태를 재면 "선택지가 화면 밖 707px" 이라는 가짜 결함이 나온다. 한 번의 evaluate
  안에서 전환을 끄고 `.open` 을 걸어 고정한 뒤 재야 한다.
- **`debug=fast` 는 54일을 30초에 끝낸다.** 대기 상태가 거의 존재하지 않아
  "대기 상태 진입 실패" 가 난다. 지형이 붙기(9초)를 함께 기다리면 영영 못 잡는다.
- **`.google-terrain-active .map-label { display:none }`** — Google 3D 가 붙으면 도시
  지명을 CSS 로 감춘다(구글 지도가 자체 지명을 그린다). 라벨 겹침을 검사하려면
  로컬 지형(`google=0`)으로 재야 한다.
- **지명은 150ms 페이드로 사라진다.** 사라지는 중인 라벨을 세면 없는 겹침이 보고된다.
  계산 결과를 보려면 inline `style.opacity` 를 봐야 한다.

기존 검증도 다시 돌렸다 — i18n 6화면, 캠페인 전체 주행 한/영, 페이싱 전부 통과.

`game.css` v51 · `game.bundle.js` v70 · 본편 런타임 134 — 공개 서버 배포 전.

## 화면 녹화기 `record.html` (2026-08-02)

자동 녹화기(`tools/record-demo-video.py`)로 만든 영상은 화질이 모자란다. 헤드리스
크롬이 GPU 없이(swiftshader) 그린 화면이라 실제 플레이보다 나쁘게 나온다.
사장님 PC 가 실제로 그리는 화면을 그대로 담는 편이 낫다.

`record.html` 은 브라우저 화면 공유(`getDisplayMedia` + `MediaRecorder`)만 쓴다.
설치할 것도 서버도 없다.

- 녹화 버튼 하나로 시작·중지. 크롬 하단의 '공유 중지' 를 눌러도 똑같이 끝난다.
- 게임 창을 원하는 크기로 열어 준다. **폭이 정확해야 폰 배치가 나온다** —
  430px 를 넘으면 데스크톱 배치가 찍힌다. 테두리 두께는 브라우저마다 달라
  짐작하지 않고, 연 뒤 안쪽 크기를 재서 모자란 만큼 `resizeTo` 로 다시 맞춘다.
  못 맞추면 실제 크기를 그대로 알려 준다(조용히 넘어가지 않는다).
- 형식은 브라우저가 지원하는 것 중 가장 나은 것을 고른다. MP4 를 지원하면 MP4다.
- 화질 8/16/24 Mbps · 30/60fps 선택.
- https 나 localhost 가 아니면 버튼을 잠그고 이유를 먼저 알려 준다.

`tools/deploy-sidak.py` 의 배포 목록과 공개 확인 표지에 넣었다.

## 2026-09-01 — GitHub 전체 소스 공개 배포

- 기존 비공개 `soccerstar` 모노레포를 공개로 바꾸지 않고, `hormuz` 공개 배포본만 독립
  저장소 `https://github.com/sminia81-rgb/hormuz` 로 분리했다.
- 공개 빌더가 실사용 코드·문서·검증 도구·런타임 자산 286파일(약 95MB)을 포함하고,
  지도 API 키·서버 접속 정보·미사용 Meshy 원본·내부 생성 참고자료는 제외하도록 확인했다.
- ElevenLabs 무료 플랜으로 만든 효과음은 MIT 상업 이용 허용과 충돌하므로 공개본에서
  제외했다. 공개 `rts-audio.json` 은 빈 이벤트를 사용해 무음 폴백한다.
- README, MIT LICENSE, DISCLAIMER, CONTRIBUTING, ASSET-LICENSES, `.gitignore`,
  `.gitattributes`, `.nojekyll` 을 공개본에 포함했다. Meshy 모델은 CC BY 4.0으로 고지했다.
- 비밀정보 정규식 검사 0건, 공개 설정의 API 키·Map ID 빈 값 확인, 로컬 공개본에서
  실제 포인터 기반 본편·전투 홈 복귀 검증 PASS.
- GitHub Pages를 `main` 루트에서 활성화했다:
  `https://sminia81-rgb.github.io/hormuz/`.

## 2026-09-01 — Water Castle Games 계정으로 공개본 재업로드

- 기존 공개본의 커밋 이력을 보존해 `https://github.com/watercastlegames/hormuz`로
  재업로드했다. 가져온 `main` HEAD가 기존 공개본과 같은
  `e03b65f030866f6e57b68e6787fadf76c89d12a9`임을 확인했다.
- 새 저장소는 공개로 설정하고 기존 설명을 복원했다. GitHub Pages는 `main` 루트에서
  활성화해 `https://watercastlegames.github.io/hormuz/`로 게시되도록 했다.
- 공개 README의 온라인 플레이·소스 링크를 새 계정 주소로 교체했다.
- 이전 `sminia81-rgb/hormuz` 저장소는 삭제 요청이 없어 보존했다.
