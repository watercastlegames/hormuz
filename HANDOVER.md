# 호르무즈 (HORMUZ) — 개발 인수인계 문서

> 작성: 2026-07-27 · 3D RTS 지휘 전투 지시 반영: 2026-07-28 · 발주자: 사장님(watercastlegames) · 수행자: 이 문서를 읽는 AI 개발 에이전트
> 작업 디렉터리: `D:\soccerstarWebSource\GameCreator\hormuz\` — **이 폴더 밖을 수정하지 마라.**

---

## 0. 한 문단 요약

호르무즈 해협에서 벌어지는 미국-이란 전쟁(2026년 7월 현재 실제 소강 국면)을 소재로 한 **웹 브라우저 3D 지휘 시뮬레이션**. 플레이어는 미국 대통령이 되어 18일 안에 전쟁을 끝낸다. **홍보용 무료 게임 + MIT 오픈소스 공개**가 목적이며, 한 판 8~10분, 모바일 우선, 서버 0원(정적 배포 + Firebase 랭킹만 클라우드). 같은 폴더의 자매 프로젝트 `../crimeGame/`(마크의 마지막 수리)이 검증한 관례를 계승한다.

---

## 1. 정본 문서 3종 (반드시 먼저 읽어라)

| 문서 | 경로 | 답하는 질문 |
|---|---|---|
| 기획서 v0.5 | `docs/hormuz-game-design.html` | 무슨 게임인가 — 실화 연표, 4막 구조, 계기판, 이란 AI, 카드 34장, 휴전 협상, 엔딩 10종 |
| 화면 구성서 v1.0 | `docs/hormuz-screen-design.html` | 어떻게 보이는가 — 자동 진행 방송 UI, 실지도 좌표 30점, 3D 오브젝트 목록, 성능 가드레일 |
| 코딩 설계서 v1.0 | `docs/hormuz-code-design.html` | 어떻게 만드는가 — 점수 공식, 파일 구조, 상태 머신, Firebase rules 전문, 마일스톤 |

**충돌 시 우선순위: 이 문서(HANDOVER.md) > 코딩 설계서 > 화면 구성서 > 기획서.**
문서는 HTML이다 — 표와 `<pre>` 코드 블록 안에 정본 수치·코드가 있으니 대충 훑지 말고 파싱해라.

---

## 2. 이미 확정된 결정 — 재논의 금지

사장님이 이미 결정한 사항이다. 다시 묻거나 임의로 바꾸지 마라.

1. **인터랙션 = 장시간 선택 + 3D RTS 지휘 전투 임무.** 3D 전장이 화면 전부. 아침 방침과 긴급 대응은 각각 180초·120초로 충분히 고민하게 하고, 전투 상황은 임무 브리핑 → 전력 선택 → 3D 전술 배치·교전 명령 → 목표 달성 판정으로 진행한다. 카드 손패·AP UI 없음(카드 34장은 내부 데이터로만).
2. **시간 구조 = 하이브리드.** 재연 챕터(2025 12일 전쟁 결정 ①~③ + 2026 브리지 결정 ④~⑤, 결과는 역사로 수렴, 시작 지표 ±5 보정) → 본편 18일(2026-07-28~08-14) 자유 시뮬레이션. 2회차부터 재연 스킵.
3. **조작 = 미니게임·직접 운전 폐기, 스타크래프트식 3D RTS 지휘.** 전투기·구축함·헬기·무인소해정 등 임무 전력을 고른 뒤 좌클릭 단일 선택, 드래그 다중 선택, 우클릭 이동·집중 공격, 공격 이동, 위치 사수, 지휘 그룹으로 명령한다. 적도 호송대와 중동 지역 미군 거점을 반격한다. 최초 P0 임무는 `IRGC 고속정 8척 무력화 + 유조선 2척 이상 생존`이며 방공·소해·전략 타격도 같은 3D RTS 임무 시스템을 재사용한다.
4. **브리핑 = 고정 텍스트.** JSON 문구 풀. **외부 AI API(Puter.js 등) 호출 금지.**
5. **언어 = 한/영 동시.** `strings.ko.json` / `strings.en.json` 문자열 테이블, 처음부터 이중화. 하드코딩 문자열 금지.
6. **실화/픽션 경계.** 실화 파트(오프닝 연표·재연 챕터)만 실명 사용(보도 사실만), DAY 1 이후 픽션은 전원 익명(대통령=직함만, 이란 신임 지도부=가상). 유혈·시신 묘사 금지, 첫 화면과 `DISCLAIMER.md`에 가상 시뮬레이션 고지.
7. **점수/랭킹.** 점수 공식은 코딩 설계서 2섹션의 `scoring.js`가 정본(속도+유가+지지율+국제+통항+확전 × 등급배수, 상한 3500). Firebase는 **최고점 갱신형**(rules가 `score > 기존`일 때만 update 허용) — rules 전문이 설계서 7섹션에 있다.
8. **성능 헌법** (화면 구성서 7섹션): 삼각형 12만·드로콜 90 상한, `setPixelRatio(min(dpr,2))`, 그림자맵 금지(블롭 셰도우), 블룸 대신 가산합성 글로우 스프라이트, 반투명 다층 겹치기 금지(단일 셰이더 합성), 스캔라인·비네트는 CSS.

---

## 3. 기술 스택·제약

- **Vanilla JS (ES2020) + three.js. 프레임워크·번들러 설정 파일·package.json 금지.**
- 빌드는 단 한 줄: `npx --yes esbuild assets/js/main.js --bundle --format=iife --target=es2020 --outfile=assets/js/game.bundle.js`
- three.js는 CDN 금지, 파일 동봉: `..\catAgentGame\node_modules\three\build\three.module.js` (r185)를 `assets/js/vendor/three.module.js`로 복사해 쓰라.
- 텍스트는 전부 HTML/CSS 오버레이 — **3D(WebGL) 안에 글자 렌더링 금지** (한글 글리프 함정).
- 모든 파일 UTF-8. index.html 스크립트 로딩 순서와 `?v=N` 캐시버스팅은 crimeGame 관례(코딩 설계서 1·3섹션).
- 데이터(cards/events/endings/timeline/strings/지리)는 전부 `assets/data/*.json` — 코드에 수치 하드코딩 금지.
- 해안선: Natural Earth 10m GeoJSON → `tools/build_coastline.py`(작성 필요)로 씬 경계(동경 54.9~57.0, 북위 25.0~27.3) 클리핑·단순화 → `assets/data/coastline.json`. 런타임 외부 요청 0. 도서·항구·TSS 좌표는 화면 구성서 4섹션 표의 검증값 하드코딩.

## 4. 참고 소스 (읽기 전용 — 수정 금지)

| 경로 | 가져올 것 |
|---|---|
| `..\crimeGame\assets\js\firebase-ranking.js` | CDN 동적 SDK 로딩·named app·popup 로그인·트랜잭션 패턴 (113줄) — 이식 후 컬렉션/정책만 개조 |
| `..\crimeGame\assets\js\state.js` | 전역 state + patch/notify + localStorage 화이트리스트 sanitize 복원 (75줄) |
| `..\crimeGame\assets\js\popup.js` | 범용 모달(포커스트랩·ESC·busy) (50줄) — 그대로 복사 가능 |
| `..\crimeGame\firestore.rules` | rules 작성 스타일 참고 (호르무즈용 전문은 코딩 설계서 7섹션) |
| `..\crimeGame\docs\guides\DEPLOYMENT.md` | 배포 범위·체크리스트 형식 |

**주의:** `..\crimeGame\assets\js\app.js`(2줄 리다이렉트 shim)는 죽은 레거시 — 복붙 금지. Puter 관련 코드도 가져오지 마라.

---

## 5. 작업 순서 — STEP 0부터 시작하라

### STEP 0 (지금 즉시): 3D 씬 퀄리티 시안 ★사장님 최우선 요청
- `mockup.html` 단일 파일 프로토타입: 실지도 지형(근사 좌표로 시작 가능) + 황혼 라이팅 + 물 셰이더 + 유닛 글로우(항모전단·구축함·고속정 스웜·유조선·기뢰밭) + 미사일 궤적/요격 섬광 1회 연출 + HUD 오버레이(날짜·계기판 6·속보 티커·선택 버튼 목업).
- **퀄리티 3단계를 쿼리 파라미터로**(`?q=basic|rich|cinematic` — 글로우 밀도·파티클·CSS 후처리 차등) 만들어 스크린샷 비교본을 뽑아라.
- 완료 기준: PC(1600×900)와 모바일 세로(430×932) 모두에서 화면이 "화려하다"고 느껴질 것. **사장님 컨펌 받고 다음 단계 진행.**

### M1: 룰 코어 (3D 없음) — 코딩 설계서 10섹션
18일 완주·이란 3태세·엔딩 10종·점수·저장/복구/재시작을 텍스트 UI로. 완료 기준: 시드 고정 자동 플레이 100판의 엔딩 분포·평균 점수 통계 출력.

### M2: 3D 전장 완성
STEP 0 씬을 본편에 결합 — 유닛 16종, 연출 프리셋 8종, 카메라 6패턴, 3D RTS 지휘 임무 4종, 막별 색온도. P0 해협 전투 버티컬 슬라이스는 `rts-combat.html`에서 먼저 검증한다. 완료 기준: 중급 폰 10분 플레이 55fps.

### M3: 랭킹·공유
Firebase 이식(최고점 갱신형), 엔딩 화면 점수 브레이크다운, 랭킹 100, 공유 카드 1080×1350, 다시 하기 루프. 완료 기준: "낮은 점수 갱신 거부" rules 테스트 통과.

### M4: 패키징
재연 챕터 ①~⑤, 영문 완성, README/LICENSE(MIT)/DISCLAIMER/개조 가이드/CONTRIBUTING, 배포 파일 정리.

---

## 6. 사람(사장님)만 할 수 있는 일 — 만나면 멈추지 말고 표시 후 우회하라

| 항목 | 처리 |
|---|---|
| Firebase 프로젝트 생성·config 발급·Google 로그인 활성화·도메인 승인·rules 게시 | `net/firebase-config.js`에 플레이스홀더(빈 문자열)로 두면 폴백 모드로 동작하게 구현. 체크리스트는 코딩 설계서 7섹션 |
| sidak.kr 실배포 (`/autodev/GameCreator/hormuz/`) | 배포 파일 목록만 정리해 두라 (index.html + assets, tmp·tools 제외) |
| 퀄리티·톤 최종 컨펌 | STEP 0 시안, M2 완성, M4 공개 직전 — 3회 컨펌 포인트 |

## 7. 함정 목록 (crimeGame에서 실제로 확인된 것)

1. 소스 수정 후 **esbuild 재번들을 안 돌리면 배포 화면에 반영 안 된다.** 수정→번들→`?v=` 갱신을 한 묶음으로.
2. localStorage 키는 반드시 호르무즈 고유(`hormuzProgressV1`, `hormuzLocalBestV1`, `hormuzReplaySeenV1`).
3. Firebase는 **신규 프로젝트 전용** — crimeGame의 config·컬렉션 재사용 절대 금지.
4. 저장값 복원 시 화이트리스트 sanitize 필수(범위 clamp, phase 유효성, 중간 비트는 그날 아침으로 되감기). 저장값을 신뢰하지 마라.
5. 결정 타이머는 장시간 폴백을 유지하되 3D 전투 임무는 플레이어가 RTS 명령으로 직접 수행한다. 디버그 자동 진행에서만 임무 성공을 빠르게 모사한다.
6. 유니코드 13+ 이모지 사용 금지(구형 기기 두부 글자). 아이콘은 캔버스/CSS로 그려라.
7. 완료 보고 전 반드시 실행 검증: 로컬 서버(`python -m http.server 8080`)로 열어 콘솔 에러 0 확인. `file://` 직접 열기는 모듈·fetch가 막힌다.

## 8. 진행 기록·보고 규칙

- 작업 단위마다 `PROGRESS.md`에 append: 날짜 / 한 일 / 남은 일 / 사장님 확인 필요 항목.
- 각 STEP·마일스톤 완료 시 실행 방법(명령·URL)과 스크린샷 경로를 함께 보고.
- 이 문서·정본 문서와 다르게 구현해야 할 사정이 생기면 **먼저 PROGRESS.md에 사유를 적고 사장님에게 물어라.** 임의 변경 금지.

---

## 9. 핵심 수치 카드 (빠른 참조 — 정본은 각 문서)

```
본편 시작값: 통항 30% · 유가 $84 · 지지 44 · 국제 55 · 탄약 45 · 확전 3/5 · 이란 안정도 40(숨김)
씬 경계: 동경 54.9~57.0° × 북위 25.0~27.3° (약 208×255km)
점수: (속도600+유가390+지지300+국제300+통항400+확전240) × 등급배수(S+1.5~F0.3), 상한 3500
속도점수: 협정 성립 엔딩만 (19-종결일)×40, 최속 성립 DAY 4
Firestore: 컬렉션 hormuz_rankings_v1, 문서ID=uid, create 1회 + score 상승 시만 update, delete 금지
정렬: 점수↓ → 종결일↑ → 통항률↓ → 선착순
빌드: npx --yes esbuild assets/js/main.js --bundle --format=iife --target=es2020 --outfile=assets/js/game.bundle.js
실제 Meshy 런타임 GLB: 9종(항모·구축함·유조선·크레인·TEL·F/A-18E·MH-60R·USV·IRGC 고속정), B-2만 절차형
배포 경로: https://sidak.kr/autodev/GameCreator/hormuz/
```

현재 P0 실행 산출물은 `rts-combat.html`이다. 다음 작업은 이 RTS 전투를 본편의 예산·전력 선택·지지도·확전도 결과에 연결하는 것이다.
