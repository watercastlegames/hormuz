# HORMUZ 인수인계 — 2026-07-29 (Claude → Codex)

> 이 문서는 **오늘(2026-07-29) 10:00~11:00 사이에 Claude가 한 작업**만 다룬다.
> 발주 범위·확정 결정·전체 작업 순서의 정본은 여전히 `HANDOVER.md`다. 그 문서를 먼저 읽어라.
> 이 문서는 그 위에 얹는 **세션 인수인계**다. 충돌하면 `HANDOVER.md` > 이 문서 순이다.

---

## 0. 한 문단 요약

너(Codex)의 세션이 오늘 09:07에 멈춘 지점에서 Claude가 이어받았다. 네가 v72~v82에서 넣은 **결합병과 확장과 신규 3개 시나리오는 코드가 완성되어 있었지만, 로드맵 반영·성능 측정·진행 기록 세 가지가 빠져 있었다.** 그 셋을 채웠고, 그 과정에서 **대규모 해전이 성능 헌법을 30% 초과(156,304 / 120,000 삼각형)하고 있던 것**을 발견해 고쳤다. 코드 로직·전투 규칙·연출은 건드리지 않았다. 바뀐 것은 모델 경로 3건, LOD 적용 범위 1건, 공중 위협 모델 1건, 캠페인 메타데이터, 그리고 새로 추가한 검증 도구 4개다.

---

## 1. 네가 멈춘 지점

| 항목 | 값 |
|---|---|
| 네 세션 | `~/.codex/sessions/2026/07/27/rollout-2026-07-27T17-01-52-019fa298-*.jsonl` |
| 마지막 기록 | 2026-07-29 09:07 |
| 마지막 파일 수정 | `assets/js/rts-combat.bundle.js` 09:06:23 |
| 코드 상태 | `rts-combat.html` 캐시 v82 |
| `PROGRESS.md` 마지막 기록 | **v71** |

**v72~v82 구간 11개 버전의 기록이 없다.** 그래서 그 구간에 무엇이 들어갔는지는 코드에서 역추적했다. 확인된 것은 결합병과(해병·IRGC 지상군·B-2·소총 배치), 항모·USV 추가, 그리고 `mine_corridor`·`missile_screen`·`coastal_battery` 시나리오 3종이다.

**앞으로는 이 구간을 만들지 마라.** 버전을 올리면 `PROGRESS.md`에 바로 append 한다. 기록 없는 구간은 다음 사람이 코드를 읽어서 복원해야 하고, 오늘처럼 측정 누락이 그 안에 숨는다.

---

## 2. 오늘 고친 것 — 성능 헌법 위반

### 발견

5개 시나리오를 실제 브라우저에서 처음으로 전부 측정했다. 결과:

| 시나리오 | 측정값 | 상한 대비 | 판정 |
|---|---:|---:|---|
| large_fleet_battle | 156,304 | **130.3%** | 위반 |
| coastal_battery | 119,236 | 99.4% | 상한 직전 |
| missile_screen | 116,059 | 96.7% | 상한 직전 |
| convoy_shield | 107,110 | 89.3% | — |
| mine_corridor | 70,780 | 59.0% | — |

v71 기록에는 `삼각형 14,825/120,000`으로 남아 있다. 그 뒤 결합병과·항모·USV가 들어오면서 10배가 됐는데 재측정이 없었다.

### 원인

**MCM USV가 16종 유닛 중 유일하게 감축본이 없었다.** 런타임 경로가 Meshy 원본 `mcm-usv-meshy6-web-v1.glb`(40,412 삼각형)를 그대로 가리키고 있었고, **이 한 척이 대규모 전투 한 프레임의 26%**를 먹었다. 다른 유닛은 전부 `-squad-` / `-lod3-` / `-strategic-` 감축본을 쓰고 있었다.

유닛 종류별 분해(대규모 전투, 수정 전):

```
ally:usv          n=4   tri=40,412   ← 한 척이 원본 그대로
enemy:enemyMarine n=12  tri=28,884
ally:marine       n=8   tri=20,144
ally:carrier      n=1   tri=14,103
ally:destroyer    n=3   tri=10,197
```

### 조치 5건

1. **MCM USV 전투 LOD 신규 생성** — `combat-v1/mcm-usv-meshy6-web-battle-lod-v1.glb` (40,412 → 17,767 삼각형, 3.0MB → 1.9MB). **Meshy 크레딧 0** — 로컬 meshoptimizer 단순화로 만들었다. 생성 스크립트는 `tools/make-battle-lod.mjs`.
2. **이미 있던 더 가벼운 변형으로 교체** — 이건 새로 만든 게 아니라 네가 이전에 받아둔 파일 중 안 쓰이던 것을 연결한 것이다.
   - IRGC 고속정 `lod3`(12,374) → `strategic-v4`(10,172)
   - VLCC 유조선 `lod3`(13,928) → `strategic-v2`(12,000)
   - 연안 TEL `strategic-v1`(19,545) → `strategic-v2`(17,563)
3. **공중 위협 모델 교체** — `missile_screen`의 `airThreat`가 전투기 **hero** 모델(6,415)을 8기 복제하고 있었다. **편대기** 모델(3,833)로 바꿔 8기 합계 51,320 → 30,664. 무인기 성격에도 편대기 실루엣이 맞는다.
4. **대규모 전투 LOD 예외에서 USV 제외** — 기존 규칙은 `sea && index > 0`이라 각 종류의 선두 1척은 정밀 모델을 유지했다. USV는 기함이 아니므로 `index > 0 || type === "usv"`로 바꿨다. **정밀 모델을 유지하는 것은 항모와 선두 구축함뿐이다.**
5. **불필요한 LOD 후보 삭제** — TEL·유조선·헬기·고속정도 새 LOD를 만들어봤으나 기존 Meshy strategic/lod3 변형이 더 가벼워 채택하지 않고 지웠다. 기록은 `assets/models/battle-lod-report-v1.json`.

### 결과

| 시나리오 | 이전 | 현재 | 상한 대비 | 드로콜 |
|---|---:|---:|---:|---:|
| convoy_shield | 107,110 | 96,648 | 80.5% | 51/90 |
| mine_corridor | 70,780 | 66,924 | 55.8% | 56/90 |
| missile_screen | 116,059 | 94,129 | 78.4% | 51/90 |
| coastal_battery | 119,236 | 111,308 | 92.8% | 46/90 |
| large_fleet_battle | 156,304 | 116,024 | 96.7% | 83/90 |

전부 콘솔 오류 0건, 바다 유닛 육지 침범 0건.

---

## 3. 오늘 고친 것 — 로드맵 정합성

`mine_corridor`·`missile_screen`·`coastal_battery`는 `rts-combat.js`에 시나리오가 완성돼 있고 `missions.json`에도 연결돼 있는데, `campaign.json`만 `next`/`planned`로 남아 있었다. 작전·예산실 화면이 **실제로 플레이 가능한 작전을 "다음 개발"로 거짓 표시**하고 있었다.

- 세 작전을 `playable`로 승격
- 5개 작전 전부에 `scenario` 키를 명시해 전투 화면과의 연결을 데이터에 박음
- `다음 개발` 표시는 `tanker_rescue`로 이동

현재 8개 작전 중 **5개 운용 중 · 1개 다음 개발 · 2개 기획 확정**이다.

---

## 4. 오늘 추가한 도구 4개 — 이게 제일 중요하다

오늘 문제의 본질은 코드가 틀린 게 아니라 **측정을 안 했다는 것**이다. 그래서 측정을 스크립트로 고정했다. 앞으로 유닛·모델·시나리오를 건드리면 이걸 돌려라.

| 도구 | 용도 | 실행 |
|---|---|---|
| `tools/validate-rts-scenarios.py` | **성능 게이트.** 5개 시나리오 삼각형·드로콜·콘솔오류·육지침범 측정 + 스크린샷 + JSON. 예산 초과 시 범인 유닛 자동 출력 | `python -X utf8 tools/validate-rts-scenarios.py --tag v84` |
| `tools/validate-mission-routing.py` | 카드→미션→시나리오 라우팅 5종 + `campaign.json` 표시 정합성 | `python -X utf8 tools/validate-mission-routing.py --tag v84` |
| `tools/glb-tri-count.py` | 전 GLB 삼각형 집계. **런타임 경로에 원본(2만 초과)을 잘못 물렸는지 즉시 경고** | `python -X utf8 tools/glb-tri-count.py --used-only` |
| `tools/make-battle-lod.mjs` | Meshy 원본 → 전투용 감축 LOD 생성 (크레딧 0) | `node tools/make-battle-lod.mjs` |

두 파이썬 도구는 **실패 시 exit code 1**을 반환한다. 커밋 전 게이트로 그대로 쓸 수 있다.

사전 준비:
```
pip install playwright
python -m playwright install chromium
# 로컬 정적 서버가 127.0.0.1:8080 에서 이 폴더를 서빙 중이어야 한다
```

`make-battle-lod.mjs`는 `@gltf-transform/core`·`@gltf-transform/extensions`·`@gltf-transform/functions`·`meshoptimizer`가 필요하다. **저장소에 `node_modules`를 만들지 마라.** 임시 폴더에서 설치해 실행하고 결과 GLB만 저장소에 남긴다.

### 본편을 헤드리스로 띄우는 법 (오늘 알아낸 것)

본편은 인트로 전투를 통과해야 본 게임에 들어가므로 그냥 열면 검증이 안 된다. director 객체까지만 만들고 멈추려면:

```
index.html?autostart=1&startMode=new&debug=fast&mapcheck=1&google=0
```

- `autostart=1` 없으면 타이틀 화면에서 클릭을 기다린다
- `debug=fast` 없으면 리플레이 → 인트로 전투로 넘어가 버린다
- `mapcheck=1`이 `director.start()` 직전에 멈춰 `window.__HORMUZ__`를 노출한다

`PROGRESS.md` v71에 적힌 `?autostart=1&prologue=complete&startMode=new&mission=large` 주소는 **지금은 인트로 전투로 넘어가고 본편 미션에 도달하지 못한다.** 그 주소를 검증용으로 다시 쓰지 마라.

---

## 5. 새로 확정된 규율 — 재논의 금지

`HANDOVER.md` 2섹션의 확정 결정 8건에 아래를 더한다.

1. **런타임 경로에 Meshy 원본(`-web-v1.glb`, 4만 삼각형대)을 쓰지 않는다.** 새 유닛을 도입하면 감축본을 먼저 만들고 그 경로를 `rts-combat.json`에 넣는다.
2. **유닛 추가·모델 교체 시 5개 시나리오를 전부 재측정한다.** 한 시나리오만 보면 안 된다. 오늘 위반은 대규모 전투에만 나타났지만 원인 모델은 다른 시나리오에도 들어 있었다.
3. **대규모 전투에서 정밀 모델을 유지하는 것은 항모와 선두 구축함뿐이다.** 지원 정선·편대기·원거리 부대는 전략 LOD로 통일한다.
4. **스킨(리깅) 메시는 로컬 단순화로 줄이지 않는다.** 해병·IRGC 지상군은 가중치가 깨질 수 있어 손대지 않았다. 더 줄여야 하면 Meshy 리메시를 사장님 승인 받고 쓴다.
5. **버전을 올리면 `PROGRESS.md`에 즉시 append 한다.** 11개 버전을 몰아서 기록하지 않는다.

---

## 6. 지금 상태와 남은 일

### 캐시 버전 현황

| 대상 | 버전 |
|---|---|
| `rts-combat.bundle.js` | v83 |
| `game.bundle.js` | v32 |
| `assets/css/rts-combat.css` | v52 |
| `assets/css/game.css` | v28 |
| 본편 데이터 fetch (`main.js`) | v29 |
| `assets/data/rts-combat.json` | version 8 |

### 남은 작전 3개

| 작전 | 표시 | 비고 |
|---|---|---|
| `tanker_rescue` (작전 04) | 다음 개발 | 나포 유조선 구출. 고속정 차단 후 구조 헬기 진입 |
| `base_retaliation` (작전 06) | 기획 확정 | 미군 기지 방공 + 발사 원점 대응. 전구 단위 |
| `ceasefire_cover` (작전 08) | 기획 확정 | 먼저 사격하지 않는 교전 규칙이 핵심 |

### 착수 전 반드시 알아야 할 것

**`coastal_battery` 여유 7.2%, `large_fleet_battle` 여유 3.3%뿐이다.** 유닛 한 종류만 더 넣어도 넘긴다.

- 새 작전에 새 유닛이 필요하면 **감축본을 먼저 만들고 시작하라.**
- `tanker_rescue`는 구조 헬기가 들어가는데 MH-60R이 13,323으로 무겁다. 기존 편대기 모델(4,885)로 충분한지 먼저 보라.
- `base_retaliation`은 전구 단위라 객체 수가 늘 가능성이 크다. 시작 전에 예산 계획을 세우고, 중간에도 `validate-rts-scenarios.py`를 돌려가며 진행하라.

---

## 7. 오늘 건드린 파일 전부

| 파일 | 변경 |
|---|---|
| `assets/js/rts-combat.js` | TEL 모델 → strategic-v2 / airThreat 모델 → 편대기 / 대규모전 USV LOD 예외 제외 (3곳) |
| `assets/data/rts-combat.json` | usv·fastBoat·tanker 모델 경로 교체, version 7 → 8 |
| `assets/data/campaign.json` | 3작전 playable 승격, 5작전에 scenario 키 명시, tanker_rescue → next |
| `assets/js/main.js` | 데이터 fetch 캐시 v28 → v29 |
| `rts-combat.html` | 번들 캐시 v82 → v83 |
| `index.html` | 번들 캐시 v31 → v32 |
| `assets/js/rts-combat.bundle.js` | 재번들 |
| `assets/js/game.bundle.js` | 재번들 |
| `assets/models/combat-v1/mcm-usv-meshy6-web-battle-lod-v1.glb` | **신규** |
| `assets/models/battle-lod-report-v1.json` | **신규** — LOD 생성 기록 |
| `tools/validate-rts-scenarios.py` | **신규** |
| `tools/validate-mission-routing.py` | **신규** |
| `tools/glb-tri-count.py` | **신규** |
| `tools/make-battle-lod.mjs` | **신규** |
| `PROGRESS.md` | v83 기록 append |
| `docs/hormuz-production-inventory.md` | 16장 「전투 런타임 LOD 규율」 신설 |

**건드리지 않은 것:** 전투 규칙·AI·연출·오디오·Google 3D 연동·UI 레이아웃·본편 카드/이벤트/엔딩 데이터. 오늘 작업은 모델 경로와 메타데이터, 그리고 측정 도구까지다.

### 검증 산출물

```
output/validation/hormuz-rts-v83-scenario-matrix-validation.json   ← 5시나리오 측정 + 유닛별 삼각형 분해
output/validation/hormuz-v83-mission-routing-validation.json       ← 라우팅 + 로드맵 정합성
output/validation/hormuz-rts-v83-<시나리오>-2560x1313.jpg  (5장)
output/validation/hormuz-v83-main-boot-1600x900.jpg
assets/models/battle-lod-report-v1.json
```

---

## 8. 첫 작업 지시

1. `HANDOVER.md`(정본)와 이 문서를 읽는다.
2. `PROGRESS.md` 맨 끝 v83 기록을 읽어 현재 상태를 파악한다.
3. **아무것도 고치기 전에** 로컬 서버를 띄우고 `tools/validate-rts-scenarios.py`와 `tools/validate-mission-routing.py`를 돌려 지금 상태가 전부 통과하는지 직접 확인한다. 통과하지 못하면 그 사실부터 보고하라.
4. 그 다음 `tanker_rescue`(작전 04)에 착수한다. **유닛을 추가하기 전에 `tools/glb-tri-count.py --used-only`로 예산을 먼저 계산하라.**
5. 버전을 올릴 때마다 `PROGRESS.md`에 append 한다. 몰아서 쓰지 않는다.

유료 리소스(Meshy 크레딧), 실서버 배포, Firebase 프로젝트 생성은 **사장님 승인 없이 진행하지 마라.** 막히면 멈추지 말고 플레이스홀더로 우회한 뒤 그 사실을 보고하라.
